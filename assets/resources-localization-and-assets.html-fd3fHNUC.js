import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as n,b as s,d,e as o,a as c,w as i,r,o as l}from"./app-CGOhbY2V.js";const p={};function u(g,e){const a=r("RouteLink");return l(),n("div",null,[e[10]||(e[10]=s('<h1 id="资源、本地化与素材" tabindex="-1"><a class="header-anchor" href="#资源、本地化与素材"><span>资源、本地化与素材</span></a></h1><h2 id="资源分类" tabindex="-1"><a class="header-anchor" href="#资源分类"><span>资源分类</span></a></h2><p>项目中有两类容易混淆的资源：</p><table><thead><tr><th>目录</th><th>构建方式</th><th>用途</th></tr></thead><tbody><tr><td><code>neo-bpsys-wpf/Assets</code></td><td>多数作为 WPF <code>Resource</code> 嵌入程序集</td><td>应用图标、首页图、字体、主题图标等 pack URI 资源</td></tr><tr><td><code>neo-bpsys-wpf/Resources</code></td><td><code>&lt;None Include=&quot;Resources\\**&quot; CopyToOutputDirectory=&quot;PreserveNewest&quot; /&gt;</code></td><td>运行时文件资源，按文件路径读取</td></tr><tr><td><code>neo-bpsys-wpf.SmartBp.Module/Resources</code></td><td><code>&lt;Content Include=&quot;Resources\\**&quot; CopyToOutputDirectory=&quot;PreserveNewest&quot; /&gt;</code></td><td>SmartBP 模块自有运行时资源，随模块复制到模块输出目录</td></tr></tbody></table><p><code>GameRule.json</code> 单独设置为 <code>CopyToOutputDirectory=Always</code>，输出到应用基目录，供 <code>GameGuidanceService</code> 读取。</p><h2 id="resources-目录" tabindex="-1"><a class="header-anchor" href="#resources-目录"><span>Resources 目录</span></a></h2><p>常见子目录：</p><table><thead><tr><th>目录</th><th>用途</th></tr></thead><tbody><tr><td><code>bpui</code></td><td>前台窗口 UI 背景、比分图、锁图、阵营图标</td></tr><tr><td><code>data</code></td><td><code>CharacterList.json</code> 及多语言角色列表</td></tr><tr><td><code>FrontedLayouts</code></td><td>v3 Window-centric 内置前台窗口默认布局</td></tr><tr><td><code>FrontedBehaviors</code></td><td>内置前台窗口默认行为与动画图</td></tr><tr><td><code>surBig/surHalf/surHeader/surHeader_singleColor</code></td><td>求生者不同展示尺寸/样式图片</td></tr><tr><td><code>hunBig/hunHalf/hunHeader/hunHeader_singleColor</code></td><td>监管者不同展示尺寸/样式图片</td></tr><tr><td><code>map/map_singleColor/map_square</code></td><td>地图图片</td></tr><tr><td><code>talent</code> / <code>trait</code></td><td>天赋和辅助特质图标</td></tr></tbody></table><p><code>ImageHelper</code> 使用 <code>AppConstants.ResourcesPath</code> 拼接这些目录，按文件路径加载。新增运行时图片时，应确认文件被放在 <code>Resources</code> 下并能复制到输出目录。</p><p>SmartBP 模块自有资源不放在主程序 <code>neo-bpsys-wpf/Resources</code> 下，应放在 <code>neo-bpsys-wpf.SmartBp.Module/Resources</code> 并由模块项目复制到输出目录。当前包括 <code>Resources/SmartBp</code> 下的 RapidOCR 模型 manifest、内置测试帧、BP 识别区域默认配置、OCR 角色别名，以及 <code>Resources/SmartBpDefaultConfigs</code> 下的赛后数据 OCR 默认区域配置。模型管理 UI 从相应 OCR manifest 动态读取 profile。</p><p>v3 默认布局采用 Window-centric 一级路径，位于 <code>Resources/FrontedLayouts/{WindowTypeName}.json</code>。每个 v3 layout window 运行时固定生成 <code>ViewBox -&gt; Canvas BaseCanvas</code>，Canvas 不再是资源路径或包管理单位。CutScene 背景使用 <code>Resources/cutScene.png</code>（解析到运行目录 <code>Resources/bpui/cutScene.png</code>），GameData 背景使用 <code>Resources/gameData.png</code>，BpWindow 背景使用 <code>Resources/bp.png</code>。<code>WidgetsWindow</code> 和 MapV1 已删除；旧 <code>BpOverViewCanvas</code> 迁移为 <code>BpOverviewWindow.json</code>，旧 <code>MapV2Canvas</code> 迁移为 <code>MapV2Window.json</code>，MapV2 背景继续使用 <code>Resources/mapBpV2.png</code>。内置业务控件复用这些资源目录：<code>TalentTraitDisplay</code> 通过 <code>ImageHelper.GetTalentImageSource</code> / <code>GetTraitImageSource</code> 读取 <code>Resources/talent</code> 和 <code>Resources/trait</code>，以图标 alpha 作为遮罩应用 <code>Color</code> 单色覆盖，默认且必填为白色；Ban 位默认布局使用通用 <code>Image</code> 绑定角色 <code>HeaderImageSingleColor</code>，锁定覆盖层优先使用 <code>LockImagePath</code>，为空时回退内置锁图；pick 呼吸边框优先使用 <code>PickingBorderImagePath</code>，为空时回退内置 BP 选择边框图；<code>CurrentBanDisplay</code>、<code>BanSlotDisplay</code> 和 <code>PickingBorderOverlay</code> 已移除；<code>MapV2Display</code> 复用现有 <code>MapV2Presenter</code> 并使用 v3 运行时默认样式。旧 Config.json 中可映射的图片会迁移到 v3 layout，旧前台设置不再作为 active Settings 运行时来源。</p><p>普通图片展示有两个内置控件类型：<code>Image</code> 和 <code>BorderedImage</code>。<code>Image</code> 是通用图片控件，<code>Canvas.Left</code> / <code>Canvas.Top</code> / <code>Width</code> / <code>Height</code> / <code>ZIndex</code> 作用于承载主图和内部 overlay 的根元素。<code>BorderedImage</code> 是外层 <code>Border</code> + 内部图片层，适合需要外层容器裁剪、外框 resize 或内层对齐控制的图片区域，例如角色 pick 图。两者的图片路径解析规则相同：<code>BindingPath</code> 绑定到 <code>ISharedDataService</code> 上的动态 <code>ImageSource</code>，<code>ImagePath</code> 保存静态资源图片路径。<code>BindingPath</code> 非空时优先使用绑定并忽略 <code>ImagePath</code>；<code>BindingPath</code> 为空且 <code>ImagePath</code> 非空时，运行时按 v3 资源 resolver 加载静态图。两者也共享 <code>Lockable</code> 和 <code>PickingBorderAvailable</code> overlay 资源路径解析；锁定图和选择边框可以分别使用独立 Stretch，关闭对应独立设置时跟随主图 Stretch。</p><p>CutScene 默认布局的图片结构以 <code>v2.1.1+af0a4be</code> 旧 XAML 为准：<code>SurTeamLogo</code> / <code>HunTeamLogo</code> 保持 direct <code>Image</code>；<code>Map</code>、<code>SurPick0</code> 到 <code>SurPick3</code>、<code>HunPick</code> 使用 <code>BorderedImage</code> 复现旧 <code>Border -&gt; Image</code>。其中 SurPick0-3 角色大图显式设置 <code>ImageWidth=556.5</code>、<code>ImageHeight=null</code>，由内层 <code>Image</code> 的 <code>UniformToFill</code>、水平居中、顶部对齐和外层 <code>ClipToBounds</code> 共同决定裁剪；HunPick 仍不设置内层图片宽高。</p><h2 id="designer-v3-资源-uri" tabindex="-1"><a class="header-anchor" href="#designer-v3-资源-uri"><span>Designer v3 资源 URI</span></a></h2>',14)),d("p",null,[e[1]||(e[1]=o("Designer v3 layout 和 ",-1)),e[2]||(e[2]=d("code",null,".bpui v3",-1)),e[3]||(e[3]=o(" 包标准允许以下资源 URI 形式，完整包规格见 ",-1)),c(a,{to:"/dev/frontend/bpui-package-v3.html"},{default:i(()=>[...e[0]||(e[0]=[o("bpui-package-v3.md",-1)])]),_:1}),e[4]||(e[4]=o("。",-1))]),e[11]||(e[11]=s(`<table><thead><tr><th>形式</th><th>含义</th></tr></thead><tbody><tr><td><code>Resources/foo.png</code></td><td>内置前台文件资源，解析到运行目录 <code>Resources/bpui/foo.png</code>。</td></tr><tr><td><code>pack://application:,,,/Assets/Fonts/#Noto Sans</code></td><td>WPF app pack resource，主要用于内置字体或 app-bundled asset。</td></tr><tr><td><code>bpui://local/resources/images/foo.png</code></td><td>编辑器本地资源命名空间，用于用户选择本地图片后的持久副本。</td></tr><tr><td><code>bpui://{PackageId}/resources/images/foo.png</code></td><td>已安装布局包资源，按包目录隔离。</td></tr><tr><td><code>bpui://{PackageId}/resources/fonts/font.ttf#FontFamilyName</code></td><td>包内字体 URI，<code>#</code> 后为字体族名。</td></tr></tbody></table><p>绝对路径只应作为编辑时临时输入。的 Canvas Properties GUI 在用户选择本地背景图片后，会复制文件到本地资源目录，并在 layout JSON 中写入 <code>bpui://local/...</code>。的 <code>.bpui v3</code> 导出会把引用到的 <code>bpui://local/...</code>、其他已安装包资源和绝对路径资源复制进导出包，并重写为 <code>bpui://{PackageId}/...</code>；缺失的绝对路径资源会让导出失败并显示错误。<code>Resources/...</code> 和 <code>pack://application:,,,/...</code> 属于应用内置资源，导出时保持原样，不复制进包内。</p><p>Designer v3 中通过 Resource Browser 选择 Canvas 背景、<code>ScoreGlobalWindow/BaseCanvas</code> 的 BO3 背景、以及 <code>ImagePath</code> / <code>BorderImagePath</code> / <code>LockImageSource</code> 等静态资源字段时会立即应用到当前内存布局、记录 undo、标记 dirty 并刷新预览；手动输入文本仍需要 Enter 或 Apply。选择本地文件或 Resource Browser 返回绝对文件路径时，会先复制到本地资源目录并写入 <code>bpui://local/...</code>，不会把绝对路径保存到 layout。立即复制产生的新文件会记录为当前编辑会话的 pending resource：保存时保留仍被当前或其他已保存布局引用的文件并清理未引用文件；放弃修改、切换布局选择“不保存”或关闭窗口选择“不保存”时，会尽力删除本会话新建且未被任何已保存布局引用的文件。undo/redo 不会立即删除 pending resource，以便 redo 可以恢复引用。</p><p>起，<code>FrontedResourceResolver</code> 支持 <code>bpui://local/resources/images/foo.png</code> 和 <code>bpui://{PackageId}/resources/images/foo.png</code>，并拒绝不安全 <code>PackageId</code>、绝对路径和路径穿越。缺失文件按 unresolved 处理，不抛出异常。</p><p>起，图片进入本地资源、包导入、包导出或 resolver 解码前都会走安全校验。支持扩展名为 png、jpg、jpeg、bmp、gif、webp、ico、tif、tiff。Canvas 背景图最大 2.5 MiB、长边 4096、像素 4096×4096；控件 UI 图片最大 1 MiB、长边 2048、像素 2048×2048。超限或无法安全解码的图片默认会被拒绝：本地资源不会复制，<code>BackgroundImage</code> / 编辑缓冲不会更新，resolver 运行时返回 <code>null</code> 并记录 warning，预览和前台不会因为坏图崩溃。Resource Browser 缩略图也使用安全解码，超限图片不做完整加载。通过队伍 Logo、成员照片或 Designer v3 的本地选图、资源浏览器、路径输入并应用超限图片时，会额外提供“压缩并应用”选项；用户确认后仅在临时副本中使用 SkiaSharp 等比缩放、重编码，原图片保持不变，压缩成功后才写入本地资源或应用到队伍信息。</p><p><code>ImagePath</code>、<code>PickingBorderImagePath</code>、<code>BanLockImagePath</code>、<code>BorderImagePath</code>、<code>LockImageSource</code> 等控件级图片字段以及队伍 LOGO 按普通 UI 图片限制校验；<code>BackgroundImage</code> 按背景图限制校验。Designer 的 Resource Browser 选择普通图片控件静态图时写入 <code>ImagePath</code>，不会写入 <code>BindingPath</code>。</p><p>推荐本地资源目录：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>%APPDATA%/neo-bpsys-wpf/FrontedLayoutPackages/local/resources/images/</span></span>
<span class="line"><span>%APPDATA%/neo-bpsys-wpf/FrontedLayoutPackages/local/resources/fonts/</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>已安装包资源必须按包隔离，不能合并进共享全局目录：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>%APPDATA%/neo-bpsys-wpf/FrontedLayoutPackages/{PackageId}/resources/</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>删除普通布局包时，应删除整个 <code>%APPDATA%/neo-bpsys-wpf/FrontedLayoutPackages/{PackageId}/</code> 目录，从而删除该包资源。不要只根据 manifest 逐个删除资源文件。<code>builtin</code> 是内置布局/资源的虚拟包 ID，<code>local</code> 是编辑器本地资源命名空间，二者都不能通过普通包删除流程删除。</p><p>的导入校验会拒绝跨包资源引用和 <code>bpui://local/...</code> 引用。包 <code>package-a</code> 中的布局可以引用 <code>bpui://package-a/...</code>、<code>Resources/...</code>、<code>pack://application:,,,/...</code>；不应引用 <code>bpui://package-b/...</code>。导出前存在的 <code>bpui://local/...</code> 必须重写为导出包自己的 <code>PackageId</code>。导入安装时资源保持在 <code>%APPDATA%/neo-bpsys-wpf/FrontedLayoutPackages/{PackageId}/resources/</code>，不会合并到共享目录。</p><p>的 legacy <code>.bpui</code> 转换会把旧 <code>CustomUi/</code> 中的图片复制到转换后包的 <code>resources/images/</code>，并在 manifest 的 <code>Content.Resources</code> 中记录 <code>Kind = Image</code> 和 <code>Sha256</code>。如果旧 <code>Config.json</code> 的明确前台图片字段指向这些文件，转换后的布局会改写为 <code>bpui://{PackageId}/resources/images/...</code>；缺失或无法安全映射的旧资源只产生 warning，不写入全局 <code>CustomUi</code>。明确映射为背景图或控件 UI 图的 PNG/JPEG 如果超过对应图片安全阈值，会在转换暂存区使用 SkiaSharp 等比缩放、重编码，压缩完成后再计算资源文件名和 SHA256，并通过转换警告提示压缩前后体积；源 <code>.bpui</code> 不会被修改。普通 manifest-based v3 包导入默认仍只做校验：当任一图片因体积或像素尺寸超限而导入失败时，界面会汇总所有超限图片并提供“压缩后导入”选项；只有用户确认后，才在临时导入副本中以 SkiaSharp 压缩全部超限图片、更新 manifest 中受影响资源的 SHA256 并安装，源 <code>.bpui</code> 不会被修改。</p><h2 id="assets-与字体" tabindex="-1"><a class="header-anchor" href="#assets-与字体"><span>Assets 与字体</span></a></h2><p>字体位于：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>neo-bpsys-wpf/Assets/Fonts</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>设置默认字体使用 pack URI，例如：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>pack://application:,,,/Assets/Fonts/#Noto Sans</span></span>
<span class="line"><span>pack://application:,,,/Assets/Fonts/#华康POP1体W5</span></span>
<span class="line"><span>pack://application:,,,/Assets/Fonts/#汉仪第五人格体简</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>legacy <code>LegacyTextSettings.FontFamily</code> 会根据 <code>FontFamilySite</code> 创建 <code>FontFamily</code>，仅供旧 Config.json 迁移和旧工具控件兼容使用。新增字体时要同时确认：</p><ol><li><code>.ttf</code> 被加入 csproj 的 <code>Resource Include</code>。</li><li>pack URI 路径正确。</li><li><code>#</code> 后的字体族名称和字体文件内部名称一致。</li><li>设置页 <code>_systemFonts</code> 如需固定展示该字体，也要加入对应 <code>FontFamily</code>。</li></ol><p>Designer v3 的字体属性下拉会把当前活动布局包 <code>resources/fonts/</code> 中的字体列在最上方，并用蓝色 BPUI 标记区分。用户从字体属性导入 <code>.ttf</code>、<code>.otf</code> 或 <code>.ttc</code> 时，字体会复制到当前可写布局包，布局字段保存为 <code>bpui://{PackageId}/resources/fonts/...#FontFamilyName</code>。这些字体不会进入系统字体或全局资源库，切换布局包后需要在新包重新导入；导出 <code>.bpui</code> 时会把被布局引用的包内字体一起打包。字体属性旁的“管理本包字体”入口只列出当前活动布局包的 <code>resources/fonts/</code>，未被当前包布局引用的字体文件可以删除；仍被引用的字体会禁用删除，需要先把对应布局属性改成其他字体。</p><h2 id="本地化资源" tabindex="-1"><a class="header-anchor" href="#本地化资源"><span>本地化资源</span></a></h2>`,22)),d("p",null,[e[6]||(e[6]=o("本地化资源已从单一 ",-1)),e[7]||(e[7]=d("code",null,"Locales/Lang*.resx",-1)),e[8]||(e[8]=o(" 拆分为按功能域拥有的资源族。完整规则、字典选择、XAML/C# 引用模式、模块归属、文化与回退、审计方法等见 ",-1)),c(a,{to:"/dev/resources/localization.html"},{default:i(()=>[...e[5]||(e[5]=[o("localization.md",-1)])]),_:1}),e[9]||(e[9]=o("。",-1))]),e[12]||(e[12]=s(`<p>主程序本地化文件（每个族均包含 neutral + <code>.en-us</code> + <code>.ja-jp</code>）：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Locales/Common.resx        Locales/Shell.resx       Locales/Team.resx</span></span>
<span class="line"><span>Locales/Game.resx          Locales/Bp.resx          Locales/Score.resx</span></span>
<span class="line"><span>Locales/FrontManage.resx   Locales/Designer.resx    Locales/AnimationEditor.resx</span></span>
<span class="line"><span>Locales/Settings.resx      Locales/PluginMarket.resx</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>模块自有本地化文件：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>neo-bpsys-wpf.ProductTour/Locales/Tour.resx (+ .en-us, .ja-jp)</span></span>
<span class="line"><span>neo-bpsys-wpf.SmartBp.Module/Locales/SmartBp.resx (+ .en-us, .ja-jp)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>所有 resx 作为 <code>EmbeddedResource</code> 嵌入各自程序集，不再使用 <code>PublicResXFileCodeGenerator</code> 或 <code>Lang.Designer.cs</code>。迁移前的三种文化快照与 key map 由 <code>neo-bpsys-wpf.Tests/TestData/I18nMigration/</code> 追踪，测试不得依赖被忽略的 <code>artifacts/</code> 目录。</p><p>XAML 常见写法（需指定 <code>DefaultAssembly</code> 和 <code>DefaultDictionary</code>）：</p><div class="language-xaml line-numbers-mode" data-highlighter="shiki" data-ext="xaml" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-xaml"><span class="line"><span>lex:ResxLocalizationProvider.DefaultAssembly=&quot;neo-bpsys-wpf&quot;</span></span>
<span class="line"><span>lex:ResxLocalizationProvider.DefaultDictionary=&quot;neo_bpsys_wpf.Locales.Shell&quot;</span></span>
<span class="line"><span>Text=&quot;{lex:Loc SomeKey}&quot;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>后台代码常见写法（需指定字典常量）：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">I18nHelper</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">GetLocalizedString</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">AppI18nDictionaries</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">Shell</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;SomeKey&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>前台布局控件等无法预先确定归属字典的场景使用全量查找：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">I18nHelper</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">GetLocalizedStringFromAnyHostDictionary</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;SomeKey&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>I18nHelper</code> 找不到 key 时返回原始 key，便于界面降级显示和定位缺失翻译。新增用户可见文本时至少添加对应功能族 neutral resx，并尽量补齐 <code>.en-us</code>、<code>.ja-jp</code>，避免用户看到裸 key。禁止重新引入单一 <code>Lang.resx</code>。</p><p>SmartBP OCR 不维护模块内角色别名表；OCR 只解析区域和槽位，角色名匹配统一交给 <code>ICharacterSelectionService</code> / <code>CharacterSelectionService</code>，并且匹配时必须限定在传入阵营内，不得跨阵营查询。Tesseract traineddata 属于托管模型资产，固定下载到 SmartBP 模块目录的 <code>OCRModels/Tesseract/tessdata/</code>。RapidOCR profile 由 <code>Resources/SmartBp/RapidOcrModelManifest.json</code> 声明，安装到 <code>OCRModels/RapidOCR/Models/{profileId}/</code>。中、日、英模型及字典的完整 ModelScope 地址摘自 RapidOCR 官方 <code>python/rapidocr/default_models.yaml</code>；模型 SHA-256 使用官方值，字典则固定校验官方文件内容，不得在代码中拼接地址。模块内下载经 <code>SmartBpParallelDownload</code> 适配到宿主 <code>IFileDownloadService</code>，暂停或取消后保留旁路分片供下次续传。AppData 只保存 SmartBP 配置，不保存托管模型文件。</p><p>RapidOCR manifest 的 <code>version</code> 和每个资产的下载契约共同生成安装指纹；安装目录内的 <code>.smartbp-install.json</code> 用于判断当前模型是否落后于随模块发布的 manifest。更新官方模型条目时必须同步提升版本或更新资产契约，并更新对应测试。</p><p>Designer v3 的显示层本地化统一使用 <code>Designer.*</code> key 前缀，归属 <code>Locales.Designer</code> 字典。代码侧通过 <code>IFrontedDesignerLocalizationService</code> 访问，WPF 宿主实现再委托 <code>I18nHelper.GetLocalizedString(AppI18nDictionaries.Designer, key)</code>；Core 中的默认实现只返回原始值，避免 Core 反向引用 WPF 项目。常用命名包括 <code>Designer.Property.*</code>、<code>Designer.PropertyGroup.*</code>、<code>Designer.ControlType.*</code>、<code>Designer.Option.{Property}.{Value}</code>、<code>Designer.Window.*</code>、<code>Designer.Canvas.*</code>、<code>Designer.Binding.*</code> 和 <code>Designer.BindingType.*</code>。</p><p>后，常用命名还包括 <code>Designer.Value.*</code>、<code>Designer.Editor.*</code> 和 <code>Designer.Validation.*</code>，用于只读布尔值、Binding Browser / Resource Browser 和属性校验提示。</p><p>这些 key 只影响编辑器 UI 显示，不改变布局文件。<code>.bpui</code> / v3 JSON 中的 schema 字段名、控件 <code>Name</code>、<code>ControlType</code>、<code>BindingPath</code>、资源 URI 和 <code>FontFamily</code> 仍写入原始契约值；例如中文界面 ComboBox 显示“居中”，保存仍是 <code>&quot;HorizontalAlignment&quot;: &quot;Center&quot;</code>。Binding Browser 可以显示本地化节点名，但界面必须保留原始路径，选择结果也必须写回原始 <code>BindingPath</code>。Resource Browser 可以显示本地化来源和类型，但选中区域必须保留原始资源 URI 或文件路径。</p><p><code>GameProgressText</code> 使用集中 helper 和资源 key 生成 <code>FREE GAME</code>、<code>GAME {n} FIRST HALF</code>、<code>GAME {n} OVERTIME SECOND HALF</code> 等文本，避免 BO3/BO5 进度文案散落在窗口 XAML 或 JSON 中。默认是单行文本（<code>DisplayMode = Inline</code>）；正式预设包括单行、双行、横排局数、横排半场、竖排、竖排双行、竖排局数、竖排半场。<code>DisplayLanguage = FollowApp</code> 时按 <code>WPFLocalizeExtension</code> 的当前应用语言生成文本，中文应用语言下应显示中文局数和半场文案。<code>MapNameText</code> 默认把 <code>CurrentGame.PickedMap</code> 枚举名作为本地化 key 查询地图名（地图 key 归属 <code>Locales.Game</code>），也可以通过 <code>BindingPath</code> 指向其他地图字段，例如当前对局的 picked / banned map 数据；新增地图时要同步补齐地图资源 key。<code>LocalizedText</code> 用 <code>LocalizationKey</code> 经 <code>I18nHelper.GetLocalizedStringFromAnyHostDictionary</code> 查询普通 resx 文案，适合 GameData 表头等静态标签；如果 key 缺失会显示 <code>FallbackText</code> 或 key 本身。普通 <code>Text.Text</code> 仍是原样静态文本，不会自动本地化。</p><h2 id="添加新素材" tabindex="-1"><a class="header-anchor" href="#添加新素材"><span>添加新素材</span></a></h2><p>添加或替换前台素材时：</p><ol><li>确认素材属于嵌入 <code>Assets</code> 还是输出 <code>Resources</code>。</li><li>如果代码用 <code>ImageHelper.GetUiImageSource(&quot;bp&quot;)</code>，文件应在 <code>Resources/bpui/bp.png</code>。</li><li>如果代码用 <code>ImageSourceKey.surHalf</code>，文件应在 <code>Resources/surHalf/{name}.png</code>。</li><li>旧 XAML-first 默认位置文件命名必须匹配 <code>{WindowTypeName}Config-{CanvasName}.default.json</code>（<code>CanvasName</code> 是旧多 Canvas 概念）；v3 默认布局使用 <code>Resources/FrontedLayouts/{WindowTypeName}.json</code>。</li><li>v3 JSON 中 <code>Resources/xxx.png</code> 会解析到运行目录 <code>Resources/bpui/xxx.png</code>，新增默认背景时要确认对应文件存在于 <code>Resources/bpui</code> 并会复制到输出目录。</li><li>SmartBP 新增 OCR 模型或测试资源时，确认其位于模块 <code>Resources</code> 下并会复制到模块输出目录；赛后数据 OCR 不再依赖独立的区域配置文件。</li></ol><h2 id="常见坑" tabindex="-1"><a class="header-anchor" href="#常见坑"><span>常见坑</span></a></h2><ol><li>放进 <code>Assets</code> 的文件不会自动成为 <code>ResourcesPath</code> 下的运行时文件。</li><li>放进 <code>Resources</code> 的文件不会自动有 pack URI。</li><li>字体文件名和字体族名称不一定一致，pack URI 的 <code>#</code> 后面要用字体族名称。</li><li>缺少 <code>CopyToOutputDirectory</code> 会导致本地调试可见、发布后缺文件。</li><li>某语言 resx 缺 key 时界面可能显示 key 本身。</li><li>自定义 UI 图片路径保存在设置中，重置窗口配置可能删除对应自定义图片文件。</li></ol>`,23))])}const k=t(p,[["render",u]]),b=JSON.parse('{"path":"/dev/resources/resources-localization-and-assets.html","title":"资源、本地化与素材","lang":"zh-CN","frontmatter":{"description":"资源、本地化与素材 资源分类 项目中有两类容易混淆的资源： GameRule.json 单独设置为 CopyToOutputDirectory=Always，输出到应用基目录，供 GameGuidanceService 读取。 Resources 目录 常见子目录： ImageHelper 使用 AppConstants.ResourcesPath 拼...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"资源、本地化与素材\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/resources/resources-localization-and-assets.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"资源、本地化与素材"}],["meta",{"property":"og:description","content":"资源、本地化与素材 资源分类 项目中有两类容易混淆的资源： GameRule.json 单独设置为 CopyToOutputDirectory=Always，输出到应用基目录，供 GameGuidanceService 读取。 Resources 目录 常见子目录： ImageHelper 使用 AppConstants.ResourcesPath 拼..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":13.44,"words":4033},"filePathRelative":"dev/resources/resources-localization-and-assets.md","autoDesc":true}');export{k as comp,b as data};
