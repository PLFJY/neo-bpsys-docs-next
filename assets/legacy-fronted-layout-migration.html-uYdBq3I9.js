import{_ as o}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as d,b as c,o as t}from"./app-CGOhbY2V.js";const a={};function n(i,e){return t(),d("div",null,[...e[0]||(e[0]=[c(`<h1 id="旧前台布局迁移" tabindex="-1"><a class="header-anchor" href="#旧前台布局迁移"><span>旧前台布局迁移</span></a></h1><p>Designer v3 layout package 是当前前台窗口自定义的唯一运行时来源。旧 <code>Config.json</code> 中的前台窗口字段已经从 active <code>Settings.cs</code> 移除，只保留为 legacy DTO，用于启动迁移和旧 <code>.bpui</code> 转换。</p><h2 id="迁移入口" tabindex="-1"><a class="header-anchor" href="#迁移入口"><span>迁移入口</span></a></h2><p>启动加载 <code>%APPDATA%\\neo-bpsys-wpf\\Config.json</code> 时，如果 <code>Version</code> 缺失或为 <code>null</code>，<code>ILegacyV2StartupMigrationService</code> 会先备份原文件，再把旧前台字段转换成普通 v3 package：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>FrontedLayoutPackages/</span></span>
<span class="line"><span>  converted-v2-{config-sha256-prefix}/</span></span>
<span class="line"><span>    manifest.json</span></span>
<span class="line"><span>    migration-state.json</span></span>
<span class="line"><span>    FrontedLayouts/{Window}.json</span></span>
<span class="line"><span>    FrontedBehaviors/{Window}.behaviors.json</span></span>
<span class="line"><span>    Resources/...</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>包 id 使用原始 legacy <code>Config.json</code> 的 SHA-256 前缀，迁移状态记录 schema version、源 hash、备份路径、包 id 和迁移时间。同一份 legacy 配置重复启动时会复用已有 converted package，不会重复创建。只有 staging package 写入和校验成功后才会激活包并保存干净 Settings v3；失败时保留原始 <code>Config.json</code>，恢复原活动包，必要时回到 <code>builtin</code>。</p><p>旧字段映射到 converted package 内的 <code>FrontedLayouts</code>：</p><table><thead><tr><th>旧字段</th><th>v3 目标</th></tr></thead><tbody><tr><td><code>BpWindowSettings.BgImageUri</code></td><td><code>FrontedLayouts/BpWindow.json</code> 的 <code>CanvasSettings.BackgroundImage</code></td></tr><tr><td><code>CutSceneWindowSettings.BgUri</code></td><td><code>FrontedLayouts/CutSceneWindow.json</code> 的 <code>CanvasSettings.BackgroundImage</code></td></tr><tr><td><code>ScoreWindowSettings.SurScoreBgImageUri</code></td><td><code>FrontedLayouts/ScoreSurWindow.json</code> 的 <code>CanvasSettings.BackgroundImage</code></td></tr><tr><td><code>ScoreWindowSettings.HunScoreBgImageUri</code></td><td><code>FrontedLayouts/ScoreHunWindow.json</code> 的 <code>CanvasSettings.BackgroundImage</code></td></tr><tr><td><code>ScoreWindowSettings.GlobalScoreBgImageUri</code></td><td><code>FrontedLayouts/ScoreGlobalWindow.json</code> 的 <code>CanvasSettings.BackgroundImage</code></td></tr><tr><td><code>GameDataWindowSettings.BgImageUri</code></td><td><code>FrontedLayouts/GameDataWindow.json</code> 的 <code>CanvasSettings.BackgroundImage</code></td></tr><tr><td><code>WidgetsWindowSettings.MapBpBgUri</code></td><td>MapV1 已删除，跳过并记录 Info</td></tr><tr><td><code>WidgetsWindowSettings.BpOverviewBgUri</code></td><td><code>FrontedLayouts/BpOverviewWindow.json</code> 的 <code>CanvasSettings.BackgroundImage</code></td></tr><tr><td><code>WidgetsWindowSettings.MapBpV2BgUri</code></td><td><code>FrontedLayouts/MapV2Window.json</code> 的 <code>CanvasSettings.BackgroundImage</code></td></tr><tr><td><code>AllowsWindowTransparency</code> / <code>AllowsScoreGlobalWindowTransparency</code></td><td><code>FrontedWindowConfig.WindowSettings.AllowsTransparency</code></td></tr></tbody></table><p>无法明确迁移的旧行为字段会写入日志 warning，不会静默伪造 v3 行为。迁移后的 active <code>Config.json</code> 只保留应用级设置，不再写出旧前台窗口字段。</p><h2 id="显式窗口映射" tabindex="-1"><a class="header-anchor" href="#显式窗口映射"><span>显式窗口映射</span></a></h2><table><thead><tr><th>legacy 来源</th><th>v3 目标</th></tr></thead><tbody><tr><td><code>BpWindow/BaseCanvas</code></td><td><code>BpWindow/BaseCanvas</code></td></tr><tr><td><code>CutSceneWindow/BaseCanvas</code></td><td><code>CutSceneWindow/BaseCanvas</code></td></tr><tr><td><code>GameDataWindow/BaseCanvas</code></td><td><code>GameDataWindow/BaseCanvas</code></td></tr><tr><td><code>ScoreGlobalWindow/BaseCanvas</code></td><td><code>ScoreGlobalWindow/BaseCanvas</code></td></tr><tr><td><code>ScoreHunWindow/BaseCanvas</code></td><td><code>ScoreHunWindow/BaseCanvas</code></td></tr><tr><td><code>ScoreSurWindow/BaseCanvas</code></td><td><code>ScoreSurWindow/BaseCanvas</code></td></tr><tr><td><code>WidgetsWindow/BpOverViewCanvas</code></td><td><code>BpOverviewWindow/BaseCanvas</code></td></tr><tr><td><code>WidgetsWindow/MapV2Canvas</code></td><td><code>MapV2Window/BaseCanvas</code></td></tr><tr><td><code>WidgetsWindow/MapBpCanvas</code></td><td>MapV1 已删除，跳过并记录兼容说明</td></tr></tbody></table><p><code>BpOverviewWindow</code> 固定使用窗口和画布尺寸 <code>1132x182</code>；<code>MapV2Window</code> 固定使用 <code>1440x160</code>。不要用旧 <code>WidgetsWindowSettings.WindowSize = 1440x716</code> 初始化这两个拆分窗口。</p><p>启动迁移不能把当前内置 v3 layout JSON 当作 v2 默认值来源。迁移来源只允许是旧设置默认、旧 XAML 样式默认、显式 <code>LegacyControlBlueprint</code>、legacy config 值和需要复制/改写的 package resources。旧 <code>TextSettings.IsActive</code> 是 CommunityToolkit <code>ObservableRecipient</code> 序列化泄漏，迁移文本样式时必须忽略它。</p><h2 id="兼容边界" tabindex="-1"><a class="header-anchor" href="#兼容边界"><span>兼容边界</span></a></h2><p>legacy 兼容只允许存在于启动迁移服务和显式 legacy <code>.bpui</code> 转换器。正常 v3 runtime 禁止读取：</p><ul><li><code>%APPDATA%\\neo-bpsys-wpf\\FrontedLayouts</code> loose 用户布局；</li><li>legacy canvas layout path；</li><li><code>Resources/FrontedLayouts</code> 作为非 <code>builtin</code> 包 fallback；</li><li>插件默认布局 fallback；</li><li><code>FrontedWindowConfig</code> 上的 canvas-centric public helper。</li></ul><h2 id="旧-bpui" tabindex="-1"><a class="header-anchor" href="#旧-bpui"><span>旧 <code>.bpui</code></span></a></h2><p>旧 <code>.bpui</code> 不带 v3 <code>manifest.json</code>，通常包含 <code>Config.json</code>、<code>CustomUi/</code> 或 <code>FrontElementsConfig/</code>。导入时应走 legacy 转换器：转换器从当前内置 v3 layout 起步，复制安全资源，应用可映射的背景图、旧 <code>TextSettings</code> 文本样式和旧 <code>ElementInfo</code> 几何，再生成干净的 v3 <code>.bpui</code> 包。</p><p>旧几何迁移按“精确控件名、窗口限定别名、聚合控件、已知旧 overlay 消费、规范化匹配、技术候选诊断”顺序处理。<code>ScoreGlobalWindow/BaseCanvas</code> 已显式兼容旧全局比分命名：</p><table><thead><tr><th>旧控件名</th><th>v3 控件名</th></tr></thead><tbody><tr><td><code>MainTeamName</code></td><td><code>HomeTeamName</code></td></tr><tr><td><code>MainScoreTotal</code></td><td><code>HomeScoreTotal</code></td></tr><tr><td><code>AwayTeamName</code></td><td><code>AwayTeamName</code></td></tr><tr><td><code>AwayScoreTotal</code></td><td><code>AwayScoreTotal</code></td></tr></tbody></table><p><code>ScoreGlobalWindow</code> 下还保留一个限定规则：旧名以 <code>Main</code> 开头时可映射为 v3 的 <code>Home</code> 前缀。旧版 <code>HomeTeamGame*FirstHalf</code> / <code>HomeTeamGame*SecondHalf</code>、<code>AwayTeamGame*FirstHalf</code> / <code>AwayTeamGame*SecondHalf</code> 以及 <code>Game*Overtime*Half</code> 不再逐个迁移为顶层控件，而是聚合到 <code>HomeGlobalScoreRow</code> / <code>AwayGlobalScoreRow</code> 的 <code>Cells</code> 子格中，并把旧绝对坐标换算为相对父行的 <code>X/Y/Width/Height</code>。另有更早期包使用 <code>MainTeamGame*</code> / <code>AwayGame*</code>，并以 <code>Extra</code> 代替 <code>Overtime</code>；这些显式名称同样聚合到对应比分行。间距不规则、overtime 单元迁入子格等细节只记录为内部诊断，不再为每个旧半场格子报 unmatched。</p><p><code>BpWindow/BaseCanvas</code> 与 <code>GameDataWindow/BaseCanvas</code> 中，早期 <code>MinorPointsSur</code> / <code>MinorPointsHun</code> 分别作为 <code>GameScoresSur</code> / <code>GameScoresHun</code> 的名称别名处理。旧 <code>CutSceneWindow/BaseCanvas</code> 的 <code>MapMask</code> 是黑色矩形遮罩；转换器将其映射为同名 v3 <code>Rectangle</code>，保留旧包中的几何并使用旧窗口的黑色默认填充。</p><p>旧 <code>WidgetsWindow/BpOverViewCanvas</code> 会转换为 <code>BpOverviewWindow</code>，旧 <code>HunBanCurrentLock*</code> / <code>SurBanCurrentLock*</code> 锁定遮罩几何会合并到对应 v3 <code>HunBanCurrent*</code> / <code>SurBanCurrent*</code>。如果目标本体几何也存在，以目标本体为准；如果只有锁遮罩几何，则用它作为 fallback。旧 Config 中可解析到 <code>CustomUi/</code> 的 Ban 锁图和 BpWindow picking border 图片/颜色会写入 v3 控件配置。</p><p>转换结果中的 <code>Infos</code> 用于记录成功复制资源、正常聚合等信息，<code>Diagnostics</code> 用于记录技术细节和近似处理；<code>Warnings</code>、<code>UnsupportedProperties</code> 和 <code>MissingResources</code> 才表示需要用户留意的问题。UI 不应把纯 <code>Infos</code> / <code>Diagnostics</code> 当作警告弹出，用户弹窗也不应展示 <code>Closest candidates</code> 等原始技术诊断。</p><p>旧 <code>Config.json</code> 明确引用的背景图和控件 UI 图在转换时按各自安全阈值检查。超过阈值的 PNG/JPEG 使用 SkiaSharp 在暂存副本中等比缩放并按原格式重编码，随后重算资源 SHA256；转换结果增加 <code>LegacyConvert.ImageCompressed</code> warning，用户可看到文件名及压缩前后体积。此规则只属于显式 legacy migration，不修改源归档，也不扩展为普通 v3 包的读取期重写。</p><p>旧 <code>TextSettings</code> 迁移由 Core 中的共享样式迁移器处理，本地 <code>Config.json</code> 启动迁移和旧 <code>.bpui</code> 包转换使用同一套映射规则。转换器会把旧字体写法如 <code>./#汉仪第五人格体简</code>、<code>./#华康POP1体W5</code> 以及内置 <code>Noto Sans</code> 归一化为 <code>pack://application:,,,/Assets/Fonts/#...</code>，普通系统字体名保留；单个样式字段格式无效时仅跳过该字段并记录本地化 warning，其余可解析字段继续迁移。样式应用记录只进入 <code>Diagnostics</code>，不作为用户警告弹窗。</p><p>旧 <code>WidgetsWindow/MapV2Canvas</code> 会转换为 <code>MapV2Window</code>。<code>MapV2Display</code> 已提供地图名、队名、阵营文字三组字体/颜色/字号配置，以及 <code>PickingBorderImagePath</code>、<code>PickingBorderFillColor</code>。普通 <code>Image</code> 和 <code>BorderedImage</code> 也使用相同的 <code>PickingBorderFillColor</code> 表达 opacity-mask 的填充色。旧 <code>MapBpV2_*</code> 文本样式和 picking border 图片/颜色可以写入这些 v3 字段，不再需要提示“MapV2Display 无 v3 image/color config”。CutScene 的非对称队名文字区域使用通用 <code>Text.ContentMargin*</code> 保存内部内容偏移；缺失这些字段的既有 v3 包保持原有视觉。</p><p>manifest-based v3 <code>.bpui</code> 仍禁止包含 <code>Config.json</code>、<code>CustomUi/</code> 或 <code>FrontElementsConfig/</code>。</p>`,28)])])}const p=o(a,[["render",n]]),l=JSON.parse('{"path":"/dev/frontend/legacy-fronted-layout-migration.html","title":"旧前台布局迁移","lang":"zh-CN","frontmatter":{"description":"旧前台布局迁移 Designer v3 layout package 是当前前台窗口自定义的唯一运行时来源。旧 Config.json 中的前台窗口字段已经从 active Settings.cs 移除，只保留为 legacy DTO，用于启动迁移和旧 .bpui 转换。 迁移入口 启动加载 %APPDATA%\\\\neo-bpsys-wpf\\\\Config...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"旧前台布局迁移\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/frontend/legacy-fronted-layout-migration.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"旧前台布局迁移"}],["meta",{"property":"og:description","content":"旧前台布局迁移 Designer v3 layout package 是当前前台窗口自定义的唯一运行时来源。旧 Config.json 中的前台窗口字段已经从 active Settings.cs 移除，只保留为 legacy DTO，用于启动迁移和旧 .bpui 转换。 迁移入口 启动加载 %APPDATA%\\\\neo-bpsys-wpf\\\\Config..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":5.03,"words":1509},"filePathRelative":"dev/frontend/legacy-fronted-layout-migration.md","autoDesc":true}');export{p as comp,l as data};
