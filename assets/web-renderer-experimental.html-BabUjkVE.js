import{_ as o}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as s,b as a,o as d}from"./app-CGOhbY2V.js";const n={};function i(r,e){return d(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="实验性-web-renderer" tabindex="-1"><a class="header-anchor" href="#实验性-web-renderer"><span>实验性 Web Renderer</span></a></h1><p>Web Renderer 是独立内置插件 <code>top.plfjy.bpsys.WebRenderer</code>。它会把当前活动 <code>.bpui v3</code> 包的 Window-centric 布局发送给 Kestrel sidecar，并在浏览器中还原固定设计画布。它不替代现有 WPF 前台窗口；WPF 与网页会并行运行，比赛状态仍只由 WPF 的 <code>ISharedDataService</code> 权威维护。</p><p>插件本体不引用 <a href="http://ASP.NET" target="_blank" rel="noopener noreferrer">ASP.NET</a> Core；它会先检测 x64 的 <code>Microsoft.AspNetCore.App 10.*</code>，然后启动同目录 <code>Host</code> 下 framework-dependent 的 <code>net10.0</code> sidecar。缺少 runtime、进程启动错误、端口占用或 IPC 断开只会记录日志和显示非模态提示，不会中止主 WPF 应用。请安装 <a href="https://dotnet.microsoft.com/en-us/download/dotnet/10.0" target="_blank" rel="noopener noreferrer">ASP.NET Core Runtime 10 (x64)</a> 后点击&quot;重新检测&quot;。</p><p>当 runtime 缺失时，管理页顶部会显示引导区域，支持一键下载并安装官方 <a href="http://ASP.NET" target="_blank" rel="noopener noreferrer">ASP.NET</a> Core Runtime 10 x64 installer：</p><ul><li><strong>版本发现</strong>：<code>WebRendererRuntimeReleaseFeed</code> 在线查询 Microsoft 官方 release metadata（<code>https://builds.dotnet.microsoft.com/dotnet/release-metadata/10.0/releases.json</code>），解析最新 10.0.x 版本号、win-x64 installer 直链与 SHA-512。网络失败时回退到插件内置常量 <code>WebRendererRuntimeReleaseFeed.KnownFallbackVersion</code>（当前 <code>10.0.10</code>）与稳定 CDN URL pattern <code>https://builds.dotnet.microsoft.com/dotnet/aspnetcore/Runtime/{version}/aspnetcore-runtime-{version}-win-x64.exe</code>。发布新版本后应同步更新该常量。</li><li><strong>下载</strong>：<code>WebRendererRuntimeSetupService</code> 通过宿主 <code>IFileDownloadService</code> 下载 installer 到 <code>%TEMP%\\neo-bpsys-wpf_WebRenderer\\</code>，支持暂停、继续和取消后续传，并实时回传进度。</li><li><strong>校验</strong>：若 release metadata 提供 SHA-512，下载后用 <code>SHA512.HashDataAsync</code> 校验；hash 缺失则跳过校验并记录 warning。</li><li><strong>安装</strong>：以 <code>/quiet /norestart</code> 参数、<code>Verb=runas</code>（UAC 提升）唤起 installer 静默安装；用户拒绝 UAC 或 installer 退出码非 0 时回退为&quot;打开官方下载页&quot;手动安装引导。</li><li><strong>重启</strong>：安装完成并重新检测通过后，设置 <code>IGlobalRestartService.IsRestartRequired = true</code>，应用标题栏出现&quot;需要重启&quot;按钮，用户点击后 <code>App.Current.Restart()</code> 重启主程序；重启后 sidecar 重新检测 runtime 并正常启动。安装完成与重启之间不自动关闭应用，避免丢失用户未保存的后台状态。</li></ul><p>安装流程状态机：<code>Idle</code> → <code>FetchingRelease</code> → <code>Downloading</code>（可进入 <code>Paused</code> 并恢复）→ <code>Verifying</code> → <code>Installing</code> → <code>AwaitingRestart</code>（或 <code>Failed</code>）。任何阶段的错误都会回写到管理页引导区域，不会中止主 WPF 应用。</p><p>默认地址为 <code>http://127.0.0.1:19527</code>，可使用：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>--web-host &lt;IPv4&gt;</span></span>
<span class="line"><span>--web-port &lt;1-65535&gt;</span></span>
<span class="line"><span>--web-no-start</span></span>
<span class="line"><span>--web-transition-exit-timeout-ms &lt;1-30000&gt;</span></span>
<span class="line"><span>--web-transition-enter-timeout-ms &lt;1-30000&gt;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>默认监听 localhost。实验性 LAN 模式可以使用 <code>--web-host 0.0.0.0</code>，此模式没有访问认证，同一网络中的设备可读取页面与实时展示数据；请只在受信任网络使用，并通过系统防火墙限制端口。sidecar 提供 <code>/</code>、<code>/health</code>、<code>/render/{encodedFullWindowType}</code>、<code>/api/windows</code>、<code>/api/bootstrap/{encodedFullWindowType}</code>、<code>/bpui-assets/{resourceToken}</code>、<code>/remote-assets/{token}</code> 和 <code>/ws</code>。<code>/assets/*.js</code> 与 <code>/assets/*.css</code> 仅用于 Vite 生成的 Web 客户端静态文件；<code>.bpui</code> 图片、字体等授权资源仅使用 <code>/bpui-assets/{resourceToken}</code>，在线图片仅使用 <code>/remote-assets/{token}</code>，这些 URL 前缀互不复用。</p><p>资源 URL 是每次 bootstrap 创建的随机 token；浏览器不会获得物理路径。插件只授权当前活动包、<code>local</code>、内置 <code>Resources/...</code> 和已知应用字体，拒绝绝对路径、跨包引用及编码路径穿越。切换包或 Designer 保存布局时会重新发送 bootstrap，页面通过 WebSocket 刷新。未知内置控件与没有 Web adapter 的 <code>plugin:*</code> 控件会显示诊断占位；纯 Binding 文本显示绑定路径占位。</p><p>每次 Web client 构建都会写入 client build id（提交标识与构建时间）。该值同时出现在最终 <code>index.html</code> 的 meta 标签、浏览器启动日志和 <code>/health</code> 的 <code>clientBuildId</code>。sidecar 启动前会验证最终 <code>Host/wwwroot/index.html</code>、build id 以及它引用的每一个本地 script/link 文件；任何缺失或越界引用都会让 sidecar 以明确错误退出，而不会在运行期间持续请求不存在的 hash 文件。</p><p><code>/</code>、<code>/render/*</code> 和 <code>/index.html</code> 始终返回 <code>Cache-Control: no-store</code>，确保入口页不会缓存旧 bundle 引用。带内容 hash 的 <code>/assets/*.js</code> 与 <code>/assets/*.css</code> 使用 immutable 缓存策略。</p><p>IPC 使用 version 8，bootstrap schema 使用 version 1，runtime value schema 使用 version 2，并采用显式会话状态：<code>Stopped</code>、<code>StartingProcess</code>、<code>WaitingForPipe</code>、<code>PipeConnected</code>、<code>BuildingBootstrap</code>、<code>WaitingForBootstrapAck</code>、<code>Ready</code>、<code>Stopping</code>、<code>Faulted</code>。主程序是状态权威；sidecar 必须先发送 <code>sidecar.ready</code>，主程序才发送 <code>host.hello</code> 和真实活动包的 <code>bootstrap.replace</code>。sidecar 会原子校验协议版本、schema、generation、窗口结构与本地资源表，成功后返回 <code>bootstrap.applied</code>；只有该确认到达，管理页、<code>/health</code> 与 <code>/api/windows</code> 才会显示 <code>Ready</code> 和已发布窗口。连接断开时 sidecar 保持 HTTP 进程并以封顶退避重连，重连后的第一组消息始终是完整 bootstrap、localization 和 runtime snapshot。</p><p>Web Renderer 的本地化唯一由主程序 <code>WebRendererLocalizationBridge</code> 负责。主程序使用显式 <code>CultureInfo</code> 调用 <code>I18nHelper</code>、<code>MapNameDisplayHelper</code>、<code>GameProgressDisplayHelper</code> 和窗口显示名 Helper，生成 <code>WebLocalizationSnapshot</code>；快照只包含稳定控件 ID 与最终显示文本，不包含 resx、字典名或浏览器查找规则。<code>LocalizedText</code>、<code>MapNameText</code>、<code>MapV2Display</code> 和 <code>GameProgressText</code> 的 runtime 投影分别提供 <code>DisplayText</code>、地图/阵营最终文本或 <code>FullText</code>/<code>GameText</code>/<code>HalfText</code>。React 只负责排版，未收到匹配 <code>Revision</code> 的 localization 和 runtime 前保持业务文本为空。</p><p>语言变化来自 <code>ISettingsHostService.LanguageSettingChanged</code>：后台构建新快照，按 latest-wins 丢弃过期 revision，通过 <code>localization.replace</code> 主动推送；sidecar 保存并广播完整快照，随后主程序发布带 <code>LocalizationRevision</code> 的 runtime snapshot。语言切换不重启 sidecar、刷新浏览器或重新下载图片。日志链为 <code>language changed</code> → <code>localization snapshot built</code> → <code>localization.replace sent</code> → <code>sidecar applied revision</code> → <code>browser applied revision</code>。缺失资源仅由主程序记录一次 <code>LocalizationMissing:{dictionary}:{key}:{culture}</code>，FallbackText 只在真实资源缺失时使用。</p><p><code>/api/windows</code> 和 <code>/api/bootstrap/{encodedFullWindowType}</code> 在 IPC 未连接时返回 <code>503 IpcUnavailable</code>，bootstrap 尚未确认时返回 <code>503 BootstrapPending</code>，构建或校验失败时返回结构化 <code>503</code> 错误。<code>/render/{...}</code> 在等待状态显示“正在等待主程序布局数据”；仅 Ready 后才区分 <code>UnknownWindow</code> 与 <code>LayoutUnavailable</code>。管理页仅把 sidecar 已确认的窗口作为可用 Web Runtime，不会用 registry 候选窗口伪装布局已发布。</p><p>Image 与 BorderedImage 按 WPF 的语义树分离外层布局、内容 viewport、Lock/PickingBorder overlay 和行为生成部件。动态图片值明确区分 <code>resolved</code>、<code>pending</code>、<code>null</code>、<code>failed</code>；runtime asset 同时携带自然 DIP、像素尺寸与 DPI。浏览器只在新图片 decode 成功后原子切换，pending/failed 保留同 generation 的上一稳定图片，业务 null 则清空。</p><h2 id="在线队伍图片" tabindex="-1"><a class="header-anchor" href="#在线队伍图片"><span>在线队伍图片</span></a></h2><p>队伍 JSON 中的 HTTP/HTTPS Logo 与选手定妆照保留为 URI。WPF 继续通过 <code>BitmapImage.UriSource</code> 加载；主程序只发布规范化 URI 的不可变描述符，不下载图片、不读取图片字节，也不在 UI 线程等待网络。真实链路为：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Team/Member JSON ImageUri</span></span>
<span class="line"><span>→ BitmapImage.UriSource</span></span>
<span class="line"><span>→ CurrentGame.SurTeam.Logo / CurrentGame.HunTeam.Logo</span></span>
<span class="line"><span>  或 CurrentGame.SurPlayerList[0..3].PictureShown / CurrentGame.HunPlayer.PictureShown</span></span>
<span class="line"><span>→ BindingPathObserver</span></span>
<span class="line"><span>→ WebRuntimeValueFactory</span></span>
<span class="line"><span>→ WebRuntimeAssetRegistry</span></span>
<span class="line"><span>→ remoteAsset.fetch IPC</span></span>
<span class="line"><span>→ sidecar RemoteAssetFetcher/cache</span></span>
<span class="line"><span>→ remoteAsset.resolved/failed IPC</span></span>
<span class="line"><span>→ runtime snapshot/bindingPatch IPC</span></span>
<span class="line"><span>→ RuntimeStore</span></span>
<span class="line"><span>→ ImageRenderer</span></span>
<span class="line"><span>→ DynamicImage</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>没有选择角色时，<code>PictureShown</code> 使用 <code>Member.Image</code>；选择角色后使用 <code>Character.HalfImage</code>；清除角色后重新使用当前 Member 的在线定妆照。两个队伍 Logo、四名求生者与一名监管者都走同一套绑定和资源链路，不含控件特判。</p><p>sidecar 独立异步下载在线图片，并只允许 HTTP/HTTPS 的 PNG、JPEG、WebP 和 GIF。下载使用操作系统默认代理和路由设置，因此浏览器、WPF 与 Clash TUN/Fake-IP 模式共用同一网络路径；sidecar 不强制直连，也不要求额外的受信任主机配置。每次下载最多跟随 5 次重定向，连接超时 5 秒、总请求超时 20 秒、单项上限 10 MiB；相同资源的并发请求合并。成功结果进入 64 MiB 内存 LRU 与 512 MiB、最长 7 天的磁盘缓存，完整写入后才通过 <code>/remote-assets/{token}</code> 提供。失败不会持久缓存，绑定仍存在时会退避重试；URL 或 generation 改变时旧任务结果会被丢弃。</p><p>每次请求和重定向都会重新执行基本地址校验：拒绝 userinfo、非 HTTP/HTTPS、localhost、环回、链路本地、组播、未指定和 RFC1918 私有地址。Clash TUN 常用的 <code>198.18.0.0/15</code> 仅作为 Fake-IP 路由地址，允许交由操作系统代理/路由处理，不将其当作用户可访问的局域网图片服务器。日志只记录稳定诊断码、generation 和截短 token，不记录源 URL、查询参数或完整缓存路径。</p><h2 id="字体分类" tabindex="-1"><a class="header-anchor" href="#字体分类"><span>字体分类</span></a></h2><p>普通字体名称（例如 Arial、Segoe UI、Microsoft YaHei、Times New Roman、sans-serif、serif、monospace）直接交给浏览器解析，不进入 bootstrap resources、不生成 <code>@font-face</code>、不发起字体资源请求，也不会映射为 Noto Sans。只有 <code>pack://...#Family</code>、<code>bpui://...#Family</code> 和明确指向 <code>.ttf</code>、<code>.otf</code>、<code>.woff</code>、<code>.woff2</code> 的 <code>Resources/...</code> 引用才作为嵌入或包字体处理。</p><p>Web 文本将 WPF FontWeight 名称集中转换为 CSS 数值：Thin 100；ExtraLight/UltraLight 200；Light 300；Normal/Regular 400；Medium 500；DemiBold/SemiBold 600；Bold 700；ExtraBold/UltraBold 800；Black/Heavy 900；ExtraBlack 950。未知或空值不直接写入 CSS，由浏览器继承。</p><p><code>.bpui v3</code> 的 Rectangle、Border、Image AnimationParts 会进入所属控件的局部 Above/Below overlay。Web 动画使用带单位的长度值，百分比 ClipInset 直接生成 <code>clip-path: inset(...)</code>，Transform 分量由同一元素状态合成。Transition 的 <code>transition.committed</code> 携带 required generation/sequence；浏览器应用到该 runtime sequence 后才启动 EnterGraph，超时、断线或 generation 变化时 fail-open，WPF 始终是唯一业务 commit 所有者。</p><p>这些能力仍属于实验性 Web Renderer。BackgroundTintRectangle、BackgroundTintPolygon、MapV2Display、GameProgressText 以及 Image/BorderedImage 的 PickingBorder 已由 Web Runtime 实现；图片编码、runtime patch、IPC 和 Web 动画保持异步，WPF Renderer 的既有语义不变。</p><p>BackgroundTint 使用独立 Web Worker 和 OffscreenCanvas 对当前 Canvas 背景执行与 WPF 同类的像素染色，主线程不进行逐像素处理。MapV2Display 使用持久化的 <code>InternalParts</code>、RuntimeAsset 图片和 <code>part:{BehaviorGuid}:PickingBorder</code> 行为目标。GameProgressText 使用现有枚举的模式、语言、数字和竖排规则；尚未实现的控件仍会在管理页和浏览器诊断中明确标记为 unsupported。</p><h2 id="管理与排查" tabindex="-1"><a class="header-anchor" href="#管理与排查"><span>管理与排查</span></a></h2><p>插件启用并重启应用后会在后台导航中出现 <strong>Web Renderer</strong> 实验页面。页面可以启动、停止和重启 sidecar，显示与 <code>/health</code> 一致的生命周期状态、本机/局域网 URL、连接客户端、已确认活动包、已确认窗口和最近错误，也可以复制或在默认浏览器打开本机 URL。Host、Port、随应用启动、Transition fail-open timeout 与协议摘要日志保存在插件自己的 <code>PluginConfigs/top.plfjy.bpsys.WebRenderer/Settings.json</code>，不会修改主程序 <code>Config.json</code>。保存配置会重启 sidecar。</p><p>命令行参数优先于插件设置：<code>--web-host</code>（或 <code>--host</code>）、<code>--web-port</code>（或 <code>--port</code>）、<code>--web-no-start</code> 与 <code>--web-log-protocol</code>。协议日志只记录连接和消息摘要，不记录动画逐帧数据。</p><p>OBS 可添加 Browser Source 并填入本机 URL 或指定窗口的 <code>/render/{encodedFullWindowType}</code> URL；浏览器字体栅格化与 WPF 可能略有不同。<code>plugin:*</code> 控件只有提供受控 Web adapter 才能渲染，否则显示诊断占位。</p><h2 id="部署验收" tabindex="-1"><a class="header-anchor" href="#部署验收"><span>部署验收</span></a></h2><p>从仓库根目录运行以下命令可验证从干净构建到最终主程序插件目录的完整静态产物链路：</p><div class="language-powershell line-numbers-mode" data-highlighter="shiki" data-ext="powershell" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-powershell"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">\\tools\\Test-WebRendererDeployment.ps1</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> -</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">Configuration Release</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>脚本会在 <code>build/web-renderer-deployment-validation/app/Plugins/top.plfjy.bpsys.WebRenderer/Host/wwwroot/index.html</code> 检查最终入口页与所有引用资源，启动该目录中的 sidecar 并验证 HTTP 响应、缓存头和 build id。随后它会临时修改并自动还原 Web 样式源文件，第二次构建后确认新的 build id、hash 文件替换和旧文件清理。</p><p>常见问题：端口被占用时更换端口后保存重启；缺少 <a href="http://ASP.NET" target="_blank" rel="noopener noreferrer">ASP.NET</a> Core Runtime 10 (x64) 时按后台提示安装；Web 页面无法更新时确认活动包已保存并刷新页面。禁用或移除该插件后，主程序与 WPF 前台继续按原有方式运行，不依赖 sidecar 或 Node.js。</p><p>构建期同样不依赖 Node.js：WebRenderer 前端由 Host sidecar 的 MSBuild target 调用 <code>pnpm</code> 构建，需要 pnpm + Node.js。若构建机未安装 pnpm/Node.js 或前端构建失败，MSBuild 不会中断主程序构建，而是发出警告并跳过该插件的打包（不复制到 <code>Plugins\\</code>），主程序及其它内置插件照常构建。这与运行期「主程序不依赖 sidecar 或 Node.js」保持一致。</p><p>停止、重启和保存后重启是互斥操作。停止会先请求 sidecar 关闭、关闭 IPC 会话并等待进程退出；超过关闭时限才终止当前会话所启动的 sidecar 进程树。sidecar 会优先加入 Windows Job Object；parent PID 及其启动时间监视是 Job 不可用时的兜底。管理页在操作期间禁用相关命令，并在成功、失败、取消或超时后恢复可操作状态。</p><p>真实 IPC 与布局验收使用：</p><div class="language-powershell line-numbers-mode" data-highlighter="shiki" data-ext="powershell" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-powershell"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">\\tools\\Test-WebRendererIpc.ps1</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> -</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">Configuration Debug</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>该脚本发布并启动真实主程序，要求 sidecar 到达 <code>Ready</code>，验证 <code>BpWindow</code> 的真实 bootstrap、AnimationParts/overlay DOM 身份与 headless Edge 截图，并输出 health、windows、bootstrap 摘要、浏览器控制台和 DOM 证据。</p>`,43)])])}const l=o(n,[["render",i]]),p=JSON.parse('{"path":"/dev/plugin/web-renderer-experimental.html","title":"实验性 Web Renderer","lang":"zh-CN","frontmatter":{"description":"实验性 Web Renderer Web Renderer 是独立内置插件 top.plfjy.bpsys.WebRenderer。它会把当前活动 .bpui v3 包的 Window-centric 布局发送给 Kestrel sidecar，并在浏览器中还原固定设计画布。它不替代现有 WPF 前台窗口；WPF 与网页会并行运行，比赛状态仍只由 WP...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"实验性 Web Renderer\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/plugin/web-renderer-experimental.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"实验性 Web Renderer"}],["meta",{"property":"og:description","content":"实验性 Web Renderer Web Renderer 是独立内置插件 top.plfjy.bpsys.WebRenderer。它会把当前活动 .bpui v3 包的 Window-centric 布局发送给 Kestrel sidecar，并在浏览器中还原固定设计画布。它不替代现有 WPF 前台窗口；WPF 与网页会并行运行，比赛状态仍只由 WP..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":10.74,"words":3221},"filePathRelative":"dev/plugin/web-renderer-experimental.md","autoDesc":true}');export{l as comp,p as data};
