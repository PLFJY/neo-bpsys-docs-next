import{_ as c}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as r,b as d,d as o,e as t,a as n,w as i,r as s,o as l}from"./app-CGOhbY2V.js";const p={};function m(h,e){const a=s("RouteLink");return l(),r("div",null,[e[5]||(e[5]=d('<h1 id="已知边界与路线图提醒" tabindex="-1"><a class="header-anchor" href="#已知边界与路线图提醒"><span>已知边界与路线图提醒</span></a></h1><p>本文面向维护者和 AI agent，用于避免把当前边界误判为已完成能力。</p><h2 id="smartbp" tabindex="-1"><a class="header-anchor" href="#smartbp"><span>SmartBP</span></a></h2><table><thead><tr><th>能力</th><th>状态</th></tr></thead><tbody><tr><td>赛后数据 OCR 自动回填</td><td>成熟且可用；整表 OCR 后按文本边界框重建行列</td></tr><tr><td>PaddleOCR BP 状态识别</td><td>默认 OCR Provider；读取文字与边界框，本地解析阶段、禁用、选择与玩家 ID</td></tr><tr><td>PaddleOCR NVIDIA CUDA 加速</td><td>可选；拥有受支持的 NVIDIA GPU 时，用户可在 SmartBP 设置卡片下载匹配的 CUDA runtime 组件并启用 GPU 推理；CPU/GPU native runtime 物理隔离，故障自动回退 CPU，不预装进主安装包</td></tr><tr><td>Tesseract BP 状态识别</td><td>可选 OCR Provider；可在 SmartBP 页面勾选下载 <code>chi_sim</code>/<code>eng</code>/<code>jpn</code> 到 SmartBP 模块目录，不会自动回退到 Paddle</td></tr><tr><td>本地视觉模型 + llama.cpp BP 状态识别</td><td>已从 SmartBP 模块移除；BP 状态识别仅支持 OCR Provider</td></tr><tr><td>GameGuidance 自动追赶</td><td>可选；统一 Reconciliation 按槽位和前置业务状态逐步调用 <code>NextStepAsync()</code>；引导落后时会从同局短时原始帧补充遗漏角色，不允许跳过未完成步骤；原地等待优先于回退</td></tr><tr><td>识别结果自动应用</td><td>可选；仅对宿主 <code>Pending</code> 槽位通过 <code>ICharacterSelectionService</code> 应用高置信度角色或目标之前的明确空操作；自动选角播放正常动画</td></tr><tr><td>手动强制同步</td><td>当前帧固定全图解析四类 Ban/Pick 区域，不受当前 phase/Guidance Action 过滤；角色以 <code>playAnimation=false</code> 直接写入主程序，随后按宿主槽位直接定位 Guidance；不作为自动模式运行</td></tr><tr><td>自动 BP 画面切换</td><td>TODO</td></tr></tbody></table><p>自动循环通过细分场景门禁限制写入：角色 BP 中应用当前角色操作；求生者/监管者天赋调整阶段只允许用画面中仍可见的对应阵营 Ban/Pick 字段完成最终角色补写并同步引导；区域选择、等待开始、加载和对局内会丢弃延迟结果并暂停。区域选择和 MapBP 均不在此识别范围内。</p><p>SmartBP 自动循环以区域门控 OCR 为入口，不使用手工任务选择作为正常入口。每个活动 BP tick 在识别当前 phase 后读取四类角色槽位，避免上一帧 phase 预先删掉本帧追赶所需的较早字段。OCR 默认使用 Paddle，也可由用户显式切换为 Tesseract 或 RapidOCR；一次只运行所选 Provider，不提供自动 fallback 或 ensemble。Provider 只读取文字、置信度和边界框；阶段、禁用、选择、玩家 ID 均由本地规则、区域 ID、固定槽位几何和角色候选字典解释。未识别槽位为 <code>Unknown</code>，只有明确空槽位证据才是 <code>Empty</code>，缺失的前位或中间位不会令后续 Ban 候选左移。</p><p>识别层不跨帧维护第二份 Ban/Pick 业务状态。轻量捕获按独立于 OCR 的较高频率写入绑定 GameGuid/GameProgress 的有界滚动原始帧缓冲；容量下限考虑 OCR 周期和已观测处理耗时，旧局帧不会作为新局证据。自动链路先比较 <code>Action + 规范化 Indexes</code>，并检查目标之前的宿主 Pending 槽位洞；对齐且没有新槽位证据时不会执行 Reconciliation 或历史 OCR。宿主槽位已提交不能单独证明同 Action 下一组已开始；只有下一组固定槽位被明确识别，或两组之间的其他 Action 已完成，才提高目标。中间 Ban 未完成时保留在前一 Pick 组；下一 Pick 槽出现后才允许据此确认并逐步跨过明确空 Ban。真正落后且 Guidance 未超过目标时，默认只回看目标之前两个工作流步骤，每个步骤最多选择一张代表帧，并只识别 Phase 和该 Action 对应区域；Phase/Action 不对齐的帧整帧拒绝。历史结果只为宿主 <code>Pending</code> 或 <code>CommittedEmpty</code> 且当前帧未明确选择的槽位补充高置信角色，不补 Empty、不覆盖当前角色或宿主 <code>CommittedCharacter</code>，也不将后续角色前移。安全角色可把同槽 <code>CommittedEmpty</code> 升级为 <code>CommittedCharacter</code>，该补充播放正常动画但不移动 Guidance。原地等待优先于回退；只有 Guidance 至少领先目标两个工作流步骤、当前 Action 已不同且目标槽位有安全 <code>Selected</code> 强证据时，才逐步回退到最早未满足前置步骤并重新逐步前进。补充结果只存在于本次对账，随后仍由 Reconciliation 按工作流顺序应用；若仍缺少某一步所需的明确证据，自动追赶会停在该最早未完成步骤。<code>DistributeChara</code> 之后允许把安全且唯一的缺失角色补进第一个宿主空位，再依据玩家 ID 做必要交换；没有实际补位或交换操作时不会重复触发。步骤变化继续复用 GameGuidance 原有导航、计时器、高亮与事件逻辑。手动强制同步不回看历史帧。地图 BP、天赋角色操作和自动切屏仍未实现。</p><p>BP 识别路径读取 SmartBP 模块资源中的 <code>BpRecognitionLayoutProfile.json</code> 粗裁剪配置。用户可通过通用 <code>RegionEditorWindow</code> 在当前捕获帧或内置测试图上可视化调整全部区域，结果保存为 AppData 下的 SmartBP recognition profile 覆盖；运行时与编辑器读取同一份配置。自动循环每 tick 先 OCR <code>TopCenterStatus</code>，用归一化、编辑距离和关键词覆盖评分识别阵营选择、双方天赋调整及即将进入区域选择；未知结果只跳过本 tick，弱 transition 需连续两次确认，强 transition 会立即阻止新内容识别并进入排空停止。<code>TopLeftStatus</code> 保留为稍晚出现的区域选择/等待开始硬确认，两种区域不会合并使用。post-BP latch 设置后，后续阶段波动不会恢复内容 OCR、字段合并或候选操作生成。这套配置独立于 PaddleOCR 赛后数据 OCR 细区域。Tesseract traineddata、Paddle/RapidOCR 模型、内置测试帧、OCR 别名和 SmartBP 默认区域配置均随 SmartBP 模块 <code>Resources</code> 或模块目录管理，不放在主程序 <code>Resources</code>。AppData 中只保存纯配置（如 <code>SmartBp/RecognitionSettings.json</code>、<code>SmartBp/BpRecognitionLayoutProfile.json</code>）。默认仍为识别预览；只有用户显式启用自动应用后，才会通过角色选择服务应用已解析候选。不识别 MapBP。</p><h2 id="插件系统" tabindex="-1"><a class="header-anchor" href="#插件系统"><span>插件系统</span></a></h2><p>插件是全信任模型，不是沙箱。当前风险控制是插件市场审核、微步云恶意文件扫描、人工审查和较小的插件生态。</p><p>插件加载发生在启动期间、Host build 前。安装或更新插件后需要重启，不能假设支持热加载或热更新。</p><p><code>Assembly.LoadFrom</code> 加载入口程序集，依赖解析主要依赖插件目录、宿主已有程序集和 .NET 默认上下文。插件打包时不要漏掉自身直接依赖，也不要把宿主已有依赖重复打包成冲突版本。</p><h2 id="版本概念" tabindex="-1"><a class="header-anchor" href="#版本概念"><span>版本概念</span></a></h2><p>插件 API 版本和 PluginSdk 源码引用版本是故意分离的：</p><table><thead><tr><th>概念</th><th>用途</th></tr></thead><tbody><tr><td>插件 API 版本</td><td><code>manifest.yml</code> 的 <code>apiVersion</code>，宿主加载兼容性</td></tr><tr><td>PluginSdk 源码引用版本</td><td>插件项目编译和打包用的 SDK 源码提交</td></tr></tbody></table><p>v3 起不再发布 PluginSdk NuGet 包；插件项目应 clone 本仓库并通过 <code>ProjectReference</code> 包含 <code>neo-bpsys-wpf.PluginSdk</code>。不要把 <code>apiVersion</code> 和 SDK 源码提交不同步当作错误。</p><h2 id="designer-v3-当前状态" tabindex="-1"><a class="header-anchor" href="#designer-v3-当前状态"><span>Designer v3 当前状态</span></a></h2><p>Fronted Designer v3 已完成基础设施环节，所有内置前台窗口均已接入 v3 renderer：</p><table><thead><tr><th>窗口</th><th>v3 layout host</th><th>状态</th></tr></thead><tbody><tr><td><code>ScoreSurWindow</code> / <code>ScoreHunWindow</code></td><td><code>FrontedWindowBase -&gt; ViewBox -&gt; BaseCanvas</code></td><td>v3 renderer，绑定 <code>MatchScore</code></td></tr><tr><td><code>ScoreGlobalWindow</code></td><td><code>FrontedWindowBase -&gt; ViewBox -&gt; BaseCanvas</code></td><td>v3 renderer + <code>GlobalScoreRow</code>，BO3/BO5 canvas states</td></tr><tr><td><code>CutSceneWindow</code></td><td><code>FrontedWindowBase -&gt; ViewBox -&gt; BaseCanvas</code></td><td>v3 renderer，业务控件封装</td></tr><tr><td><code>GameDataWindow</code></td><td><code>FrontedWindowBase -&gt; ViewBox -&gt; BaseCanvas</code></td><td>v3 renderer，表头 <code>LocalizedText</code></td></tr><tr><td><code>BpOverviewWindow</code></td><td><code>FrontedWindowBase -&gt; ViewBox -&gt; BaseCanvas</code></td><td>v3 renderer，原 Widgets overview 独立窗口</td></tr><tr><td><code>MapV2Window</code></td><td><code>FrontedWindowBase -&gt; ViewBox -&gt; BaseCanvas</code></td><td>v3 renderer，保留 MapV2</td></tr><tr><td><code>BpWindow</code></td><td><code>FrontedWindowBase -&gt; ViewBox -&gt; BaseCanvas</code></td><td>v3 renderer + 内置 Transition / Loop 行为</td></tr></tbody></table><p>Designer v3 独立编辑器（<code>FrontedDesignerWindow</code>）已实现并作为设计编辑器唯一入口。旧版真实窗口设计器模式、SettingPage 旧前台自定义入口和旧位置保存/恢复 API 已移除。</p><h3 id="已知边界" tabindex="-1"><a class="header-anchor" href="#已知边界"><span>已知边界</span></a></h3><ol><li>v3 layout window 的窗口 Width/Height 已由 <code>WindowSettings.WindowWidth</code> / <code>WindowHeight</code> 独立保存。<code>CanvasSettings.CanvasWidth</code> / <code>CanvasHeight</code> 只表示内部设计画布尺寸；legacy canvas-centric 转换缺少窗口尺寸时才用 Canvas 尺寸初始化窗口尺寸。传统固定 XAML window 仍由原窗口逻辑管理尺寸。</li><li>SettingPage 旧 <code>.bpui</code> import/export UI 入口已删除。旧 <code>.bpui</code> 现在通过 <code>FrontManagePage</code> 的 Layout Packages 管理，会触发 v3 转换，不再覆盖全局 Config.json。旧 Config 字段已移入 legacy DTO / 转换器 / 迁移代码，不再作为 active <code>Settings.cs</code> 运行时属性。</li><li>Resource Browser 控件级浏览不复制/导入外部图片。</li><li>运行时关键控件名称只读且不能删除。被其他控件引用的普通控件在 reference-aware rename/delete 实现前阻止改名和删除。</li><li><code>CurrentBanDisplay</code>、<code>BanSlotDisplay</code> 和 <code>PickingBorderOverlay</code> 已移除，不再作为兼容控件读取。新布局推荐使用 <code>Image</code> / <code>BorderedImage</code> 的 <code>Lockable</code> 与 <code>PickingBorderAvailable</code> overlay，它们的内部覆盖层不作为普通可选/可编辑/可添加控件。</li><li><code>.bpui v3</code> 导出、导入/安装、激活复制和删除已实现；导出固定为全部前台布局。</li><li>v3 导出器不会写入全局 <code>Config.json</code>、<code>CustomUi/</code> 或 <code>FrontElementsConfig/</code>。</li><li>编辑器手写输入按上限截断，外部导入超限 JSON/manifest/layout/package 会拒绝。</li><li>图片按用途限制大小和像素，resolver 对坏图安全返回 <code>null</code>。</li><li>Canvas 控件数 160 warning、256 hard limit。</li><li>编辑器支持内部 <code>Ctrl+C</code> / <code>Ctrl+V</code> 控件复制粘贴且不抢文本输入控件的普通复制粘贴。</li><li>插件 <code>ControlType</code> 标准为 <code>plugin:&lt;PackageId&gt;/&lt;ControlTypeName&gt;</code>，Canvas 可声明并由编辑器/导出器同步 <code>RequiredPlugins</code>，<code>.bpui</code> manifest 汇总 <code>PluginDependencies</code>。</li><li>缺失插件窗口布局和控件配置会保留，Designer 显示 MissingPlugin 占位符，直播前台 runtime 跳过缺失插件控件并记录 warning。安装/更新后仍需重启，不把新插件当作当前进程已加载继续导入。</li><li><code>.bpui</code> 不得包含插件 DLL、安装包或脚本；导入器会拒绝这类条目。</li></ol><h2 id="文档边界" tabindex="-1"><a class="header-anchor" href="#文档边界"><span>文档边界</span></a></h2><p>公开 VuePress 文档面向用户，可能落后于 UI 或内部实现。仓库内 <code>/docs</code> 面向维护者和 AI agent，应跟随代码架构变化更新。</p><h2 id="代码中观察到的边界" tabindex="-1"><a class="header-anchor" href="#代码中观察到的边界"><span>代码中观察到的边界</span></a></h2><ol><li>SmartBP 已覆盖固定 Ban 槽位、Unknown/Empty、Observation TTL、跨局隔离、缓冲容量和 Guidance 补偿等自动回归；真实赛事画面、不同缩放与 OCR Provider 组合仍需持续扩充样本。</li><li><code>App.xaml.cs</code> 更新检查条件写作 <code>#if !DEBUG &amp;&amp; !Preview</code>，而项目配置定义 <code>PREVIEW</code>。这是代码观察到的命名 caveat；本文档不声称其运行时效果已经通过编译验证，本任务也不修改代码。</li><li><code>GameRule.json</code> 是项目内规则配置，不是外部权威赛事规则源。</li><li>前台默认布局依赖文件命名约定，插件窗口默认布局缺失时恢复默认会失败。</li></ol><h2 id="score-system-v2" tabindex="-1"><a class="header-anchor" href="#score-system-v2"><span>Score System v2</span></a></h2>',27)),o("p",null,[e[1]||(e[1]=t("比分系统已迁移到现有 ",-1)),e[2]||(e[2]=o("code",null,"Core.Models.Game",-1)),e[3]||(e[3]=t(" 持有权威状态，详见 ",-1)),n(a,{to:"/dev/business/score-system-v2.html"},{default:i(()=>[...e[0]||(e[0]=[t("score-system-v2.md",-1)])]),_:1}),e[4]||(e[4]=t("。当前代码仍存在这些边界：",-1))]),e[6]||(e[6]=d("<table><thead><tr><th>边界</th><th>说明</th></tr></thead><tbody><tr><td><code>Team.Score</code> 语义混杂</td><td>运行时不再从 <code>MatchScoreState</code> 同步它；仅保留旧 JSON/旧 DTO 反序列化兼容，不得重新作为权威状态。</td></tr><tr><td><code>ScoreGlobalWindow</code> BO3/BO5 状态</td><td>v3 已使用通用 Canvas BO states，不依赖旧 <code>MajorGameGap</code> / <code>HalfGameGap</code>。</td></tr><tr><td><code>GameProgress.Free</code> 未定义比分语义</td><td>Score System v2 暂把它记录为设计缺口。</td></tr><tr><td><code>Game3Overtime*</code> 与 <code>Game4*</code> enum 数值重叠</td><td><code>MatchScoreService</code> 结合 BO3/BO5 状态解析；缺少上下文的保守按 BO5 第四局解析。</td></tr><tr><td>旧记录 <code>Team.Score</code> 无法还原完整历史</td><td>旧 JSON 没有 <code>MatchScore</code> 时会创建默认 <code>MatchScoreState</code>，不会从 <code>Team.Score</code> 反推出 per-Game/per-Half 结果。</td></tr></tbody></table>",1))])}const B=c(p,[["render",m]]),v=JSON.parse('{"path":"/dev/known-limitations-and-roadmap.html","title":"已知边界与路线图提醒","lang":"zh-CN","frontmatter":{"description":"已知边界与路线图提醒 本文面向维护者和 AI agent，用于避免把当前边界误判为已完成能力。 SmartBP 自动循环通过细分场景门禁限制写入：角色 BP 中应用当前角色操作；求生者/监管者天赋调整阶段只允许用画面中仍可见的对应阵营 Ban/Pick 字段完成最终角色补写并同步引导；区域选择、等待开始、加载和对局内会丢弃延迟结果并暂停。区域选择和 M...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"已知边界与路线图提醒\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/known-limitations-and-roadmap.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"已知边界与路线图提醒"}],["meta",{"property":"og:description","content":"已知边界与路线图提醒 本文面向维护者和 AI agent，用于避免把当前边界误判为已完成能力。 SmartBP 自动循环通过细分场景门禁限制写入：角色 BP 中应用当前角色操作；求生者/监管者天赋调整阶段只允许用画面中仍可见的对应阵营 Ban/Pick 字段完成最终角色补写并同步引导；区域选择、等待开始、加载和对局内会丢弃延迟结果并暂停。区域选择和 M..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":9.99,"words":2996},"filePathRelative":"dev/known-limitations-and-roadmap.md","autoDesc":true}');export{B as comp,v as data};
