import{_ as n}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as t,d as i,e as o,a,w as s,b as c,r,o as l}from"./app-CGOhbY2V.js";const p={};function h(k,e){const d=r("RouteLink");return l(),t("div",null,[e[8]||(e[8]=i("h1",{id:"wpf-ui-坑点记录",tabindex:"-1"},[i("a",{class:"header-anchor",href:"#wpf-ui-坑点记录"},[i("span",null,"WPF-UI 坑点记录")])],-1)),i("p",null,[e[2]||(e[2]=o("线程和异步相关规则另见 ",-1)),a(d,{to:"/dev/architecture/threading-dispatcher-and-async.html"},{default:s(()=>[...e[0]||(e[0]=[o("threading-dispatcher-and-async.md",-1)])]),_:1}),e[3]||(e[3]=o("。资源、字体和本地化规则另见 ",-1)),a(d,{to:"/dev/resources/resources-localization-and-assets.html"},{default:s(()=>[...e[1]||(e[1]=[o("resources-localization-and-assets.md",-1)])]),_:1}),e[4]||(e[4]=o("。",-1))]),e[9]||(e[9]=c(`<h2 id="导航和-di" tabindex="-1"><a class="header-anchor" href="#导航和-di"><span>导航和 DI</span></a></h2><p>后台页面、内置前台窗口、ViewModel 和多数服务都通过 DI 注册。不要绕开：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">services</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">AddBackendPage</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&lt;</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">MyPage</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">MyViewModel</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&gt;();</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">services</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">AddFrontedWindow</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&lt;</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">MyWindow</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">MyWindowViewModel</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&gt;();</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>手动 <code>new Page()</code> 或 <code>new Window()</code> 会丢失 DataContext、服务注入、注册表信息和 WPF-UI page provider 集成。插件前台窗口应通过 <code>services.AddFrontedWindow&lt;TWindow,TViewModel&gt;()</code>（XAML 窗口，配合 <code>[FrontedWindowInfo(&quot;GUID&quot;, &quot;DisplayName&quot;, IsBuiltIn = false)]</code>）或 <code>services.AddFrontedV3LayoutWindow(&quot;WindowId&quot;, isBuiltIn: false)</code>（v3 布局窗口，PackageId 由宿主自动注入）注册；窗口元数据由 <code>FrontedWindowRegistration</code>（<code>FrontedXamlWindowRegistration</code> / <code>FrontedV3LayoutWindowRegistration</code>，<code>FrontedWindowRegistrationKind</code> 为 <code>Xaml</code> / <code>V3Layout</code>）承载，统一进入 <code>IFrontedWindowRegistry</code>。</p><h2 id="page-宿主限制" tabindex="-1"><a class="header-anchor" href="#page-宿主限制"><span>Page 宿主限制</span></a></h2><p>WPF 的 <code>Page</code> 只能由 <code>Window</code>、<code>Frame</code> 或导航宿主承载，不能直接放进 <code>ContentControl</code>、<code>Border</code>、<code>Grid</code>、<code>TabItem</code>、插件内容 host 或模块内容 host。否则运行时会抛出 <code>InvalidOperationException: Page can have only Window or Frame as parent.</code></p><p>需要嵌入到页面内部、插件 host、模块 host、overlay 或 <code>ContentControl</code> 的内容必须实现为 <code>UserControl</code> 或普通 <code>Control</code>。只有真正参与 WPF-UI 导航或 Frame 导航的后台页面才使用 <code>Page</code>，并通过 <code>AddBackendPage&lt;TView,TViewModel&gt;()</code> 注册。</p><h2 id="生命周期" tabindex="-1"><a class="header-anchor" href="#生命周期"><span>生命周期</span></a></h2><p>当前注册模式中：</p><ol><li>后台页面是 singleton。</li><li>后台页面 ViewModel 是 singleton。</li><li>前台窗口是 singleton。</li><li>前台窗口 ViewModel 是 singleton。</li><li>大部分业务服务是 singleton。</li></ol><p>这适合导播工具的长期状态，但也意味着页面构造函数、事件订阅和计时器要谨慎。不要在页面构造里做不可重复释放的重操作；如果订阅全局事件，要考虑是否会泄漏或重复处理。</p><p>因为页面/ViewModel 是 singleton，构造函数里的事件订阅通常只发生一次；但如果在命令、页面 Loaded、弹窗或临时对象中订阅事件，就要有解绑策略。否则长时间直播中会出现重复响应或对象无法释放。</p><h2 id="本地化" tabindex="-1"><a class="header-anchor" href="#本地化"><span>本地化</span></a></h2><p>项目使用 <code>WPFLocalizeExtension</code>：</p><div class="language-xaml line-numbers-mode" data-highlighter="shiki" data-ext="xaml" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-xaml"><span class="line"><span>Text=&quot;{lex:Loc SomeKey}&quot;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>启动时设置 <code>LocalizeDictionary.Instance.Culture</code>，并将 <code>Application.Current.Resources[&quot;CurrentLanguage&quot;]</code> 设为当前 <code>XmlLanguage</code>。</p><p>坑点：</p><ol><li>新增用户可见文本应优先添加到 <code>Locales/Lang.resx</code>、<code>Lang.en-us.resx</code>、<code>Lang.ja-jp.resx</code>。</li><li>后台代码提示文本应通过 <code>I18nHelper.GetLocalizedString(...)</code>。</li><li>硬编码中文只适合内部日志、临时调试或明确不本地化的标识。</li><li>语言切换相关逻辑会监听 <code>Settings.Language</code> 和 <code>CultureInfo</code>，不要直接改 <code>CultureInfo</code> 私有 setter。</li><li><code>I18nHelper</code> 找不到 key 时会返回 key 本身；如果界面显示类似 <code>SomeMissingKey</code>，优先检查 resx 是否缺项或默认字典配置是否正确。</li></ol><h2 id="wpf-ui-图标" tabindex="-1"><a class="header-anchor" href="#wpf-ui-图标"><span>WPF-UI 图标</span></a></h2><p>后台代码中创建 WPF-UI 图标的安全模式可参考主窗口：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">new</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> SymbolIcon</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">SymbolRegular</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">Info24</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">24D</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>或：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">new</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> SymbolIcon</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> { </span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">Symbol</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;"> SymbolRegular</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">ArrowExit20</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> }</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>BackendPageInfo</code> 的图标字段直接使用 <code>SymbolRegular</code>。创建或更新 <code>SymbolIcon</code> 属于 WPF UI 对象操作，应在 UI 线程执行。后台下载、OCR、插件市场回调更新 UI 时，参考 <code>PluginMarketService.RunOnUiThread</code> 或 <code>Application.Current.Dispatcher.Invoke</code>。</p><p>不要在后台线程中创建 <code>NavigationViewItem</code>、<code>SymbolIcon</code>、<code>Page</code> 或 <code>Window</code> 并交给 UI 绑定集合。</p><h2 id="infobar-和-snackbar" tabindex="-1"><a class="header-anchor" href="#infobar-和-snackbar"><span>InfoBar 和 Snackbar</span></a></h2><p>主窗口构造时把控件交给服务：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">infoBarService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">SetInfoBarControl</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">InfoBar</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">snackbarService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">SetSnackbarPresenter</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;">SnbPre</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>页面或服务应通过 <code>IInfoBarService</code> / <code>ISnackbarService</code> 访问，不要跨层直接找主窗口控件。错误、警告和下载失败等适合 InfoBar；更新后提示使用 Snackbar。</p><h2 id="observablecollection" tabindex="-1"><a class="header-anchor" href="#observablecollection"><span>ObservableCollection</span></a></h2><p>绑定到 UI 的 <code>ObservableCollection</code> 必须在 UI 线程更新。插件市场下载队列已经用 Dispatcher 包装；新下载器回调、OCR 回调或捕获回调如果要改集合，应复用同类模式。直接从后台线程 Add/Remove 会导致 WPF 线程异常或间歇性 UI 崩溃。</p><h2 id="资源字典和主题" tabindex="-1"><a class="header-anchor" href="#资源字典和主题"><span>资源字典和主题</span></a></h2><p>启动时默认深色主题，主题变化会更新 <code>IconThemesDictionary.Theme</code>。如果新增图标资源或主题资源，需要确认它在浅色/深色下都可用。</p><p><code>MainWindow.xaml</code> 大量依赖动态资源和 WPF-UI 样式。修改全局样式前先确认前台窗口是否也引用了同名资源，避免导播输出窗口被后台样式意外影响。</p><h2 id="前台窗口透明和背景" tabindex="-1"><a class="header-anchor" href="#前台窗口透明和背景"><span>前台窗口透明和背景</span></a></h2><p>部分前台窗口支持透明背景。透明时返回 <code>Transparent</code>，非透明时默认绿色 <code>#00FF00</code>。OBS 场景可能依赖绿幕色或透明窗口，改默认颜色和 <code>AllowsWindowTransparency</code> 行为时需要兼顾直播工作流。</p><p><code>FrontedWindowBase</code> 会把内容自动包进 <code>Viewbox</code>，并设置 <code>Stretch.Fill</code>。前台窗口内部布局应以固定画布和绑定宽高为基础，不要假设窗口内容原样作为根元素存在。</p><p>Designer v3 的 <code>Image</code> 控件有 <code>Auto</code>、<code>FillContainer</code>、<code>OverflowCrop</code> 三种 <code>SizingMode</code>。旧 XAML 同时存在 direct fixed-size <code>ui:Image</code>、<code>Border + Image + ClipToBounds</code>、默认 <code>Border</code> 内图片和自定义 <code>MapV2Presenter</code>，迁移时必须逐个按旧结构选择模式。队伍 LOGO 和 MapBp v1 地图通常用 <code>FillContainer</code>；角色裁剪图通常用 <code>OverflowCrop</code>；GameData 求生者表头头像这类旧默认 <code>Image</code> 应保留 <code>Auto</code>。BpWindow 的求生者 pick 使用 <code>OverflowCrop + UniformToFill</code>，监管者 pick 保留旧 XAML 中本地 <code>Stretch=&quot;Uniform&quot;</code> 的效果。<code>CornerRadius</code> 只负责圆角裁剪，不应顺手把所有图片改成填满容器。</p><p>BpWindow 已由 v3 renderer 生成控件。默认动画通过行为文档中的稳定 <code>BehaviorGuid</code> 查找 <code>SurPick0..3</code>、<code>HunPick</code>，并通过 <code>part:{BehaviorGuid}:PickingBorder</code> 查找内部呼吸边框。修改内置布局时必须同步维护 <code>Resources/FrontedBehaviors/BpWindow.behaviors.json</code>；旧 <code>PickingBorderOverlay</code> 控件已移除。</p><h2 id="fronted-designer-编辑器" tabindex="-1"><a class="header-anchor" href="#fronted-designer-编辑器"><span>Fronted Designer 编辑器</span></a></h2>`,40)),i("p",null,[e[6]||(e[6]=o("独立编辑器的详细规格见 ",-1)),a(d,{to:"/dev/frontend/fronted-designer-editor.html"},{default:s(()=>[...e[5]||(e[5]=[o("fronted-designer-editor.md",-1)])]),_:1}),e[7]||(e[7]=o("。实现时特别注意这些 WPF 坑点：",-1))]),e[10]||(e[10]=c('<ol><li>编辑器窗口应使用 WPF-UI <code>FluentWindow</code> 和项目既有 <code>CustomTitleBar</code>，标题栏必须单独占一行。不要把 toolbar、preview 或验证面板放到标题栏同一行；否则关闭按钮会被内容覆盖。编辑器默认隐藏 <code>CustomTitleBar</code> 的主题切换按钮，保留最小化、最大化和关闭。</li><li>不要把真实前台窗口当作设计 surface。原生标题栏、窗口 chrome 和 <code>FrontedWindowBase</code> 的 <code>Viewbox</code> 包裹会让坐标混入内容区以外的高度，造成纵向偏移。编辑器应使用纯 <code>Canvas</code>，尺寸精确等于 <code>FrontedCanvasConfig.CanvasWidth</code> / <code>CanvasHeight</code>。</li><li>zoom/pan 修正后，编辑器预览不再用 <code>ViewBox</code> 控制 Fit 或手动缩放。结构应为 <code>ScrollViewer -&gt; PreviewWorkspace -&gt; PreviewZoomHost -&gt; DesignSurfaceGrid</code>，<code>PreviewZoomHost.LayoutTransform</code> 绑定唯一缩放来源 <code>ZoomScale</code>，这样放大后 <code>ScrollViewer</code> 才能获得真实可滚动 extent。</li><li><code>PreviewCanvas</code> 负责真实渲染，<code>InteractionLayer</code> 负责 hitbox、选择框、拖拽、缩放和键盘微调。两层尺寸都必须等于 <code>FrontedCanvasConfig.CanvasWidth</code> / <code>CanvasHeight</code>，鼠标位置使用 <code>e.GetPosition(InteractionLayer)</code> 得到逻辑 Canvas 坐标，不要乘除 zoom，也不要把窗口标题栏或真实前台窗口尺寸纳入坐标计算。</li><li>透明或空内容控件不可靠。空 <code>Text</code>、<code>Source = null</code> 的 <code>Image</code>、透明 <code>Border</code> 和没有当前业务数据的控件都可能难以命中。编辑器应在独立 <code>InteractionLayer</code> 为每个设计项创建透明 hitbox；hitbox 是 editor-only，不写入 layout JSON。</li><li>v3 JSON 的 root-level 控件 key 就是控件名。该名称同时参与 <code>FrameworkElement.Name</code> 和 WPF namescope 注册。不要在 config 类里再加 <code>Name</code> 字段，也不要让编辑器只改生成控件的 <code>Name</code> 而忘记 dictionary key。</li><li>空图片和空文本在 preview 中应通过编辑器 overlay 或设计时 placeholder 辅助识别。placeholder 只属于编辑器预览，不应写入 layout JSON 或运行时设置。</li><li>的方向键移动步长默认是 <code>0.5</code>。键盘事件应避开 <code>TextBox</code>、<code>ComboBox</code>、<code>DataGrid</code> 等编辑控件，避免后续 Property Grid 实现后抢输入焦点。<br> 9.，编辑器主区域是左侧控件列表、中间设计 surface、右侧选中/校验面板。左侧列表用于选中被遮挡或低 ZIndex 控件；筛选文本在切换窗口、切换 Canvas 或成功重载布局时清空。</li><li>鼠标选择语义是“单击选择，拖拽不切换选择”。<code>MouseLeftButtonDown</code> 只记录候选控件和起点；超过 3-5 logical px 阈值后，只有候选项本来就是当前选中项才开始拖拽。拖到未选中控件上不应改变焦点。</li><li>被选中控件的 hitbox、outline 和 handles 会使用 editor-only 高 ZIndex 放在其他 hitbox 上方，以便拖动重叠下层控件。该值不能写入 v3 JSON，也不能改变 preview/runtime <code>ZIndex</code>。</li><li>拖拽和缩放过程中要同步更新生成 preview root element 的 <code>Canvas.Left</code> / <code>Canvas.Top</code> / <code>Width</code> / <code>Height</code>，不要等 mouse-up 重渲染后才看到真实预览移动。mouse-up 可再重渲染一次保证一致。</li><li>选择边界优先使用显式 <code>Width</code> / <code>Height</code>；缺失时使用渲染 root element 的 <code>ActualWidth</code> / <code>ActualHeight</code>；再不可用才回退到 <code>40x24</code>。这对无 <code>Height</code> 的文本控件尤其重要。</li><li><code>Image</code> / <code>BorderedImage</code> 的 picking border 和 lock 是内部视觉层：编辑器中不生成普通 hitbox、不进入普通控件列表、不允许直接拖拽或缩放。移动/缩放图片控件时 overlay 自动跟随；picking border 的运行时名称由控件名自动生成。</li><li><code>CurrentBanDisplay</code>、<code>BanSlotDisplay</code> 和 <code>PickingBorderOverlay</code> 已移除。新 Ban 位和 pick 图不要再新增专用业务控件，优先使用通用 <code>Image</code> binding + overlay。</li><li>视口导航优先于选择：Fit 模式根据 <code>ScrollViewer</code> viewport 和 Canvas 尺寸计算 <code>ZoomScale</code>；<code>Ctrl + mouse wheel</code> 进入手动缩放并保持 25% 到 200%；右键拖拽或 <code>Space + left mouse drag</code> 只平移 <code>ScrollViewer</code> offset。这些操作不能写回 layout 坐标，也不能改变当前选中控件。</li><li>的 Property Grid 基于 <code>ItemsControl</code>，编辑的是 <code>FrontedControlDesignItem</code> 和其 <code>Config</code>。<code>Name</code> 仍是设计项/JSON key，不能加到 config 类；运行时关键 <code>Name</code> 只读，被其他控件引用的普通控件在 8E 也阻止改名。<br> 18.，Property Grid 行编辑器通过模板按需实例化，不要恢复成“TextBox、CheckBox、ComboBox 全部创建再用 Visibility 隐藏”的结构；否则切换选中控件时会出现未套样式的原生控件闪烁。</li><li>属性编辑事件必须避开绑定初始化：ComboBox 用 <code>DropDownClosed</code> 提交，TextBox 用 <code>LostFocus</code> 或 Enter 提交，CheckBox 用 Click 提交，ColorPicker 只在用户更改颜色后提交。属性网格重建和 layout pass 期间应抑制提交，避免 BpWindow / CutSceneWindow 选中控件时递归触发 <code>ApplyPropertyEdit</code>。</li><li>的 <code>FontFamily</code> 行使用可编辑 ComboBox，初始化、选中项同步和手写提交都必须尊重同一套提交抑制逻辑。内置字体选项保存 pack URI 原值，预览字体时沿用运行时的 <code>Uri + &quot;./#FontName&quot;</code> 构造方式，不要把显示名写回布局。</li><li>中 <code>BindingPath</code> 和图片/资源路径仍是显式提交文本框，但旁边会显示 Binding Browser 或 Resource Browser 的 <code>...</code> 按钮。浏览器选择只能写入 <code>FrontedPropertyEditorItem.EditText</code>，不能直接调用 <code>ApplyPropertyEdit</code>，也不能写入 config、推 Undo snapshot 或刷新真实前台窗口；用户按 Apply 或 Enter 后才提交。颜色字符串使用项目已有 <code>PortableColorPicker</code>，仍按 <code>#AARRGGBB</code> 存储，并保留文本 fallback；ColorPicker 选色只同步 Hex 编辑缓冲，必须由 Apply 或 Enter 显式提交，避免初始化/选色时绕过显式提交模型。</li><li>验证详情表不在右侧属性面板常驻显示；右侧应主要保留选中控件摘要和 Property Grid。底部左侧验证摘要可点击打开非模态验证详情窗口。</li><li>拖拽和缩放 live edit 中不要运行完整校验、不要重建 Property Grid、不要强制完整重渲染。只更新几何、linked overlay、preview element、hitbox/adorner、选中几何摘要和 dirty 状态；mouse-up/commit 后再统一校验和刷新。</li><li>Property Grid 输入控件获得键盘焦点时，方向键不应触发设计 surface 微调。新增编辑器控件后要继续更新 <code>ShouldIgnoreKeyboardInput()</code> 的排除列表。</li><li>的 Add Control 只添加内存设计项并重渲染编辑器 preview，不保存用户布局。新控件应放在当前滚动视口中心附近，并且不应暴露已移除的 <code>CurrentBanDisplay</code>、<code>BanSlotDisplay</code> 和 <code>PickingBorderOverlay</code>。<br> 26.，Delete Control 只在设计 surface 焦点下响应 Delete 键；焦点位于 <code>TextBox</code>、<code>ComboBox</code>、<code>DataGrid</code>、ColorPicker 或属性编辑器内部时必须忽略，避免编辑文本时误删控件。左侧控件列表右键菜单和 Property Grid 底部删除按钮都应调用同一个删除命令，继续复用运行时关键控件和 incoming reference 的删除保护。</li><li><code>Name</code>、<code>BindingPath</code> 和普通文本/资源路径属性使用显式提交：文本框绑定 <code>EditText</code>，按 Enter 或 Check/Apply 按钮提交。Enter 处理必须直接读取 <code>TextBox.Text</code>，不能依赖 <code>UpdateSourceTrigger=LostFocus</code> 后的 <code>Value</code>，否则会提交旧值或空值。</li><li>属性编辑失败时不要重建到丢失输入。应保留 <code>EditText</code>、设置 <code>HasEditError</code> / <code>EditError</code>、显示红色边框和行内错误消息；失败提交不应触发 preview render。</li><li><code>FontFamily</code> 的可编辑 ComboBox 不应在下拉打开时由 LostFocus 触发提交。下拉选择保存 <code>FrontedFontFamilyOption.Value</code>，手写字体按 Enter 或真正失焦保存 <code>ComboBox.Text</code>，内置字体 pack URI 不能被显示名替换。</li><li>右侧 Property Grid 面板通过中间 <code>GridSplitter</code> 调整宽度。拖动 splitter 只改变编辑器窗口布局，不写回 v3 layout JSON，也不需要在 持久化。</li><li>设计器 preview 使用独立 <code>DesignerPreviewSharedDataService</code>，只通过 <code>FrontedRenderContext.SharedDataServiceOverride</code> 传给 renderer。不要为了预览调用真实共享数据服务的 <code>NewGame()</code> 或修改真实 <code>CurrentGame</code>，否则会污染导播运行时状态。</li><li>Undo/Redo 快捷键只在设计 surface、列表或编辑器背景获得焦点时执行布局撤销/重做；焦点在 <code>TextBox</code>、<code>ComboBox</code>、ColorPicker 等属性编辑器内时必须让控件自身处理文本撤销。切换窗口/Canvas 或 reload 必须清空 undo/redo 栈。</li><li>Binding Browser 使用显式 root + attribute 反射 catalog，不应全局扫描任意服务或调用 getter，也不应恢复逐属性手写整树。绑定树节点必须保留真实 <code>ValueType</code>，树过滤和搜索都要使用同一 <code>FrontedBindingTypeFilter</code>：文本控件只允许字符串/数字，图片控件只允许 <code>ImageSource</code> 兼容值，bool overlay 绑定只允许 bool，<code>GameProgressText</code> 只允许 <code>GameProgress</code>，<code>MapNameText</code> 只允许 <code>Map</code> / <code>Map?</code>。浏览器选择仍只能写入属性行 <code>EditText</code>，不能绕过 Apply/Enter 直接提交 config。Resource Browser 读取 <code>Resources/bpui</code> 时缩略图必须用 <code>BitmapImage.CacheOption=OnLoad</code> 等方式避免锁文件；外部绝对路径只引用原文件，不复制到用户布局目录或 <code>.bpui</code> 包。</li><li>的吸附开关分为持久 <code>SnapEnabled</code> 和临时 <code>IsShiftSnapActive</code>。ToggleSwitch 只能绑定 <code>SnapEnabled</code>；Shift KeyDown/KeyUp 只更新临时状态和状态文字，不能反向修改 ToggleSwitch，否则拖拽时会造成开关频繁刷新和输入延迟。</li><li>的保存只写用户 AppData 布局，不能覆盖 <code>Resources/FrontedLayouts</code>。切换窗口/Canvas、reload、reset 和关闭窗口前都要处理 dirty prompt；Save 因校验 Error 失败时必须取消原动作。</li><li>WPF 不允许在窗口已经继续 closing 的过程中调用 <code>Show</code>、<code>ShowDialog</code>、<code>Close</code> 或 <code>EnsureHandle</code>。编辑器关闭时如果要询问未保存修改，必须先取消 <code>Closing</code>，再用 Dispatcher 异步显示提示；确认 Save/Discard 后用强制关闭标记再次关闭。非模态验证详情窗口随父窗口关闭时也要 try/catch <code>InvalidOperationException</code>。</li><li>编辑器顶部工具栏不能使用单行固定列布局承载全部命令。窗口/Canvas 选择器、缩放、吸附状态和 layout path 应放在可换行容器中；长路径必须限制宽度并省略显示，完整路径放到 tooltip。</li></ol><h2 id="插件-ui" tabindex="-1"><a class="header-anchor" href="#插件-ui"><span>插件 UI</span></a></h2><p>插件注册的页面/窗口也进入同一 DI 和 WPF UI 环境。插件作者应：</p><ol><li>后台页面使用 <code>BackendPageInfo</code>，前台 XAML 窗口使用 <code>AddFrontedWindow&lt;TWindow,TViewModel&gt;()</code> 配合 <code>[FrontedWindowInfo(IsBuiltIn = false)]</code>，v3 布局窗口使用 <code>AddFrontedV3LayoutWindow(&quot;WindowId&quot;, isBuiltIn: false)</code>。</li><li>避免和宿主或其他插件重复 ID。</li><li>插件 v3 控件应使用稳定控件名和合理默认几何。</li><li>用户可见文本同样考虑本地化。</li></ol><h2 id="navigationview" tabindex="-1"><a class="header-anchor" href="#navigationview"><span>NavigationView</span></a></h2><p>WPF-UI 默认的 <code>NavigationView</code> 样式和行为有坑点。Page被加载后外面会自动包一层 <code>ScrollViewer</code>，不要重复包裹，否则就导致页面无法滚动</p>',6))])}const y=n(p,[["render",h]]),m=JSON.parse('{"path":"/dev/wpf-ui-pitfalls.html","title":"WPF-UI 坑点记录","lang":"zh-CN","frontmatter":{"description":"WPF-UI 坑点记录 线程和异步相关规则另见 。资源、字体和本地化规则另见 。 导航和 DI 后台页面、内置前台窗口、ViewModel 和多数服务都通过 DI 注册。不要绕开： 手动 new Page() 或 new Window() 会丢失 DataContext、服务注入、注册表信息和 WPF-UI page provider 集成。插件前台窗...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"WPF-UI 坑点记录\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/wpf-ui-pitfalls.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"WPF-UI 坑点记录"}],["meta",{"property":"og:description","content":"WPF-UI 坑点记录 线程和异步相关规则另见 。资源、字体和本地化规则另见 。 导航和 DI 后台页面、内置前台窗口、ViewModel 和多数服务都通过 DI 注册。不要绕开： 手动 new Page() 或 new Window() 会丢失 DataContext、服务注入、注册表信息和 WPF-UI page provider 集成。插件前台窗..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":13.43,"words":4028},"filePathRelative":"dev/wpf-ui-pitfalls.md","autoDesc":true}');export{y as comp,m as data};
