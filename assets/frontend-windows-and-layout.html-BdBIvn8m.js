import{_ as s}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as a,b as i,d,e as o,a as t,w as c,r,o as l}from"./app-CGOhbY2V.js";const p={};function h(u,e){const n=r("RouteLink");return l(),a("div",null,[e[11]||(e[11]=i(`<h1 id="前台窗口与布局" tabindex="-1"><a class="header-anchor" href="#前台窗口与布局"><span>前台窗口与布局</span></a></h1><p><code>builtin</code> 与已安装方案一样是普通活动布局包，只是物理根目录指向应用运行时 <code>Resources/FrontedLayouts</code>。package manager 模式不读取旧用户布局存储。包切换会完整刷新已创建 v3 窗口；透明度变化通过单窗口静默重启生效。</p><h2 id="前台窗口是什么" tabindex="-1"><a class="header-anchor" href="#前台窗口是什么"><span>前台窗口是什么</span></a></h2><p>“前台窗口”对应代码中的 <code>FrontedWindow</code>，是独立 WPF 窗口，用于直播软件捕获。它不是 Web frontend。</p><p>内置前台窗口包括 BP、过场、求生者/监管者比分、全局比分、赛后数据、BP 概览和地图 BP v2 等。OBS 工作流通常是：后台控制数据，前台窗口显示画面，OBS 捕获前台窗口。</p><h2 id="注册模型" tabindex="-1"><a class="header-anchor" href="#注册模型"><span>注册模型</span></a></h2><p>前台窗口采用强类型 registration 模型，基类 <code>FrontedWindowRegistration</code> 只暴露 <code>Id</code>、<code>LocalId</code>、<code>PackageId</code>、<code>IsBuiltIn</code>、<code>DisplayName</code>、<code>Kind</code>。来源分组（BuiltIn / Plugin / External）由 UI 层基于 <code>IsBuiltIn + PackageId</code> 推导，顺序使用 DI 注册顺序或 UI 按 <code>LocalId</code> 排序，不再有 <code>GroupKey</code>/<code>DisplayOrder</code>/<code>I18nDisplayNames</code>。</p><p>内置前台窗口在 <code>App.Services.xaml.cs</code> 中通过 <code>AddFrontedV3LayoutWindow</code> 注册：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">services</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">AddFrontedV3LayoutWindow</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;BpWindow&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">isBuiltIn</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">true</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">services</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">AddFrontedV3LayoutWindow</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;CutSceneWindow&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">isBuiltIn</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">: </span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">true</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">);</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// ... 其他内置 v3 窗口</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>v3 layout 已改为 Window-centric。新的 v3 layout window 只以 Window 为管理单位，运行时固定由 <code>FrontedWindowBase</code> 创建 <code>ViewBox -&gt; Canvas BaseCanvas</code>，不再向用户、包管理或 FrontManagePage 暴露 Canvas。传统固定 XAML window 可继续使用原有注册方式，但不强制 BaseCanvas。</p><p>插件前台窗口通过两个公开 API 注册，窗口类型不在 <code>manifest.yml</code> 中指定：</p><ol><li><strong>XAML 窗口</strong>：<code>services.AddFrontedWindow&lt;TWindow, TViewModel&gt;()</code><ul><li>窗口类需 <code>[FrontedWindowInfo]</code> 特性，<code>Id</code> 为稳定窗口 Local ID（推荐 GUID 以降低与历史插件冲突的可能，但不强制；允许任何通过 Window Local ID 安全校验的稳定字符串，不含 <code>/</code>、<code>\\</code>、<code>:</code>、控制字符或前后空白）</li><li>继承 <code>FrontedWindowBase</code></li><li>Attribute ID 作为运行时 Canonical ID（PackageId 仅表示来源，不参与 v3 layout / Designer）</li><li>示例：<div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">[</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">FrontedWindowInfo</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;3363BFE1-1393-4765-B926-001B6848FAF7&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">, </span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">&quot;Example XAML Window&quot;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">)]</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> partial</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> class</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> ExampleXamlWindow</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> : </span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">FrontedWindowBase</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    public</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> ExampleXamlWindow</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">() =&gt; </span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">InitializeComponent</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">();</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">// 注册：services.AddFrontedWindow&lt;ExampleXamlWindow, ExampleXamlWindowViewModel&gt;();</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul></li><li><strong>v3 Layout 窗口</strong>：<code>services.AddFrontedV3LayoutWindow(&quot;WindowId&quot;, isBuiltIn: false)</code><ul><li><code>windowId</code> 是局部标识，只需插件内唯一</li><li>PackageId 由宿主自动注入，不是 API 参数</li><li>加载顺序：激活 package → 空模板（无默认 JSON 时使用空模板；宿主不从插件安装目录加载默认布局）</li><li>示例：<code>services.AddFrontedV3LayoutWindow(&quot;ExampleLayoutOverlay&quot;);</code></li></ul></li></ol><p><code>FrontedWindowRegistrationKind</code> 枚举只有 <code>Xaml</code> / <code>V3Layout</code>。窗口身份使用 Canonical ID：内置为 <code>BpWindow</code>，插件 v3 layout 为 <code>plugin:{PackageId}/{LocalWindowId}</code>，XAML 窗口为 Attribute ID（插件为 <code>plugin:{PackageId}/{AttributeId}</code>，宿主直接注册的为 <code>{AttributeId}</code>）；该 ID 作为布局、<code>.bpui</code> manifest 和用户目录中的窗口身份。XAML 窗口不进入 v3 layout / Designer / <code>.bpui</code> layout entry。插件窗口分为两类：</p><table><thead><tr><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>Xaml</code></td><td>插件提供真实 WPF <code>Window</code> 类型，可选 ViewModel，由宿主统一显示/隐藏；不参与 v3 layout / Designer</td></tr><tr><td><code>V3Layout</code></td><td>插件只声明窗口，宿主用 <code>FrontedWindowBase</code> 配置驱动 host 承载 v3 renderer；无默认 JSON 时使用空模板</td></tr></tbody></table><h2 id="frontedwindowservice" tabindex="-1"><a class="header-anchor" href="#frontedwindowservice"><span>FrontedWindowService</span></a></h2><p><code>FrontedWindowService</code> 构造时只保存服务依赖和 registry，不创建前台输出窗口。窗口实例通过 <code>EnsureWindowCreated(windowId)</code> 按需创建：</p><ol><li><code>ShowWindow(windowId)</code> 首次调用时只创建对应 registration 的窗口。</li><li><code>AllWindowShow()</code> 遍历 registry registration，再逐个按需创建并显示。</li><li><code>HideWindow()</code>、<code>GetWindowName()</code>、动画查找和布局 dirty 标记都不会创建窗口。</li><li>已创建窗口保留在 <code>FrontedWindows</code> 表中；关闭按钮触发 <code>OnClosing -&gt; Hide()</code>，不会销毁窗口内容。</li></ol><p>核心状态：</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>FrontedWindows</code></td><td><code>windowId -&gt; Window</code>；v3 layout window 由 <code>FrontedWindowBase</code> host 创建</td></tr><tr><td><code>FrontedWindowStates</code></td><td>窗口是否已显示</td></tr></tbody></table><blockquote><p><strong>注意</strong>：旧 <code>FrontedWindowService</code> 的位置保存/恢复逻辑已删除。运行时不再从 <code>AppData</code> 读写 <code>{WindowName}Config-{CanvasName}.json</code>，也不再从 <code>Resources/FrontedDefaultPositions</code> 读取默认位置。前台布局状态现在完全由 v3 <code>FrontedLayouts</code> 驱动。</p></blockquote><h2 id="显示与隐藏" tabindex="-1"><a class="header-anchor" href="#显示与隐藏"><span>显示与隐藏</span></a></h2><p>窗口由 <code>ShowWindow</code> / <code>HideWindow</code> / <code>AllWindowShow</code> / <code>AllWindowHide</code> 管理。关闭前台窗口时，<code>FrontedWindowBase.OnClosing</code> 会取消关闭并改为 <code>Hide()</code>，避免窗口实例被销毁后 OBS 捕获关系变得不可预期。<code>FrontedWindowService</code> 需要真正销毁实例时会走显式 service close 通道，绕过 close-to-hide。</p><p>v3 layout 窗口的显示流程分两段执行：Show 前调用 <code>EnsureInitialWindowSettingsAppliedAsync()</code>，只读取并应用 <code>WindowSettings</code> 中必须在 HWND/source 创建前确定的设置（尺寸、位置、Topmost、AllowsTransparency、BackgroundColor、ViewboxStretch）；随后立刻 <code>Show()</code>。完整 <code>CanvasSettings</code>、控件渲染、资源解析和 behavior runtime attach 由 <code>LoadOrReloadContentAsync(force: false)</code> 在 Show 后异步完成。</p><p>已经创建且 <code>IsContentRendered == true</code>、<code>IsLayoutDirty == false</code> 的 v3 窗口再次显示时，不重新加载完整 layout，也不重新 <code>RenderToCanvas</code>。如果 Hide/Unloaded 时 behavior runtime 已 detach，下一次 Show 只重新 attach behavior runtime，不重建控件。Designer 保存布局、包激活/删除或 BO3/BO5 切换会标记或触发 reload；普通 Hide/Show 不会把内容标脏。</p><p><code>FrontedWindowBase</code> 还会：</p><ol><li>自动用 <code>Viewbox</code> 包裹内容，让内容按窗口宽高填充。</li><li>默认无边框、不可调整大小、居中启动。</li><li>非设计模式下支持鼠标拖动窗口。</li></ol><h2 id="设计者模式和布局文件" tabindex="-1"><a class="header-anchor" href="#设计者模式和布局文件"><span>设计者模式和布局文件</span></a></h2><blockquote><p><strong>注意</strong>：旧版&quot;真实前台窗口内编辑&quot;设计器模式已废弃移除。Designer v3 独立编辑器（<code>FrontedDesignerWindow</code>）是当前唯一支持的设计编辑器。</p></blockquote><p>Fronted Designer v3 的基础设施已经存在：<code>FrontedWindowConfig</code> 是新主路径模型，包含 <code>WindowSettings</code>、<code>CanvasSettings</code> 和 <code>ControlLayout</code>。<code>IFrontedLayoutService</code> 会按活动布局方案读取 <code>FrontedLayouts/{WindowTypeName}.json</code>；内置默认布局位于 <code>Resources\\FrontedLayouts\\{WindowTypeName}.json</code>，包内布局位于 <code>FrontedLayouts/{WindowTypeName}.json</code>，behavior 位于 <code>FrontedBehaviors/{WindowTypeName}.behaviors.json</code>。<code>IFrontedRenderer</code> 可用注册的控件工厂生成 Text/Image/GlobalScoreRow 等控件。<code>Text</code> 和 <code>LocalizedText</code> 的动态内容使用 <code>TextBinding</code>：有序 <code>Sources</code> 绑定 <code>ISharedDataService</code>，<code>StringFormat</code> 非空时使用复合格式，否则按 <code>JoinSeparator</code> 连接；没有有效 source 时分别回退到静态 <code>Text</code> 或 <code>LocalizationKey</code>。需要业务规则文本时，应使用 <code>GameProgressText</code> / <code>MapNameText</code> 等业务控件，而不是普通静态 <code>Text</code>。<code>WidgetsWindow</code> 和 MapV1 已删除；旧 <code>BpOverViewCanvas</code> 迁移为 <code>BpOverviewWindow</code>，旧 <code>MapV2Canvas</code> 迁移为 <code>MapV2Window</code>。<code>MapV2Display</code> 继续复用 <code>MapV2Presenter</code>，不要把地图 BP v2 拆成普通图片和文字控件。<code>FrontedWindowService</code> 不会读取旧 <code>FrontedDefaultPositions</code> 作为 v3 输入。</p><p>当前已迁移的内置前台窗口全部使用 v3 layout 作为默认渲染来源。v3 layout 管理单位只剩 Window；Canvas/BaseCanvas 只是运行时实现细节，不再按 Canvas 维度读取、编辑、保存或恢复布局。</p><p>v3 renderer 会为生成控件注册 namescope 名称，并在清理生成控件前注销这些名称。行为动画通过稳定 <code>BehaviorGuid</code> 和 <code>part:{BehaviorGuid}:PickingBorder</code> 定位目标；注册名称继续用于布局契约、迁移和诊断。</p>`,31)),d("p",null,[e[1]||(e[1]=o("v3 layout 中 ",-1)),e[2]||(e[2]=d("code",null,"ControlLayout.Controls",-1)),e[3]||(e[3]=o(" 的 JSON key 就是控件名。该名称同时作为控件 dictionary key、生成控件 ",-1)),e[4]||(e[4]=d("code",null,"FrameworkElement.Name",-1)),e[5]||(e[5]=o(" 和 namescope 注册名。独立编辑器必须通过设计项 ",-1)),e[6]||(e[6]=d("code",null,"Name",-1)),e[7]||(e[7]=o(" 编辑 dictionary key，不能给 config 类新增重复 ",-1)),e[8]||(e[8]=d("code",null,"Name",-1)),e[9]||(e[9]=o(" 字段。详细编辑器规格见 ",-1)),t(n,{to:"/dev/frontend/fronted-designer-editor.html"},{default:c(()=>[...e[0]||(e[0]=[o("fronted-designer-editor.md",-1)])]),_:1}),e[10]||(e[10]=o("。",-1))]),e[12]||(e[12]=i(`<p>v3 layout 支持通用 BO3/BO5 Canvas states。<code>CanvasSettings</code> root 是默认/BO5；<code>EnableBoModeStates = true</code> 时，<code>BoModeStates[&quot;Bo3&quot;]</code> 可保存独立 BO3 背景、插件依赖和控件集合。所有已创建的内置 v3 前台窗口和插件 Layout 承载窗口都会在 <code>ISharedDataService.IsBo3ModeChanged</code> 后标记 layout dirty；窗口可见时立即异步重载内容，窗口隐藏时等下次 Show 再刷新。renderer 根据当前 BO 模式选择 root/BO5 或 BO3 state；如果启用但缺少 BO3 state，则回退 root/BO5 并记录 warning。<code>ScoreGlobalWindow</code> 只是该通用机制的一个使用者，不再使用窗口专用背景切换逻辑。</p><h2 id="naming-rule-do-not-use-generic-isactive" tabindex="-1"><a class="header-anchor" href="#naming-rule-do-not-use-generic-isactive"><span>Naming rule: do not use generic IsActive</span></a></h2><p><code>IsActive</code> 只保留给内部框架/运行时激活语义，尤其是 CommunityToolkit.Mvvm <code>ObservableRecipient.IsActive</code>。</p><p>不要把 <code>IsActive</code> 用作布局、包、设置或业务状态字段。包激活状态使用 <code>IsActivePackage</code>；窗口列表或设计器可见性使用 <code>IsVisibleInFrontManage</code>、<code>IsVisible</code>、<code>IsBadgeVisible</code> 等明确名称；启用、选中、展开状态分别使用 <code>IsEnabled</code>、<code>IsSelected</code>、<code>IsExpanded</code>。</p><p>旧 <code>.bpui</code> 包可能在 <code>TextSettings</code> 中包含 <code>IsActive</code>，这是旧设置类继承 <code>ObservableRecipient</code> 造成的序列化泄漏。该字段不是文本样式启用标记，LegacyConverter 必须忽略它。</p><p><code>Visibility</code> 绑定必须使用 <code>IsVisible</code> 或具体的可见性语义属性，不得绑定泛名 <code>IsActive</code>。</p><p>后台侧独立 <code>FrontedDesignerWindow</code> shell 已实现。<code>FrontedDesignerLayoutCatalog</code> 只从 <code>IFrontedWindowRegistry.GetV3LayoutWindows()</code> 获取可定制 v3 layout window（例如 <code>ScoreSurWindow</code>、<code>ScoreHunWindow</code>、<code>ScoreGlobalWindow</code>、<code>CutSceneWindow</code>、<code>GameDataWindow</code>、<code>BpWindow</code>、<code>BpOverviewWindow</code> 和 <code>MapV2Window</code>，具体由 App DI 注册决定），不存在硬编码 fallback 或内置窗口清单；XAML 窗口不进入 Designer。选择窗口后，编辑器按 <code>IFrontedLayoutService</code> 的活动布局方案规则加载 <code>FrontedWindowConfig</code>，内部转换到设计文档，运行 <code>FrontedLayoutValidator</code>，再用现有 <code>IFrontedRenderer</code> 渲染到编辑器自己的只读 <code>PreviewCanvas</code>。如果当前活动方案是 <code>builtin</code>，保存时会自动复制出可编辑用户布局方案并激活，避免覆盖内置资源。</p><p>该预览 Canvas 的 <code>Width</code> 和 <code>Height</code> 直接来自 <code>CanvasSettings.CanvasWidth</code> / <code>CanvasHeight</code>，不使用真实前台窗口的 <code>ActualHeight</code>、外框或标题栏尺寸，因此不会引入标题栏高度偏移。v3 layout window 的真实窗口宽高来自 <code>WindowSettings.WindowWidth</code> / <code>WindowHeight</code>，不会在普通读取、保存、包导入或导出时被 Canvas 尺寸覆盖。独立编辑器已支持内存交互层、基础 Property Grid 和 Add Control：可选中普通设计项，编辑名称、布局、绑定文本和简单控件属性，把新控件添加到当前内存文档并即时重渲染预览。它不创建真实前台输出窗口作为设计 surface。</p><p>注意：<code>ScoreGlobalWindow</code> 的 BO3/BO5 背景、总分位置和比分行配置现在由通用 Canvas state 控制；BO5 使用 root state，BO3 使用 <code>BoModeStates[&quot;Bo3&quot;]</code>。</p><p>legacy 布局文件命名约定（仅用于 legacy <code>.bpui</code> 转换，不再被运行时读取）：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>%APPDATA%\\neo-bpsys-wpf\\{WindowTypeName}Config-{CanvasName}.json</span></span>
<span class="line"><span>%APPDATA%\\neo-bpsys-wpf\\{WindowTypeName}Config-{CanvasName}.default.json</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>legacy 内置默认布局位于：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>neo-bpsys-wpf/Resources/FrontedDefaultPositions</span></span>
<span class="line"><span>{pluginFolder}/FrontedDefaultPositions/{WindowTypeName}Config-{CanvasName}.default.json (legacy)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>注意</strong>：这些 legacy 位置文件不再被运行时 <code>FrontedWindowService</code> 读取。它们只属于 legacy <code>.bpui</code> 转换流程。重置布局通过 Layout Packages 激活内置布局或删除用户布局实现。</p></blockquote><p>v3 布局文件命名约定：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>%APPDATA%\\neo-bpsys-wpf\\FrontedLayouts\\{WindowTypeName}.json</span></span>
<span class="line"><span>neo-bpsys-wpf/Resources/FrontedLayouts/{WindowTypeName}.json</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>v3 独立编辑器保存用户布局时应写入 AppData 的 <code>FrontedLayouts</code> 目录；“重置为内置”应删除或忽略用户布局，再回落到 <code>Resources/FrontedLayouts</code> 下的默认布局。</p><p><code>FrontManagePage</code> 使用顶层 tabs：<code>Frontend Windows</code> 提供前台窗口打开/关闭和独立编辑器入口，<code>Layout Packages</code> 提供 v3 布局包管理器。<code>Frontend Windows</code> tab 不再包含旧版设计模式 ToggleSwitch，Reset 按钮也已移除；布局重置通过激活/删除包实现。包列表使用紧凑两栏布局，右侧详情按 Basic、Contents、Location、Validation 分组。当前可列出系统内置包、已安装包和活动包状态，并可导入、导出、激活和删除 v3 <code>.bpui</code> 包。导出固定为全部前台布局；导入 legacy <code>.bpui</code> 会触发转换流程。</p><p>激活普通包时，包内 <code>FrontedLayouts/{WindowTypeName}.json</code> 和 <code>FrontedBehaviors/{WindowTypeName}.behaviors.json</code> 会作为活动包数据读取。激活内置布局或删除活动包会回退到内置 <code>Resources/FrontedLayouts</code>。已创建的前台窗口会尝试重新渲染 v3 布局；未创建过的窗口不会因此被创建。</p><p>注意：v3 布局读取用户布局优先。如果用户目录下已有旧开发版 v3 JSON，不保证兼容；需要重置为内置布局或通过 legacy <code>.bpui</code> 转换重新生成 Window-centric 布局。</p><h2 id="插件前台窗口和控件" tabindex="-1"><a class="header-anchor" href="#插件前台窗口和控件"><span>插件前台窗口和控件</span></a></h2><p>旧的“向现有内置前台窗口注入 WPF 控件”能力已移除。插件应使用 Designer v3 插件控件、Plugin XAML Window 或 Plugin v3 Layout Window。</p><p>前台窗口使用 Canonical ID 作为运行时、Designer 和 <code>.bpui</code> 的统一身份：内置窗口为 <code>BpWindow</code> 等短名（即 LocalId），插件 v3 窗口为 <code>plugin:{PackageId}/{LocalWindowId}</code>，XAML 窗口为 Attribute ID（插件为 <code>plugin:{PackageId}/{AttributeId}</code>，宿主直接注册的为 <code>{AttributeId}</code>）。<code>FrontedWindowInfo.Id</code> 是稳定的窗口 Local ID；推荐新插件使用 GUID 以降低与历史插件冲突的可能，但不强制。XAML 窗口不参与 v3 layout、Designer 或 <code>.bpui</code> layout entry。用户目录中的插件窗口磁盘路径会转换为安全路径，例如 <code>FrontedLayouts/plugin/top.plfjy.demo/ExampleLayoutOverlay.json</code>。插件 v3 窗口不从插件安装目录加载默认布局；无默认 JSON 时使用空模板。XAML 窗口的 PackageId 仅表示来源，不参与 v3 layout / Designer。</p><h2 id="透明背景" tabindex="-1"><a class="header-anchor" href="#透明背景"><span>透明背景</span></a></h2><p>v3 layout window 的 <code>AllowsTransparency</code> 和 <code>BackgroundColor</code> 位于 <code>WindowSettings</code>。<code>AllowsTransparency</code> 必须在 WPF window source 初始化前应用；切换这类会影响 source 的 WPF 属性时，<code>FrontedWindowService</code> 会在窗口已经创建的情况下静默重启对应前台窗口实例，不需要重启应用。若窗口当前可见，旧实例会先发布 hidden、真正关闭并移出字典，然后创建新实例、在 Show 前应用最新 <code>WindowSettings</code>、Show 后重新加载 layout 内容并发布 shown。若窗口已创建但隐藏，只关闭并移除旧实例，下一次 <code>ShowWindow</code> 会按最新设置重新创建。若窗口从未创建，则无需操作。Canvas 纯色背景不属于 <code>CanvasSettings.BackgroundColor</code>，应使用 Rectangle / Shape 控件实现。</p>`,25))])}const y=s(p,[["render",h]]),v=JSON.parse('{"path":"/dev/frontend/frontend-windows-and-layout.html","title":"前台窗口与布局","lang":"zh-CN","frontmatter":{"description":"前台窗口与布局 builtin 与已安装方案一样是普通活动布局包，只是物理根目录指向应用运行时 Resources/FrontedLayouts。package manager 模式不读取旧用户布局存储。包切换会完整刷新已创建 v3 窗口；透明度变化通过单窗口静默重启生效。 前台窗口是什么 “前台窗口”对应代码中的 FrontedWindow，是独立 ...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"前台窗口与布局\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/frontend/frontend-windows-and-layout.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"前台窗口与布局"}],["meta",{"property":"og:description","content":"前台窗口与布局 builtin 与已安装方案一样是普通活动布局包，只是物理根目录指向应用运行时 Resources/FrontedLayouts。package manager 模式不读取旧用户布局存储。包切换会完整刷新已创建 v3 窗口；透明度变化通过单窗口静默重启生效。 前台窗口是什么 “前台窗口”对应代码中的 FrontedWindow，是独立 ..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":10.84,"words":3252},"filePathRelative":"dev/frontend/frontend-windows-and-layout.md","autoDesc":true}');export{y as comp,v as data};
