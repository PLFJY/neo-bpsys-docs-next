import{_ as a}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c,d,e as t,a as i,w as n,b as r,r as s,o as l}from"./app-CGOhbY2V.js";const p={};function u(h,e){const o=s("RouteLink");return l(),c("div",null,[e[5]||(e[5]=d("h1",{id:"引导式-bp",tabindex:"-1"},[d("a",{class:"header-anchor",href:"#引导式-bp"},[d("span",null,"引导式 BP")])],-1)),d("p",null,[e[1]||(e[1]=t("首次启动导览和页面 Product Tour 见 ",-1)),i(o,{to:"/dev/backend/product-tour-and-onboarding.html"},{default:n(()=>[...e[0]||(e[0]=[t("product-tour-and-onboarding.md",-1)])]),_:1}),e[2]||(e[2]=t("。Product Tour 可以引导用户点击“开启对局引导”并等待相关 signal，但不替代 ",-1)),e[3]||(e[3]=d("code",null,"GameGuidanceService",-1)),e[4]||(e[4]=t("，也不重写对局引导的规则推进、计时器、导航和高亮业务逻辑。",-1))]),e[6]||(e[6]=r(`<h2 id="核心服务" tabindex="-1"><a class="header-anchor" href="#核心服务"><span>核心服务</span></a></h2><p><code>GameGuidanceService</code> 实现对局引导功能。它依赖：</p><table><thead><tr><th>依赖</th><th>用途</th></tr></thead><tbody><tr><td><code>ISharedDataService</code></td><td>读取当前对局、设置 Ban 位数量、启动计时器</td></tr><tr><td><code>INavigationService</code></td><td>切换后台页面</td></tr><tr><td><code>IInfoBarService</code></td><td>显示非阻断提示</td></tr><tr><td><code>WeakReferenceMessenger</code></td><td>发送高亮消息</td></tr></tbody></table><p>规则文件路径固定为：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>{AppDomain.CurrentDomain.BaseDirectory}\\GameRule.json</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>主项目文件中设置了 <code>GameRule.json</code> 总是复制到输出目录。</p><h2 id="规则结构" tabindex="-1"><a class="header-anchor" href="#规则结构"><span>规则结构</span></a></h2><p><code>GameRule.json</code> 是 <code>GameProgress -&gt; GameProperty</code> 的字典。<code>GameProperty</code> 包含：</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>SurCurrentBan</code></td><td>当前局求生者 Ban 位数</td></tr><tr><td><code>HunCurrentBan</code></td><td>当前局监管者 Ban 位数</td></tr><tr><td><code>SurGlobalBan</code></td><td>全局求生者 Ban 位数</td></tr><tr><td><code>HunGlobalBan</code></td><td>全局监管者 Ban 位数</td></tr><tr><td><code>WorkFlow</code></td><td>步骤列表</td></tr></tbody></table><p>每个步骤包含：</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>Action</code></td><td><code>GameAction</code>，如 <code>BanMap</code>、<code>PickSur</code></td></tr><tr><td><code>Index</code></td><td>需要高亮的索引列表，可为 <code>null</code></td></tr><tr><td><code>Time</code></td><td>计时器秒数，可为 <code>null</code></td></tr></tbody></table><h2 id="启动流程" tabindex="-1"><a class="header-anchor" href="#启动流程"><span>启动流程</span></a></h2><p><code>StartGuidance()</code> 会：</p><ol><li>读取当前 <code>CurrentGame.GameProgress</code> 对应规则。</li><li>如果是 <code>GameProgress.Free</code> 或规则不支持，提示自由赛不可用。</li><li>设置当前/全局 Ban 位数量。</li><li>从记录更新双方全局 Ban。</li><li>设置 <code>IsGuidanceStarted = true</code>。</li><li>立即进入下一步。</li></ol><p><code>HandleStepChange</code> 会：</p><ol><li>在更新 <code>_currentStep</code> 前读取当前步骤，作为步骤变化事件的上一步信息。</li><li>更新 <code>_currentStep</code>。</li><li>如果步骤不是 <code>PickCamp</code>，按 <code>_actionToPage</code> 导航到对应后台页面。</li><li>调用 <code>_sharedDataService.TimerStart(thisStep.Time)</code>。</li><li>等待 250ms，让待选框动画/页面状态就位。</li><li>构造包含当前步骤和上一步骤 payload 的 <code>GameGuidanceStepChangedEventArgs</code>。</li><li>触发权威步骤事件 <code>GuidanceStepChanged</code>，前台行为运行时通过 <code>Guidance.StepChanged</code> 消费它。</li><li>触发后台高亮事件 <code>GuidanceHighlightChanged</code>。</li><li>发送后台 UI 便利消息 <code>HighlightMessage(thisStep.Action, thisStep.Index)</code>。</li><li>返回本地化后的步骤名称。</li></ol><p><code>MoveToStepAsync</code> 用于 SmartBP 手动强制同步等明确允许直接定位的路径，成功时返回 <code>null</code>，失败时返回错误文本。SmartBP 自动落后追赶禁止调用该接口，正常追赶只能通过 <code>NextStepAsync</code> 按工作流逐步前进。安全角色证据修正较早的同槽 <code>CommittedEmpty</code> 时只补充角色，不移动 Guidance。自动回退是低优先级的明显超前纠偏：仅当 Guidance 至少领先槽位目标两个工作流步骤、当前 Action 与目标 Action 不同，且目标槽位存在安全 <code>Selected</code> 强证据时，才逐次调用 <code>PrevStepAsync</code> 回到最早未满足的前置步骤；随后仍逐步前进，不直接定位。它和 <code>StartGuidance</code>、<code>NextStepAsync</code>、<code>PrevStepAsync</code>、<code>StopGuidance</code>、<code>CompleteGuidance</code> 都会在存在 WPF <code>Application.Current.Dispatcher</code> 时切回 UI Dispatcher 执行，避免后台识别线程直接修改 WPF 导航、计时器、消息和事件状态。</p><p>SmartBP 不会在每个 OCR Tick 都执行追赶。它将当前步骤和槽位推导目标表示为 <code>Action + 规范化 Indexes</code> 的值对象；只有两者不相等、目标之前存在宿主 <code>Pending</code> 槽位洞、目标 <code>Pending</code>/<code>CommittedEmpty</code> 槽位出现新的已选角色证据、<code>DistributeChara</code> 存在实际补位/交换操作，或 Guidance 尚未启动时才进入自动 Reconciliation。仅仅看到同 Action 前一组槽位选满，或主程序已经提交这些槽位，都不会推断到下一组；必须存在下一组槽位证据，或由中间其他 Action 的已完成槽位证明已经跨过。中间 Ban 未完成时保留在前一 Pick 组，下一 Pick 槽真正出现后才允许把中间明确空 Ban 作为前置步骤逐步提交。位置已对齐且没有待补槽位时不会调用 <code>NextStepAsync</code>，也不会安排历史回看 OCR；Guidance 已经超过目标时也不安排历史回看，优先原地等待或按严格条件纠偏。</p><h2 id="行为事件-payload" tabindex="-1"><a class="header-anchor" href="#行为事件-payload"><span>行为事件 payload</span></a></h2><p><code>Guidance.StepChanged</code> 同时暴露当前步骤和上一步骤 payload。当前步骤 payload 适合启动动画，上一步骤 payload 适合停止由切换前引导步骤启动的动画。首次进入步骤时，所有 <code>Previous*</code> 值为 <code>null</code>，<code>PreviousIndexesText</code> 为 <code>[]</code>。</p><p><code>Guidance.Cancelled</code> 在用户取消当前引导时触发，<code>Guidance.Stopped</code> 在引导以停止/完成语义结束时触发。两者 payload 使用稳定机器值：<code>Reason</code>、<code>Time</code>、<code>PreviousStepIndex</code>、<code>PreviousAction</code>、<code>PreviousIndexes</code> 和 <code>PreviousIndexesText</code>，不包含本地化操作名。Loop 行为的 <code>StopTriggers</code> 应把这些事件与 <code>Guidance.StepChanged</code> 的上一步过滤一起配置，避免取消引导后循环动画残留。</p><p>列表索引的字符串过滤应优先使用 <code>IndexesText</code> / <code>PreviousIndexesText</code>，其格式稳定为 <code>[1, 2]</code>。高亮变化与清除事件只用于后台引导 UI，不暴露给前台行为触发器。</p><p>例如，启动求生者 1 号位呼吸灯：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Event.Action == PickSur</span></span>
<span class="line"><span>Event.IndexesText contains &quot;1&quot;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>停止由上一步启动的求生者 1 号位呼吸灯：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>Event.PreviousAction == PickSur</span></span>
<span class="line"><span>Event.PreviousIndexesText contains &quot;1&quot;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="action-到页面映射" tabindex="-1"><a class="header-anchor" href="#action-到页面映射"><span>Action 到页面映射</span></a></h2><table><thead><tr><th>Action</th><th>页面</th></tr></thead><tbody><tr><td><code>BanMap</code> / <code>PickMap</code></td><td><code>MapBpPage</code></td></tr><tr><td><code>BanSur</code></td><td><code>BanSurPage</code></td></tr><tr><td><code>BanHun</code></td><td><code>BanHunPage</code></td></tr><tr><td><code>PickSur</code> / <code>DistributeChara</code> / <code>PickHun</code></td><td><code>PickPage</code></td></tr><tr><td><code>PickSurTalent</code> / <code>PickHunTalent</code></td><td><code>TalentPage</code></td></tr></tbody></table><p><code>PickCamp</code> 当前不会触发页面切换。</p><h2 id="维护建议" tabindex="-1"><a class="header-anchor" href="#维护建议"><span>维护建议</span></a></h2><p>规则应跟随实际第五人格赛事规则变化维护，但本仓库中的 <code>GameRule.json</code> 不是外部权威规则源。修改规则时应：</p><ol><li>明确对应哪个 <code>GameProgress</code>。</li><li>检查 Ban 位数量与页面控件容量一致。</li><li>确认 <code>GameAction</code> 是否已映射到后台页面。</li><li>检查 <code>Index</code> 是否能被对应页面的高亮逻辑理解。</li><li>自由赛 <code>GameProgress.Free</code> 当前不支持引导式 BP，这是代码显式行为。</li></ol>`,32))])}const b=a(p,[["render",u]]),v=JSON.parse('{"path":"/dev/backend/game-guidance.html","title":"引导式 BP","lang":"zh-CN","frontmatter":{"description":"引导式 BP 首次启动导览和页面 Product Tour 见 。Product Tour 可以引导用户点击“开启对局引导”并等待相关 signal，但不替代 GameGuidanceService，也不重写对局引导的规则推进、计时器、导航和高亮业务逻辑。 核心服务 GameGuidanceService 实现对局引导功能。它依赖： 规则文件路径固定为...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"引导式 BP\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/backend/game-guidance.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"引导式 BP"}],["meta",{"property":"og:description","content":"引导式 BP 首次启动导览和页面 Product Tour 见 。Product Tour 可以引导用户点击“开启对局引导”并等待相关 signal，但不替代 GameGuidanceService，也不重写对局引导的规则推进、计时器、导航和高亮业务逻辑。 核心服务 GameGuidanceService 实现对局引导功能。它依赖： 规则文件路径固定为..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":4.64,"words":1392},"filePathRelative":"dev/backend/game-guidance.md","autoDesc":true}');export{b as comp,v as data};
