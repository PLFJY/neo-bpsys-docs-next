import{_ as i}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as n,d,e as t,a as o,w as c,b as s,r,o as l}from"./app-CGOhbY2V.js";const p={};function h(m,e){const a=r("RouteLink");return l(),n("div",null,[e[5]||(e[5]=d("h1",{id:"smartbp-与-ocr",tabindex:"-1"},[d("a",{class:"header-anchor",href:"#smartbp-与-ocr"},[d("span",null,"SmartBP 与 OCR")])],-1)),d("p",null,[e[2]||(e[2]=t("线程和后台任务注意事项见 ",-1)),o(a,{to:"/dev/architecture/threading-dispatcher-and-async.html"},{default:c(()=>[...e[0]||(e[0]=[t("threading-dispatcher-and-async.md",-1)])]),_:1}),e[3]||(e[3]=t("。SmartBP 默认配置和资源文件见 ",-1)),o(a,{to:"/dev/resources/resources-localization-and-assets.html"},{default:c(()=>[...e[1]||(e[1]=[t("resources-localization-and-assets.md",-1)])]),_:1}),e[4]||(e[4]=t("。",-1))]),e[6]||(e[6]=s(`<h2 id="架构概览" tabindex="-1"><a class="header-anchor" href="#架构概览"><span>架构概览</span></a></h2><p>SmartBP 是面向第五人格赛事的直播导播辅助系统中的智能 BP 识别与赛后数据回填子系统。它采用<strong>模块分离架构</strong>：主应用只保留 SmartBP 页面壳、安装覆盖层、模块加载器、状态模型和功能命令抽象；真正的 SmartBP UI、OCR 引擎、OpenCvSharp 等重型依赖全部位于 <code>neo-bpsys-wpf.SmartBp.Module</code> 独立项目中。</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>neo-bpsys-wpf (主应用)</span></span>
<span class="line"><span>├── Views/Pages/SmartBpPage.xaml          ← 页面壳 + 模块未加载覆盖层</span></span>
<span class="line"><span>├── ViewModels/Pages/SmartBpPageViewModel  ← 模块加载/安装/版本管理</span></span>
<span class="line"><span>├── Core/Models/SmartBpModule/             ← 模块 manifest、状态、命令抽象</span></span>
<span class="line"><span>└── Core/Abstractions/Services/ISmartBpService.cs  ← 服务契约（宿主侧）</span></span>
<span class="line"><span></span></span>
<span class="line"><span>neo-bpsys-wpf.SmartBp.Module (SmartBP 模块)</span></span>
<span class="line"><span>├── SmartBpModuleEntryPoint.cs             ← 模块入口，实现 ISmartBpModuleEntryPoint</span></span>
<span class="line"><span>│   ├── CreateSmartBpContent() → 构建 DI 容器并返回 SmartBpModuleContentView</span></span>
<span class="line"><span>│   └── GetFeatureCommands()   → 暴露 AutoFillGameData 命令</span></span>
<span class="line"><span>├── Services/</span></span>
<span class="line"><span>│   ├── SmartBpService.cs                  ← 赛后数据 OCR 回填（宿主侧服务）</span></span>
<span class="line"><span>│   ├── Recognition/</span></span>
<span class="line"><span>│   │   ├── SmartBpOcrRecognitionServices.cs  ← OCR BP 识别全流程</span></span>
<span class="line"><span>│   │   ├── SmartBpSceneGateService.cs        ← 场景门禁</span></span>
<span class="line"><span>│   │   ├── SmartBpAutoRecognitionCoordinator ← 自动循环主控</span></span>
<span class="line"><span>│   │   └── ... (更多识别/状态/应用管线)</span></span>
<span class="line"><span>│   └── OcrService.cs / PaddleOcrProvider.cs / TesseractOcrProvider.cs / RapidOcrNetProvider.cs</span></span>
<span class="line"><span>├── ViewModels/SmartBpModuleContentViewModel.cs ← 模块 UI 主 ViewModel</span></span>
<span class="line"><span>├── Abstractions/                          ← 模块内部接口</span></span>
<span class="line"><span>└── Models/Recognition/                    ← 识别数据模型</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="两条能力线" tabindex="-1"><a class="header-anchor" href="#两条能力线"><span>两条能力线</span></a></h3><p>SmartBP 包含两条独立的能力线：</p><table><thead><tr><th>能力线</th><th>入口</th><th>引擎</th><th>产出</th><th>成熟度</th></tr></thead><tbody><tr><td>赛后数据 OCR 回填</td><td><code>ISmartBpService.AutoFillGameDataAsync</code></td><td>PaddleOCR</td><td>直接写 <code>CurrentGame</code></td><td>成熟可用</td></tr><tr><td>BP 状态自动识别</td><td><code>SmartBpAutoRecognitionCoordinator</code></td><td>OCR-only</td><td>候选操作 → 应用管线 → <code>CurrentGame</code></td><td>BP 状态 OCR 可用</td></tr></tbody></table><p>两条线共享同一套窗口捕获和 OCR 模型管理基础设施。赛后数据回填直接基于完整捕获帧的 OCR 坐标重建表格；全流程 BP 状态识别则使用可编辑的粗裁剪区域。</p><h3 id="宿主侧接口" tabindex="-1"><a class="header-anchor" href="#宿主侧接口"><span>宿主侧接口</span></a></h3><p>宿主侧在 <code>Core.Abstractions.Services</code> 中定义 <code>ISmartBpService</code>：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> interface</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;"> ISmartBpService</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    bool</span><span style="--shiki-light:#4078F2;--shiki-dark:#ABB2BF;"> IsSmartBpRunning</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> { </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">get</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">; }</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    void</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> StartSmartBp</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">();</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">    void</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> StopSmartBp</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">();</span></span>
<span class="line"><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">    Task</span><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;"> AutoFillGameDataAsync</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">(</span><span style="--shiki-light:#C18401;--shiki-dark:#E5C07B;">CancellationToken</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;"> cancellationToken</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;"> =</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> default);</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>实际实现 <code>SmartBpService</code> 位于 SmartBP 模块，依赖模块提供的 <code>IOcrService</code>、角色解析和捕获服务等。</p><h3 id="模块入口与-di" tabindex="-1"><a class="header-anchor" href="#模块入口与-di"><span>模块入口与 DI</span></a></h3><p><code>SmartBpModuleEntryPoint</code> 是模块的单一入口点，实现 <code>ISmartBpModuleEntryPoint</code>：</p><ul><li><code>CreateSmartBpContent(hostServices)</code>：构建独立的 <code>ServiceProvider</code>，注入所有模块级服务（OCR Provider、识别管线、debug log 等），创建 <code>SmartBpModuleContentView</code> 并绑定 <code>SmartBpModuleContentViewModel</code></li><li><code>GetFeatureCommands()</code>：返回宿主可调用的功能命令，目前只有 <code>AutoFillGameData</code>（赛后数据回填）</li></ul><p>模块 DI 容器注册了 OCR Provider（Paddle、Tesseract、RapidOCR）、BP 识别管线、场景门禁、候选操作和唯一的 Reconciliation 服务。SmartBP 不注册 Ban/Pick StateStore、进度评分器、回填服务或操作 ledger。</p><h2 id="当前边界" tabindex="-1"><a class="header-anchor" href="#当前边界"><span>当前边界</span></a></h2><table><thead><tr><th>能力</th><th>当前状态</th></tr></thead><tbody><tr><td>赛后数据 OCR 自动回填</td><td>已经成熟且可用</td></tr><tr><td>PaddleOCR BP 状态识别</td><td>默认 OCR Provider；读取文字与边界框，本地解析阶段、禁用、选择与玩家 ID</td></tr><tr><td>Tesseract BP 状态识别</td><td>可选 OCR Provider；可在 SmartBP 页面勾选下载 <code>chi_sim</code>/<code>eng</code>/<code>jpn</code> 到 SmartBP 模块目录，不会自动回退到 Paddle</td></tr><tr><td>RapidOCR BP 状态识别</td><td>可选本地 OCR Provider；使用 SmartBP 托管的中文 det/cls/rec/dict 资产，不会自动回退到其他 Provider</td></tr><tr><td>本地视觉模型 + llama.cpp BP 状态识别</td><td>已从 SmartBP 模块移除；BP 状态识别仅支持 OCR Provider</td></tr><tr><td>GameGuidance 自动追赶</td><td>可选；按工作流槽位逐步前进，不跨过未完成的 Ban/Pick 步骤</td></tr><tr><td>识别结果自动应用</td><td>可选；仅通过 <code>ICharacterSelectionService</code> 应用高置信度且已解析的当前帧角色证据，或在引导落后时由同局短时帧安全补充的遗漏证据</td></tr><tr><td>手动强制同步</td><td>对当前帧执行独立全量 OCR；角色无动画写入主程序，再直接定位到当前画面对应步骤</td></tr><tr><td>全流程自动 BP 画面切换</td><td>TODO。当前识别结果只进入现有候选操作/应用管线，不实现自动切屏</td></tr><tr><td>MapBP 识别</td><td>不识别</td></tr><tr><td>天赋角色操作识别</td><td>不识别</td></tr></tbody></table><p>不要在文档、UI 或提交说明中把&quot;全流程自动 BP&quot;或 MapBP 识别描述为已完成。</p><h2 id="smartbp-ui-页面" tabindex="-1"><a class="header-anchor" href="#smartbp-ui-页面"><span>SmartBP UI 页面</span></a></h2><p>SmartBP 后台页面 (<code>SmartBpPage.xaml</code>) 是一个 WPF <code>Page</code>，包含两层结构：</p><ol><li><strong>模块未加载层</strong>：当 SmartBP 模块尚未安装或加载失败时，显示覆盖层，包含模块路径选择、在线安装/导入压缩包/选择本地目录等按钮</li><li><strong>模块内容层</strong>：通过 <code>ContentControl</code> 承载 <code>SmartBpModuleEntryPoint.CreateSmartBpContent()</code> 返回的 <code>SmartBpModuleContentView</code></li></ol><p><code>SmartBpModuleContentViewModel</code> 是模块 UI 的主 ViewModel，管理：</p><ul><li>窗口捕获（WGC / Bitblt）和窗口选择</li><li>OCR 模型下载/切换/删除</li><li>赛后数据区域配置编辑器（通过 <code>RegionEditorWindow</code>）</li><li>OCR BP 自动识别启停、设置与 debug 日志展示</li><li>自动识别启停与 debug 日志展示</li></ul><h2 id="ocr-模型管理" tabindex="-1"><a class="header-anchor" href="#ocr-模型管理"><span>OCR 模型管理</span></a></h2><p>SmartBP 重型运行时不再随默认安装包进入主应用发布目录。主应用只保留 SmartBP 页面壳、安装覆盖层、模块加载器、状态模型和功能命令抽象；真实 SmartBP UI、<code>SmartBpService</code>、<code>OcrService</code>、OpenCvSharp、PaddleOCR、PaddleInference 和 OCR 模型下载逻辑位于 <code>neo-bpsys-wpf.SmartBp.Module</code>。当前代码库和构建流水线以 .NET 10 为基线。</p><p>模块安装状态保存到：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>%APPDATA%\\neo-bpsys-wpf\\SmartBpModuleState.json</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>状态中的 <code>ModuleRoot</code> 指向 SmartBP 模块目录。Debug 构建可以直接加载 <code>neo-bpsys-wpf.SmartBp.Module\\bin\\Debug\\net10.0-windows10.0.20348\\</code>，并以 <code>DevelopmentDirectory</code> 记录，不要求 <code>component.json</code> 或版本比较。</p><p><code>OcrService</code> 负责 PaddleOCR 模型和推理实例：</p><table><thead><tr><th>功能</th><th>代码位置</th></tr></thead><tbody><tr><td>模型枚举</td><td><code>SmartBpOcrModelRegistry.Models</code></td></tr><tr><td>模型本地根目录</td><td><code>{SmartBpModuleRoot}\\OCRModels</code></td></tr><tr><td>下载</td><td><code>DownloadModelAsync</code>，分 det/cls/rec 三步</td></tr><tr><td>删除</td><td><code>TryDeleteModel</code></td></tr><tr><td>切换</td><td><code>TrySwitchOcrModel</code></td></tr><tr><td>推理</td><td><code>RecognizeText(Mat img)</code></td></tr><tr><td>推理失败重建</td><td><code>RecognizeTextCore</code> 中捕获异常后 <code>TryRebuildCurrentOcrUnsafe()</code></td></tr></tbody></table><p>可用模型键包括 <code>zh-cn-v5-mobile</code>、<code>en-v4-mobile</code>、<code>ja-v4-mobile</code>、<code>zh-cn-v4</code>、<code>zh-cn-v3-slim</code>。是否安装通过 det/cls/rec 目录下的 <code>inference.pdiparams</code> 和 <code>inference.pdmodel</code> 或 <code>inference.json</code> 判断。</p><p>OCR 下载失败会清理模型目录和字典残留；切换成功会把 <code>Settings.OcrModelKey</code> 持久化到配置文件。旧版本已经下载到 <code>Documents\\neo-bpsys-wpf\\OCRModels</code> 的模型会在模块首次安装或首次成功加载后迁移到 <code>{SmartBpModuleRoot}\\OCRModels</code>，迁移使用 copy -&gt; verify -&gt; delete，且不会删除 <code>Documents\\neo-bpsys-wpf</code> 根目录。</p><h2 id="模块加载与安装" tabindex="-1"><a class="header-anchor" href="#模块加载与安装"><span>模块加载与安装</span></a></h2><p>SmartBP 页面先显示宿主侧页面壳。模块未加载时，内容区域显示覆盖层；模块加载成功后，模块入口实现 <code>ISmartBpModuleEntryPoint</code> 并返回真实 SmartBP 内容。</p><p>模块目录通过 <code>component.json</code> 校验：</p><ol><li><code>ComponentId</code> 必须是 <code>SmartBpModule</code>。</li><li><code>Rid</code> 必须匹配 <code>win-x64</code>。</li><li><code>RuntimeAbiVersion</code> 必须匹配宿主 ABI。</li><li>必须存在模块入口程序集 <code>neo-bpsys-wpf.SmartBp.Module.dll</code>。</li></ol><p>宿主使用独立 <code>AssemblyLoadContext</code> 加载模块。托管依赖优先复用宿主程序集，再从模块根目录解析；OpenCvSharp、PaddleInference 等原生依赖则从模块的 <code>runtimes/{Rid}/native/</code> 目录解析，并兼容位于模块根目录的原生 DLL。模块发布和迁移时必须保留 <code>runtimes</code> 目录结构，否则托管包装程序集存在也无法初始化对应 native runtime。</p><p>Release 构建只检查当前应用版本对应的 tag，不会获取最新 release。优先直接访问 GitHub Releases 下载当前 tag 对应的 <code>SmartBpModuleManifest.json</code>（<code>https://github.com/PLFJY/neo-bpsys-wpf/releases/download/{tag}/SmartBpModuleManifest.json</code>），失败时回退到 <code>https://smartbp-module-manifest.plfjy.top/?tag={tag}</code> 获取。官方模块 asset 是 <code>SmartBpModule.7z</code>，实际下载地址会套用设置中的 GitHub 下载镜像。该镜像由软件更新、插件市场设置和 SmartBP 模块下载高级选项共用 <code>GhProxyMirror</code> 持久化字段，SmartBP 页面可直接测试候选镜像延迟。</p><p>远程版本检查只作为更新提示，不阻塞本地模块加载：ABI 兼容性由 <code>component.json</code> 的 <code>RuntimeAbiVersion</code> 硬性校验保证，本地模块只要通过目录、RID、ABI 和入口程序集校验就立即加载显示。加载成功后异步拉取远端 manifest，仅在本地版本低于要求版本时通过 <code>ModuleVersionOutdated</code> 事件触发 <code>IInfoBarService</code> 警告提示用户更新；拉取失败或网络不可达时静默跳过，不影响已加载模块使用。Preview 构建不进行在线检查，主要支持选择本地模块目录或导入 <code>SmartBpModule.7z</code> / 旧 <code>SmartBpModule.zip</code>。</p><p>SmartBP 模块在线安装和手动导入支持 <code>.7z</code> 与旧 <code>.zip</code> 包，归档格式通过文件内容探测，不只依赖扩展名。运行时解压使用随应用发布的官方 x64 7-Zip（位于 <code>&lt;AppBase&gt;/Tools/7Zip/</code>），用户不需要单独安装 7-Zip。这只影响 SmartBP 模块包；<code>.bpui</code> / Designer v3 布局包导入导出行为不变。</p><h2 id="smartbpservice-赛后数据流程" tabindex="-1"><a class="header-anchor" href="#smartbpservice-赛后数据流程"><span>SmartBpService 赛后数据流程</span></a></h2><p><code>AutoFillGameDataAsync</code> 的主流程：</p><ol><li>检查 OCR 模型是否已选择且已安装。</li><li>检查 <code>IWindowCaptureService.IsCapturing</code>。</li><li>通过窗口捕获服务读取当前帧。</li><li>对完整捕获帧只执行一次带边界框的 OCR。</li><li>从“玩家 ID（角色名）”文本按 Y 坐标建立五个有效行；取角色文本右侧的数字和空值标记，按 X 坐标推断五个统计列，再按最近 Y 坐标归属。</li><li>对整表 OCR 漏掉的数据格，按推断出的行列中心紧密裁剪小区域，并生成原图放大、CLAHE 对比度增强、Otsu 二值反色加粗三种变体。Paddle 模式下直接调用字符识别器而跳过文本位置检测，避免细窄数字 <code>1</code> 因没有检测框而消失；结果只有在多个变体一致，或单一变体置信度足够高时才并回整表重新解析。低置信度的干净 <code>1</code> 以及类似 <code>1r</code>、<code>1A</code> 的“数字加一个尾随噪声字符”都会触发排除底部表格线的窄中心裁剪识别，并检查中心高窄亮色笔画；它们和图像形态证据都不能各自单独回填，必须形成至少两份相互确认的证据。</li><li>名称左侧的等级、天赋数字、徽章和其他残留文字不会参与名称、角色或数据列解析。</li><li>将监管者字段直接写回 <code>CurrentGame.HunPlayer.Data</code>。</li><li>将求生者数据按角色名匹配后写回 <code>CurrentGame.SurPlayerList</code>。</li></ol><p>求生者匹配先做规范化精确匹配，再用 Jaro-Winkler 模糊匹配兜底，阈值当前为 <code>0.50</code>。</p><h2 id="bp-状态-ocr-识别流程" tabindex="-1"><a class="header-anchor" href="#bp-状态-ocr-识别流程"><span>BP 状态 OCR 识别流程</span></a></h2><p>BP 状态识别和赛后数据 OCR 是两条不同流程。BP 识别先生成仅代表当前帧的 <code>SmartBpBusinessStateRecognitionResult</code>，再由统一 Reconciliation 读取主程序槽位状态并通过 <code>ICharacterSelectionService</code> 提交；SmartBP 不合并或持久化第二份 Ban/Pick 业务状态。</p><p>默认引擎是 PaddleOCR：</p><ol><li>从当前捕获帧裁剪 <code>phase_top</code> 和 planner 请求的内容区域。</li><li>默认把裁剪图按纵向拼成无文字标签的 OCR contact sheet，只运行一次 PaddleOCR。</li><li>读取 <code>PaddleOcrResult.Regions</code> 中的文本、置信度和 <code>RotatedRect</code>，转换为按 <code>CenterY</code>、<code>CenterX</code> 排序的文本行与轴对齐边界框。</li><li>按 contact sheet 坐标把文本行映射回 <code>phase_top</code>、<code>left_top</code>、<code>right_top</code>、<code>left_bottom</code>、<code>right_bottom</code>。</li><li>本地规则根据 <code>phase_top</code> 文本和左右侧 X 坐标判断阶段；非活动侧 <code>等待中</code> 不覆盖活动阶段。</li><li>本地解析四个粗区域：<code>right_top -&gt; banned_sur</code>、<code>left_top -&gt; banned_hun</code>、<code>left_bottom -&gt; picked_sur</code>、<code>right_bottom -&gt; picked_hun</code>。</li><li>角色名只从 <code>ISharedDataService.SurCharaDict</code> / <code>HunCharaDict</code> 匹配；无法明确解析的 OCR 文本只进入诊断，不会应用为角色。</li></ol><p><code>UseOcrContactSheet = false</code> 时会逐区域 OCR，主要用于排查 contact sheet 映射问题。OCR 识别默认间隔较短；短时回看窗口由帧缓冲长度、OCR 周期、最低 OCR 周期和 <code>RecognitionTransitionLookBehindMilliseconds</code> 共同限定。</p><p>自动 BP 循环使用 <code>SmartBpRecognitionScene</code> 场景门禁。角色 BP 场景才允许生成和应用 Ban/Pick 操作；求生者/监管者天赋阶段只允许同步引导；大厅、规则、禁选顺序、转场不写入。区域选择、等待开始、加载和对局内会阻断当前帧的内容识别与新操作生成，并停止调度后续 tick；已经排队或正在应用的角色 BP 操作会继续完成，队列排空后才以 <code>SmartBpCharacterBpEnded</code> 正常完成 GameGuidance 和自动识别，不触发取消事件。区域选择不属于 MapBP 或角色 BP 识别范围。用户手动停止仍会立即取消当前识别。</p><p>PaddleOCR 模型、RapidOCR 模型和 Tesseract 语言文件统一经 <code>SmartBpParallelDownload</code> 适配到宿主 <code>IFileDownloadService</code>：文件按顺序处理，单个文件支持暂停、继续、取消后保留分片，以及由 <code>Downloader</code> 管理的 HTTP Range 续传；百分比和速度直接采用 <code>Downloader</code> 原生进度回调。服务端不支持 Range 时会安全地从头下载。瞬时网络错误最多重试 5 次。多个安装资源仍按顺序处理，避免同时下载模型造成网络、内存与磁盘争抢。</p><p>当前不识别 MapBP，不识别天赋结果，不直接修改 <code>CurrentGame</code>。</p><h3 id="自动识别循环全流程" tabindex="-1"><a class="header-anchor" href="#自动识别循环全流程"><span>自动识别循环全流程</span></a></h3><p>BP 状态自动识别的完整循环由 <code>SmartBpAutoRecognitionCoordinator</code> 协调。业务状态只存在于主程序；一次 tick 的 OCR 结果只代表当前帧：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>捕获帧</span></span>
<span class="line"><span>  ├─ 高频轻量采样 → SmartBpFrameRingBuffer（有界、绑定 Game Guid + GameProgress）</span></span>
<span class="line"><span>  ├─ OCR TopCenterStatus / TopLeftStatus → 生命周期和结束门禁</span></span>
<span class="line"><span>  ├─ OCR PhaseTop → 得到本帧权威 phase</span></span>
<span class="line"><span>  ├─ 当前 phase 确认后识别四类角色槽位</span></span>
<span class="line"><span>  │    └─ SmartBpOcrRegionParser → 固定几何槽位、名称解析、置信度和安全元数据</span></span>
<span class="line"><span>  ├─ SmartBpSceneGateService → 决定本帧能否处理角色</span></span>
<span class="line"><span>  ├─ SmartBpCatchUpTriggerEvaluator</span></span>
<span class="line"><span>  │    ├─ 值比较 Action + 规范化 Indexes</span></span>
<span class="line"><span>  │    └─ 仅在位置不一致、前置 Pending 槽位洞或 Pending 目标槽已有新角色证据时触发</span></span>
<span class="line"><span>  ├─ 触发落后追赶时 SmartBpHistoricalFrameReviewService</span></span>
<span class="line"><span>  │    ├─ 默认只看目标之前 2 个工作流步骤，每步最多 1 张代表帧</span></span>
<span class="line"><span>  │    ├─ Phase/Action 必须和所补步骤严格对齐</span></span>
<span class="line"><span>  │    └─ 只补 Pending 且当前帧未明确选择的历史角色证据</span></span>
<span class="line"><span>  └─ SmartBpReconciliationService</span></span>
<span class="line"><span>       ├─ IGameGuidanceService.GetRuntimeSnapshot() → 工作流 Action/Indexes</span></span>
<span class="line"><span>       ├─ ICharacterSelectionService.GetCurrentBpSlotCommitState() → 主程序槽位真值</span></span>
<span class="line"><span>       ├─ SmartBpCandidateOperationBuilder → 当前帧安全候选</span></span>
<span class="line"><span>       ├─ ICharacterSelectionService → 角色或明确空操作提交</span></span>
<span class="line"><span>       └─ IGameGuidanceService → 自动逐步追赶或手动直接定位</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="场景门禁详解" tabindex="-1"><a class="header-anchor" href="#场景门禁详解"><span>场景门禁详解</span></a></h3><p><code>SmartBpSceneGateService.Classify()</code> 根据识别到的 phase 文本、业务状态和原始引擎响应，将当前画面分为以下场景：</p><table><thead><tr><th>场景</th><th>枚举值</th><th>角色操作</th><th>引导同步</th><th>暂停循环</th><th>触发条件</th></tr></thead><tbody><tr><td>角色 BP</td><td><code>CharacterBp</code></td><td>允许</td><td>允许</td><td>否</td><td>检测到屏蔽/选择/角色选择中等文本</td></tr><tr><td>求生者天赋</td><td><code>SurvivorTalent</code></td><td>允许补齐仍可见槽位</td><td>允许</td><td>否</td><td>检测到&quot;求生者天赋特质调整&quot;</td></tr><tr><td>监管者天赋</td><td><code>HunterTalent</code></td><td>允许补齐仍可见槽位</td><td>允许</td><td>否</td><td>检测到&quot;监管者天赋特质调整&quot;/&quot;监管者选择天赋中&quot;</td></tr><tr><td>天赋已锁定</td><td><code>TalentLocked</code></td><td>禁止</td><td>允许</td><td>是</td><td>检测到&quot;天赋已锁定&quot;</td></tr><tr><td>区域选择-求生者</td><td><code>AreaSelectionSurvivor</code></td><td>禁止</td><td>禁止</td><td>是</td><td>检测到&quot;求生者选择区域中&quot;</td></tr><tr><td>区域选择-监管者</td><td><code>AreaSelectionHunter</code></td><td>禁止</td><td>禁止</td><td>是</td><td>检测到&quot;监管者选择区域中&quot;</td></tr><tr><td>等待开始</td><td><code>WaitingGameStart</code></td><td>禁止</td><td>禁止</td><td>是</td><td>检测到&quot;等待游戏开始&quot;</td></tr><tr><td>加载中</td><td><code>Loading</code></td><td>禁止</td><td>禁止</td><td>是</td><td>检测到&quot;加载中&quot;/&quot;正在加载&quot;</td></tr><tr><td>对局中</td><td><code>InGame</code></td><td>禁止</td><td>禁止</td><td>是</td><td>检测到对局内 HUD 文本</td></tr><tr><td>大厅</td><td><code>Lobby</code></td><td>禁止</td><td>禁止</td><td>否</td><td>检测到&quot;大厅&quot;</td></tr><tr><td>规则设置</td><td><code>RulesDialog</code></td><td>禁止</td><td>禁止</td><td>否</td><td>检测到&quot;规则设置&quot;</td></tr><tr><td>禁选顺序</td><td><code>BanPickOrderDialog</code></td><td>禁止</td><td>禁止</td><td>否</td><td>检测到&quot;查看禁选顺序&quot;/&quot;选择禁用数量&quot;</td></tr><tr><td>转场</td><td><code>Transition</code></td><td>禁止</td><td>禁止</td><td>否</td><td>检测到&quot;开始案件还原&quot;/&quot;阵容选择中&quot;等</td></tr></tbody></table><h3 id="自动逐步追赶与手动强制同步" tabindex="-1"><a class="header-anchor" href="#自动逐步追赶与手动强制同步"><span>自动逐步追赶与手动强制同步</span></a></h3><p>两种行为共享 <code>SmartBpReconciliationService</code> 和主程序槽位真值，但执行语义不同，禁止混用。</p><h4 id="自动逐步追赶" tabindex="-1"><a class="header-anchor" href="#自动逐步追赶"><span>自动逐步追赶</span></a></h4><p>自动追赶用当前帧 phase 确定 Action 类别，用 <code>IGameGuidanceService.GetRuntimeSnapshot()</code> 中每个步骤的 <code>Indexes</code> 区分同一 Action 的多次出现，并将当前帧已识别槽位与主程序已提交槽位合并判断。<code>SmartBpWorkflowPosition</code> 对 <code>Action + 排序去重后的 Indexes</code> 实现值相等；相同 Action 但槽位集合不同仍然是不相等的位置。仅识别到前一组槽位刚刚选满时不会擅自跳到同 Action 的下一组；必须识别到下一组的槽位证据，或由两次同 Action 之间的其他业务步骤已经完成来证明阶段确实越过。例如画面为第二次 <code>PickSur</code> 且已经识别到求生者槽位 2，而引导尚未开始时，目标是 <code>PickSur[2]</code>，追赶顺序必须是：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>BanSur[0,1] → PickSur[0,1] → BanSur[2] → PickSur[2]</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>具体约束如下：</p><ol><li>先执行廉价触发判定。只有以下任一条件成立才安排 Reconciliation：Guidance 尚未启动但允许自动同步；当前 <code>Action + Indexes</code> 与槽位推导目标不同；目标之前仍有 <code>Pending</code> 业务步骤；<code>Pending</code> 或 <code>CommittedEmpty</code> 槽位已有新的安全 <code>Selected</code> 证据；或 <code>DistributeChara</code> 有安全的分配证据需要恢复/交换。全部不成立时本 Tick 不调用 Reconciliation，也不调度历史 OCR。宿主槽位已经写入只表示该业务操作已提交，不能单独证明画面已进入同 Action 的下一组槽位。</li><li>引导未启动或没有有效当前步骤时，调用 <code>StartGuidance()</code> 进入工作流首步。</li><li>只有引导未超过槽位推导目标，且位置不一致或目标之前存在 Pending 槽位洞时才回看。<code>OcrBackfillLookBehindSteps</code> 默认 2，表示只考虑目标之前两个工作流步骤；每个待补步骤最多选择一张当前对局上下文中的历史代表帧，并只请求 Phase 加该 Action 对应的一个角色区域，避免旧实现每 Tick 最多 16 次四区域完整 OCR。引导已经在目标之后时，本轮优先依据当前帧决定等待或纠偏，不额外用历史 OCR 放大回跳判断。</li><li>历史帧识别出的 Phase 必须映射为被补步骤的同一 Action，否则整帧拒绝。回看结果采用“补充合并”：宿主必须仍为 <code>Pending</code> 或 <code>CommittedEmpty</code>，当前帧已有明确角色时禁止覆盖，历史 Empty 不补入，也不把某槽候选顺延到相邻槽位。当前帧已经给出安全角色时直接进入 Reconciliation，不再额外调度历史 OCR。</li><li>每次只处理当前 Guidance 步骤的 <code>Indexes</code>，且只写主程序中的可补槽位。安全角色允许把同槽 <code>Pending</code> 或 <code>CommittedEmpty</code> 升级为 <code>CommittedCharacter</code>，但绝不覆盖已有 <code>CommittedCharacter</code>。若 Guidance 已经越过该槽，安全角色以 <code>AutomaticSupplement</code> 模式补入同一固定槽位，仍播放正常角色动画，但补入本身不移动 Guidance。回看角色和当前帧角色进入同一候选校验与正常业务服务，不存在直接改集合的强行填入。</li><li>普通 Ban/Pick 严格按视觉槽位 Index 写入。一个多槽 Ban 步骤中，若较早槽位具有明确、安全的 Empty 证据，且同一步骤更后槽位已经明确选择角色，则该空洞可提交为 <code>CommittedEmpty</code>；没有后续选择证明的当前/未来“未选择”仍保持 <code>Pending</code>。<code>CommittedEmpty</code> 会保留为空，除非同一固定槽位后来出现安全、明确的角色证据；这种证据只升级该空槽，不会移动角色或覆盖其他槽位。</li><li>角色写入使用 <code>CurrentStep</code> 模式，向 <code>ICharacterSelectionService</code> 传 <code>playAnimation=true</code>；这就是识别选角应出现入场动画的路径。</li><li>写入后重新读取 <code>BpSlotCommitState</code>。当前业务步骤仍未完成时立即停止，不能跨越。</li><li>当前步骤完成且尚未到目标时，只调用一次 <code>NextStepAsync()</code>；循环逐步执行，自动路径禁止 <code>MoveToStepAsync()</code> 跳步。目标定位遵循“当前槽位证据 → 已完成的跨 Action 前置步骤 → 原地等待”的优先级：例如 <code>PickSur[0,1]</code> 已出现但 <code>BanSur[2]</code> 仍为 <code>Pending</code> 时，不能仅因 <code>PickSur[2]</code> 为空而前进；只有画面真正识别到槽位 2，才能据此证明中间 Ban 可能为空，并按 <code>BanSur[2] → PickSur[2]</code> 逐步追上。若中间 Ban 已由宿主确认完成，则停在 <code>PickSur[2]</code> 等待其角色出现，不回退。</li><li>原地等待的优先级高于自动回退。同一 Action 的相邻出现即使 Indexes 不同，也不会自动回跳；较早 <code>CommittedEmpty</code> 槽位出现角色时只做同槽补充。只有 Guidance 至少领先槽位目标两个工作流步骤、当前 Action 已与目标 Action 不同，且目标槽位存在安全 <code>Selected</code> 强证据时，才认为引导明显超前。此时逐次调用 <code>PrevStepAsync()</code> 回到最早未满足的前置步骤，再按正常 <code>NextStepAsync()</code> 走到画面目标；仍禁止直接定位。</li></ol><p>回看只服务真正触发的自动落后追赶。<code>OcrBackfillLookBehindSteps</code>、<code>RecognitionTransitionLookBehindMilliseconds</code>、<code>RecognitionTransitionReplayMinimumConfidence</code> 保留在高级参数中，默认分别为 2、800ms、0.95。切换捕获源会清空缓冲，对局 Guid 或进度不匹配的帧不会被读取。手动强制同步始终只使用用户触发时的当前帧，不读取回看结果。</p><h4 id="手动强制同步" tabindex="-1"><a class="header-anchor" href="#手动强制同步"><span>手动强制同步</span></a></h4><p>强制同步由用户显式触发，对当前捕获帧执行一次独立、完整的四区域 OCR，不读写 SmartBP 累计业务状态：</p><ol><li>将本帧明确识别到的四类角色槽位直接通过 <code>ICharacterSelectionService</code> 写入主程序。</li><li>所有角色选择、交换操作都传 <code>playAnimation=false</code>；强制同步不播放入场动画。</li><li>当前或未来的“未选择”保持 <code>Pending</code>；仅目标步骤之前的明确 Empty 可提交为空，避免把尚未选择的后续槽位误判为空操作。</li><li>角色写入后重新读取主程序槽位状态，在当前 phase 对应的同 Action 步骤中寻找最早未完成步骤。例如已选 2 名求生者、已 Ban 3 名求生者、已 Ban 2 名监管者，phase 为 <code>BanSur</code>，目标就是 <code>BanSur[3]</code>。</li><li>Guidance 对齐可直接调用 <code>MoveToStepAsync()</code>，不播放逐步追赶过程。角色同步和 Guidance 对齐分别返回结果；phase 无法解析时，明确角色仍保留在主程序中，Guidance 原位保持。</li></ol><p>这里的“完整四区域 OCR”同时约束请求层与解析层。强制同步固定请求 <code>banned_sur</code>、<code>banned_hun</code>、<code>picked_sur</code>、<code>picked_hun</code>，并使用 <code>GlobalSnapshot</code> 字段解析上下文；当前 phase 和当前 Guidance Action 只参与最后的步骤定位，不得把非当前 Action 的区域过滤掉，也不得改变这些区域的行语义。例如当前画面为 <code>BanSur</code> 时，左下角已经可见的求生者选择仍按角色行/选手 ID 行解析，天赋或附加行只作为噪声忽略，然后与另外三个区域一起对账。只有本帧确实为 <code>Unknown</code> 的槽位才保持主程序原值。</p><p>旧的 <code>SmartBpRecognitionStateStore</code>、启发式进度评分、<code>WorkflowBackfill</code>、<code>TransitionReplay</code>、操作 ledger 和 <code>FreeFullSync</code> 自动模式均已移除，不存在隐藏 fallback。</p><p>在角色分配画面中，视觉槽位永远不直接当作内部 Pick 槽位覆盖 <code>SurPlayerList</code>。进入 <code>DistributeChara</code> 后采用唯一的“空位补充”例外：任一当前帧高置信、安全、名称唯一的角色证据，只要主程序尚未持有该角色且仍有空求生者槽，就依画面顺序填入第一个空槽，不要求四个分配槽全部齐全；之后只有具备安全玩家 ID 匹配的证据才按玩家身份交换到固定内部玩家位。普通 Ban/Pick 不使用这个策略，仍严格按原视觉槽位 Index。重复角色、低置信角色、无空位或玩家 ID 冲突都保持原状态。</p><h3 id="ocr-provider" tabindex="-1"><a class="header-anchor" href="#ocr-provider"><span>OCR Provider</span></a></h3><p>SmartBP 支持三种 OCR Provider，一次只运行所选 Provider，不提供自动 fallback 或跨 Provider ensemble：</p><table><thead><tr><th>Provider</th><th>接口</th><th>适用场景</th><th>依赖</th></tr></thead><tbody><tr><td>PaddleOCR</td><td><code>PaddleOcrProvider : IOcrProvider</code></td><td>默认引擎，BP 状态识别 + 赛后数据回填</td><td>PaddleInference 原生库 + det/cls/rec 模型</td></tr><tr><td>Tesseract</td><td><code>TesseractOcrProvider : IOcrProvider</code></td><td>可选引擎，手动切换</td><td>Tesseract 原生库 + <code>traineddata</code> 语言文件</td></tr><tr><td>RapidOCR</td><td><code>RapidOcrNetProvider : IOcrProvider</code></td><td>可选引擎，手动切换</td><td>RapidOcrNet + 托管中文 det/cls/rec/dict</td></tr></tbody></table><p><code>SmartBpOcrProviderSelector</code> 根据 <code>SmartBpRecognitionSettings.OcrProviderMode</code> 显式选择当前 Provider。Tesseract 的 <code>traineddata</code> 通过 <code>ITesseractDataAssetManager</code> 管理。RapidOCR 资产由 <code>IRapidOcrModelAssetManager</code> 下载到 <code>{SmartBpModuleRoot}\\OCRModels\\RapidOCR\\Models\\{profileId}</code>；只有选中 profile 的 det、cls、rec、dict 均存在且 RapidOcrNet 能初始化时才为 ready，不隐式使用包内拉丁模型。</p><p>RapidOCR manifest 预置中、日、英三个官方组合。检测、分类、识别模型和匹配字典的完整 ModelScope URL 摘自 RapidOCR 官方 <code>python/rapidocr/default_models.yaml</code>，不在代码中推导或拼接；模型使用官方 SHA-256，字典固定校验官方文件内容。下载经 <code>SmartBpParallelDownload</code> 复用统一服务的暂停、续传、重试和取消能力，并使用稳定下载暂存目录和安装完成后的原子提升。RapidOCR 将 <code>Mat</code> 在内存中转换为 <code>SKBitmap</code>，输出统一映射到输入粗区域坐标；可选预处理变体只在同尺寸图像上运行并去重。</p><p>每次安装会在 profile 目录写入 <code>.smartbp-install.json</code>，记录 profile、上游版本和内置 manifest 指纹。普通状态刷新比较安装记录与当前内置 manifest；版本或任一资产 URL、文件名、SHA-256、转换声明变化时提示更新。“检查模型更新”还会通过统一下载器临时读取 RapidOCR 官方 <code>default_models.yaml</code>，按当前识别模型的官方 ONNX URL 比较上游版本。若官方版本已领先内置 manifest，UI 会要求先更新 SmartBP 模块，不会把旧模型误标为可安装更新。旧版安装没有记录时标为“未知（旧版安装）”并提示更新，但现有完整模型仍可继续使用。</p><p>RapidOCR 与其他 OCR Provider 没有依赖关系。SmartBP 自动识别只使用所选 OCR Provider 读取文字与坐标；本地解释器负责当前帧证据，<code>CharacterSelectionService</code> 和主程序槽位提交状态负责业务语义，场景门禁与 Reconciliation 负责安全应用。</p><h3 id="旧-ai-字段兼容" tabindex="-1"><a class="header-anchor" href="#旧-ai-字段兼容"><span>旧 AI 字段兼容</span></a></h3><p>SmartBP 自动识别曾提供 Pure AI、AI+OCR、AI+AI OCR、业务 AI 融合和 AI OCR transcript 方案。这些路径及其对应的 Llama 服务器、Qwen 模型、融合模式、结构化输出、运行时更新检查等设置字段、相关枚举（<code>SmartBpRecognitionEngine</code> / <code>SmartBpRecognitionStrategy</code> / <code>AiStructuredOutputMode</code> / <code>SmartBpHybridFusionMode</code> 等）、守卫方法（<code>IsGameDataAiRecognitionSelected()</code>）和 UI 文本（<code>SmartBpGameDataAiRecognitionNotImplemented</code> 等）已彻底从代码库中删除。开发阶段无需向后兼容，不再保留任何旧配置 JSON 兼容代码。</p><h3 id="识别设置总览" tabindex="-1"><a class="header-anchor" href="#识别设置总览"><span>识别设置总览</span></a></h3><p><code>SmartBpRecognitionSettings</code>（持久化到 <code>%APPDATA%\\neo-bpsys-wpf\\SmartBp\\RecognitionSettings.json</code>）包含以下关键配置组：</p><table><thead><tr><th>配置组</th><th>关键字段</th><th>默认值</th></tr></thead><tbody><tr><td>OCR BP</td><td><code>EnableOcrBpRecognition</code>, <code>UseOcrContactSheet</code>, <code>OcrRecognitionIntervalMs</code></td><td>true / true / 3000ms</td></tr><tr><td>OCR Provider</td><td><code>SelectedOcrProviderMode</code>, <code>OcrProviderMode</code></td><td><code>Paddle</code></td></tr><tr><td>Tesseract</td><td><code>EnableTesseractOcr</code>, <code>TesseractLanguages</code>, <code>TesseractDefaultPsm</code></td><td>true / &quot;chi_sim+eng&quot; / 6</td></tr><tr><td>RapidOCR 模型</td><td><code>SelectedRapidOcrModelId</code></td><td>&quot;ppocr-v5-zh-mobile&quot;</td></tr><tr><td>RapidOCR 推理</td><td><code>RapidOcrPadding</code>, <code>RapidOcrMaxSideLen</code>, <code>RapidOcrBoxScoreThreshold</code>, <code>RapidOcrBoxThreshold</code>, <code>RapidOcrUnclipRatio</code>, <code>RapidOcrUseAngleClassifier</code>, <code>RapidOcrUsePreprocessingVariants</code></td><td>0 / 1024 / 0.5 / 0.3 / 1.6 / true / false</td></tr><tr><td>循环控制</td><td><code>RecognitionIntervalMs</code>, <code>OcrRecognitionIntervalMs</code></td><td>1200ms / 3000ms</td></tr><tr><td>自动应用</td><td><code>EnableAutoApplyRecognition</code>, <code>EnableAutoGuidanceSync</code></td><td>false / false</td></tr><tr><td>稳定确认</td><td><code>RequiredStableSnapshots</code></td><td>1</td></tr><tr><td>图像编码</td><td><code>PhaseCropMaxImageWidth</code>, <code>ContentCropMaxImageWidth</code></td><td>640 / 768</td></tr><tr><td>有界采样与回看</td><td><code>RecognitionFrameBufferMilliseconds</code>, <code>RecognitionSamplingIntervalMilliseconds</code>, <code>OcrBackfillLookBehindSteps</code>, <code>RecognitionTransitionLookBehindMilliseconds</code>, <code>RecognitionTransitionReplayMinimumConfidence</code></td><td>1500ms / 150ms / 2 步 / 800ms / 0.95</td></tr></tbody></table><h2 id="区域配置" tabindex="-1"><a class="header-anchor" href="#区域配置"><span>区域配置</span></a></h2><h3 id="全流程-bp-识别粗区域配置-recognition-layout" tabindex="-1"><a class="header-anchor" href="#全流程-bp-识别粗区域配置-recognition-layout"><span>全流程 BP 识别粗区域配置（Recognition Layout）</span></a></h3><p>用于 BP 状态识别，定义 5 个粗裁剪区域：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>%APPDATA%\\neo-bpsys-wpf\\SmartBp\\BpRecognitionLayoutProfile.json</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><table><thead><tr><th>区域 ID</th><th>枚举值</th><th>画面对应位置</th><th>识别内容</th></tr></thead><tbody><tr><td><code>phase_top</code></td><td><code>PhaseTop</code></td><td>顶部操作栏</td><td>BP 阶段文本（屏蔽/选择/天赋等）</td></tr><tr><td><code>top_left_status</code></td><td><code>TopLeftStatus</code></td><td>画面绝对左上角</td><td>角色 BP 结束后的区域选择和等待开始标题；默认 16:9 区域为 <code>(0, 0, 0.36, 0.11)</code></td></tr><tr><td><code>left_top</code></td><td><code>LeftTop</code></td><td>左上角</td><td>监管者 Ban 位</td></tr><tr><td><code>right_top</code></td><td><code>RightTop</code></td><td>右上角</td><td>求生者 Ban 位</td></tr><tr><td><code>left_bottom</code></td><td><code>LeftBottom</code></td><td>左下角</td><td>求生者 Pick 位</td></tr><tr><td><code>right_bottom</code></td><td><code>RightBottom</code></td><td>右下角</td><td>监管者 Pick 位</td></tr></tbody></table><p>默认配置来自 SmartBP 模块 <code>Resources</code> 中的 <code>BpRecognitionLayoutProfile.json</code>。用户可通过 <code>RegionEditorWindow</code> 在当前捕获帧或内置测试图上可视化调整六个粗区域（包括 <code>TopLeftStatus</code>）。保存后的同一份用户 profile 同时供编辑器预览和本地 OCR 状态检测读取。</p><p>自动循环会先 OCR <code>TopCenterStatus</code> / <code>TopLeftStatus</code>。本地规则以“求生者选择区域中”“监管者选择区域中”“等待游戏开始”三个标题为强锚点，并使用关键词与编辑距离容忍少量 OCR 错字；“剩余…秒”和“前往【…】”只作为辅助证据。命中后设置 post-BP latch，并在任何角色内容区域识别和字段合并前进入排空队列停止流程。</p><h2 id="图像预处理" tabindex="-1"><a class="header-anchor" href="#图像预处理"><span>图像预处理</span></a></h2><p>赛后数据回填先对完整捕获帧执行一次可返回文本边界框的 OCR。解析器以 <code>玩家名（角色名）</code> 文本建立有效行，按 Y 坐标排序；再取角色文本右侧的数字/空值标记，按 X 坐标推断五个统计列、按最近 Y 坐标归属行。名称左侧的等级、天赋数字不会进入数据列，也不会参与玩家或角色解析。</p><p>只有整表解析确认缺失的数据格会进入局部识别。局部图像固定来自当前显式选择的 OCR Provider，不会静默切换引擎；Paddle 使用不经过检测器的单区域字符识别，其他 Provider 使用各自的单文本区域模式。每个变体的原始文本、规范化数字、置信度、投票数和最终接受/拒绝结果都会写入统一识别日志窗口。</p><h2 id="调试点" tabindex="-1"><a class="header-anchor" href="#调试点"><span>调试点</span></a></h2><h3 id="赛后数据-ocr" tabindex="-1"><a class="header-anchor" href="#赛后数据-ocr"><span>赛后数据 OCR</span></a></h3><ol><li>OCR 不工作先看是否已下载并切换模型。</li><li>捕获不到数据先看窗口捕获服务是否处于 capturing。</li><li>查看全流程 BP 调试选项中的赛后调试表格和统一识别日志窗口：其中列出 OCR 原始文本、边界框、行聚类、列归属、被排除的名称列文本，以及 <code>numeric fallback candidate/accepted/rejected</code> 局部识别结果。</li><li>角色匹配失败时看日志中的 <code>SmartBp Match failed</code> 和 OCR 原始名称。</li></ol><h3 id="bp-状态识别" tabindex="-1"><a class="header-anchor" href="#bp-状态识别"><span>BP 状态识别</span></a></h3><ol><li>自动识别不工作先检查 <code>EnableOcrBpRecognition</code>、捕获状态以及自动应用/Guidance 同步开关</li><li>场景门禁阻断写入时查看 <code>SmartBpSceneGateResult</code> 的 <code>Reason</code> 字段</li><li>OCR contact sheet 映射异常时，可设置 <code>UseOcrContactSheet = false</code> 逐区域识别排查</li><li>角色解析失败时查看日志中的 <code>ocr-match</code> 诊断行，包含 <code>raw</code>、<code>result</code>、<code>matchMode</code>、<code>score</code> 等信息</li><li>可开启 <code>ISmartBpDebugLog.IsEnabled</code>，在 SmartBP 页面 UI 中查看当前帧 OCR、历史回看、槽位对账、逐步追赶和强制同步诊断</li><li>追赶问题重点查看 <code>Historical review supplemented</code>、<code>merge_mode=supplement-only</code>、<code>Guided catch-up target</code>、逐步 <code>advanced one step</code> 和 <code>earliest incomplete step</code>；强制同步问题查看 <code>playAnimation=false</code> 与最终目标 Action/Indexes</li></ol><h3 id="通用" tabindex="-1"><a class="header-anchor" href="#通用"><span>通用</span></a></h3><ol><li>模块加载失败时检查 <code>SmartBpModuleState.json</code> 中的 <code>ModuleRoot</code> 路径和 <code>component.json</code> 校验结果</li><li>原生依赖缺失时确认 <code>runtimes/win-x64/native/</code> 目录结构完整</li><li>OCR 推理失败重建可通过日志 <code>OCR run failed, trying to rebuild OCR predictor and retry once.</code> 确认</li></ol><h3 id="常见日志关键词" tabindex="-1"><a class="header-anchor" href="#常见日志关键词"><span>常见日志关键词</span></a></h3><table><thead><tr><th>关键词</th><th>含义</th></tr></thead><tbody><tr><td><code>SmartBp AutoFill skipped: OCR model is not ready.</code></td><td>未选择或未安装 OCR 模型</td></tr><tr><td><code>SmartBp AutoFill skipped: capture is not running.</code></td><td>窗口捕获未启动</td></tr><tr><td><code>SmartBp OCR Survivor</code> / <code>SmartBp OCR Hunter</code></td><td>行识别结果和原始 OCR 文本</td></tr><tr><td><code>SmartBp OCR RowData batch</code></td><td>整行数字条带一次 OCR 的结果</td></tr><tr><td><code>SmartBp OCR RowData fallback</code></td><td>批量识别失败后的逐列 OCR 回退结果</td></tr><tr><td><code>SmartBp Match success</code></td><td>求生者角色匹配成功，含 exact/fuzzy 模式</td></tr><tr><td><code>SmartBp Match failed</code></td><td>识别角色无法映射到当前对局求生者</td></tr><tr><td><code>OCR run failed, trying to rebuild OCR predictor and retry once.</code></td><td>PaddleOCR 推理失败，正在重建后重试</td></tr><tr><td><code>OCR contact sheet: regions=[...]</code></td><td>contact sheet 拼接信息，含区域列表和 unmapped 行数</td></tr><tr><td><code>phase line:</code> / <code>ocr-match</code></td><td>BP 阶段文本行和 OCR 角色匹配诊断</td></tr><tr><td><code>OCR provider selected:</code></td><td>当前活跃的 OCR Provider</td></tr><tr><td><code>Frame sequence N:</code></td><td>自动识别 tick 的帧序号和请求字段</td></tr></tbody></table><h2 id="关键数据模型速查" tabindex="-1"><a class="header-anchor" href="#关键数据模型速查"><span>关键数据模型速查</span></a></h2><h3 id="识别结果模型" tabindex="-1"><a class="header-anchor" href="#识别结果模型"><span>识别结果模型</span></a></h3><table><thead><tr><th>模型</th><th>用途</th></tr></thead><tbody><tr><td><code>SmartBpPhaseRecognitionResult</code></td><td>阶段识别结果（仅 <code>Phase</code> 字段）</td></tr><tr><td><code>SmartBpBusinessStateRecognitionResult</code></td><td>合并后的业务状态（banned_sur/hun + picked_sur/hun）</td></tr><tr><td><code>SmartBpFocusedBusinessExtractionResult</code></td><td>单个粗区域的业务提取结果</td></tr><tr><td><code>SmartBpSnapshotDeltaResult</code></td><td>增量识别 delta（仅含请求字段的更新）</td></tr><tr><td><code>SmartBpSnapshotFieldUpdate</code></td><td>单个字段更新（banned_sur/banned_hun/picked_sur/picked_hun）</td></tr><tr><td><code>SmartBpOcrRecognitionResult</code></td><td>OCR BP 识别完整结果（阶段 + 业务状态 + 区域文本 + 诊断）</td></tr><tr><td><code>SmartBpAutoRecognitionTickResult</code></td><td>一次自动识别 tick 的完整结果</td></tr></tbody></table><h3 id="槽位证据与宿主状态模型" tabindex="-1"><a class="header-anchor" href="#槽位证据与宿主状态模型"><span>槽位证据与宿主状态模型</span></a></h3><table><thead><tr><th>模型</th><th>用途</th></tr></thead><tbody><tr><td><code>SmartBpRecognizedCharacterSlot</code></td><td>单个角色槽位（含识别置信度、自动应用安全标记）</td></tr><tr><td><code>SmartBpRecognizedPlayerCharacterSlot</code></td><td>绑定玩家 ID 的角色槽位（继承 <code>SmartBpRecognizedCharacterSlot</code>）</td></tr><tr><td><code>SmartBpNormalizedCharacter</code></td><td>规范化角色解析结果（原始名 → 标准名 → 匹配模式）</td></tr><tr><td><code>BpSlotCommitStateSnapshot</code></td><td>主程序权威槽位状态，区分 Pending / CommittedEmpty / CommittedCharacter</td></tr></tbody></table><h3 id="操作与对账模型" tabindex="-1"><a class="header-anchor" href="#操作与对账模型"><span>操作与对账模型</span></a></h3><table><thead><tr><th>模型</th><th>用途</th></tr></thead><tbody><tr><td><code>SmartBpDetectedOperation</code></td><td>检测到的候选操作（Ban/Pick/Swap + 关联的 GameGuidance 步骤）</td></tr><tr><td><code>SmartBpGuidanceSyncResult</code></td><td>引导同步结果（是否变更、是否接受、目标步骤）</td></tr><tr><td><code>SmartBpReconciliationResult</code></td><td>角色、明确空操作与 Guidance 三部分独立结果</td></tr></tbody></table><h2 id="文件路径速查" tabindex="-1"><a class="header-anchor" href="#文件路径速查"><span>文件路径速查</span></a></h2><table><thead><tr><th>路径</th><th>内容</th></tr></thead><tbody><tr><td><code>%APPDATA%\\neo-bpsys-wpf\\SmartBpModuleState.json</code></td><td>模块安装状态（ModuleRoot 路径）</td></tr><tr><td><code>%APPDATA%\\neo-bpsys-wpf\\SmartBp\\BpRecognitionLayoutProfile.json</code></td><td>BP 识别粗区域配置</td></tr><tr><td><code>%APPDATA%\\neo-bpsys-wpf\\SmartBp\\RecognitionSettings.json</code></td><td>识别引擎设置</td></tr><tr><td><code>{SmartBpModuleRoot}\\OCRModels\\{modelKey}\\</code></td><td>PaddleOCR 模型文件（det/cls/rec）</td></tr><tr><td><code>{SmartBpModuleRoot}\\tessdata\\</code></td><td>Tesseract traineddata 语言文件</td></tr><tr><td><code>{SmartBpModuleRoot}\\Resources\\</code></td><td>内置默认配置、prompt、测试帧等资源</td></tr></tbody></table>`,112))])}const B=i(p,[["render",h]]),b=JSON.parse('{"path":"/dev/business/smartbp-and-ocr.html","title":"SmartBP 与 OCR","lang":"zh-CN","frontmatter":{"description":"SmartBP 与 OCR 线程和后台任务注意事项见 。SmartBP 默认配置和资源文件见 。 架构概览 SmartBP 是面向第五人格赛事的直播导播辅助系统中的智能 BP 识别与赛后数据回填子系统。它采用模块分离架构：主应用只保留 SmartBP 页面壳、安装覆盖层、模块加载器、状态模型和功能命令抽象；真正的 SmartBP UI、OCR 引擎、O...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"SmartBP 与 OCR\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/business/smartbp-and-ocr.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"SmartBP 与 OCR"}],["meta",{"property":"og:description","content":"SmartBP 与 OCR 线程和后台任务注意事项见 。SmartBP 默认配置和资源文件见 。 架构概览 SmartBP 是面向第五人格赛事的直播导播辅助系统中的智能 BP 识别与赛后数据回填子系统。它采用模块分离架构：主应用只保留 SmartBP 页面壳、安装覆盖层、模块加载器、状态模型和功能命令抽象；真正的 SmartBP UI、OCR 引擎、O..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":27.38,"words":8215},"filePathRelative":"dev/business/smartbp-and-ocr.md","autoDesc":true}');export{B as comp,b as data};
