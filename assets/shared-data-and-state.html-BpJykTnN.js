import{_ as r}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as s,b as t,d as a,e as d,a as c,w as n,r as i,o as l}from"./app-CGOhbY2V.js";const p={};function h(m,e){const o=i("RouteLink");return l(),s("div",null,[e[10]||(e[10]=t(`<h1 id="共享数据与状态流" tabindex="-1"><a class="header-anchor" href="#共享数据与状态流"><span>共享数据与状态流</span></a></h1><h2 id="为什么-shareddataservice-是中心" tabindex="-1"><a class="header-anchor" href="#为什么-shareddataservice-是中心"><span>为什么 SharedDataService 是中心</span></a></h2><p><code>SharedDataService</code> 是导播后台、前台窗口、引导式 BP、比分、SmartBP 等功能的共享状态中心。它通过 <code>ISharedDataService</code> 暴露当前对局、主客队、角色字典、Ban 位可用状态、倒计时、BO3/BO5 状态和地图 V2 状态。</p><p>前台窗口 ViewModel 不应复制一份比赛状态，而应读取 <code>ISharedDataService</code>：</p><div class="language-csharp line-numbers-mode" data-highlighter="shiki" data-ext="csharp" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-csharp"><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> Game</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;"> CurrentGame</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> =&gt; </span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">_sharedDataService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">CurrentGame</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;"> string</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;"> RemainingSeconds</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> =&gt; </span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">_sharedDataService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">RemainingSeconds</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span>
<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">public</span><span style="--shiki-light:#383A42;--shiki-dark:#E06C75;"> ObservableCollection</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">&lt;</span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">bool</span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">&gt;</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;"> CanCurrentSurBanned</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> =&gt; </span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">_sharedDataService</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.</span><span style="--shiki-light:#383A42;--shiki-dark:#E5C07B;">CanCurrentSurBannedList</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>典型例子是旧 XAML 前台窗口 ViewModel 和当前 v3 renderer 绑定链路：它们读取 <code>ISharedDataService</code> 的当前状态，并在 <code>CurrentGameChanged</code>、<code>IsBo3ModeChanged</code>、<code>CountDownValueChanged</code> 等事件后刷新绑定或重新渲染需要切换状态的布局。</p><h2 id="稳定对象与可替换对象" tabindex="-1"><a class="header-anchor" href="#稳定对象与可替换对象"><span>稳定对象与可替换对象</span></a></h2><p><code>HomeTeam</code> 和 <code>AwayTeam</code> 在接口注释中明确是“全场始终不变”的对象。导入队伍信息时使用 <code>Team.ImportTeamInfo(Team)</code> 更新对象内部内容，而不是替换 <code>HomeTeam</code> / <code>AwayTeam</code> 引用。</p><p><code>Team.ColorHex</code> 是队伍展示色的权威字段，序列化格式统一为 <code>#AARRGGBB</code>。输入 <code>#RRGGBB</code> 时自动补全不透明 Alpha；主队和客队默认值分别为 <code>#FF337FB9</code>、<code>#FFE34341</code>。<code>ImportTeamInfo</code> 必须同步复制此字段，避免导入队伍或对局后丢失颜色。Designer v3 的颜色绑定可直接使用 <code>HomeTeam.ColorHex</code> 和 <code>AwayTeam.ColorHex</code>。</p><p><code>CurrentGame</code> 则可能被替换：</p><table><thead><tr><th>操作</th><th>行为</th></tr></thead><tbody><tr><td><code>NewGame()</code></td><td>根据当前主客队阵营创建新的 <code>Game</code>，保留地图和对局进度</td></tr><tr><td><code>ImportGameAsync(filePath)</code></td><td>导入对局 JSON，更新主客队信息，再构造新的 <code>Game</code></td></tr><tr><td><code>Game.Swap()</code></td><td>当前对局内部交换求生/监管队伍，并触发换边事件</td></tr></tbody></table><p><code>SharedDataService</code> 在替换 <code>CurrentGame</code> 时会先取消旧对局事件订阅，再订阅新对局事件，并触发 <code>CurrentGameChanged</code>。页面如果缓存了旧 <code>Game</code> 引用，可能会读到过期状态。</p><h2 id="game-team-player-数据关系" tabindex="-1"><a class="header-anchor" href="#game-team-player-数据关系"><span>Game / Team / Player 数据关系</span></a></h2><table><thead><tr><th>模型</th><th>职责</th></tr></thead><tbody><tr><td><code>Game</code></td><td>当前局信息：队伍、进度、地图、当前 Ban、上场选手、赛后数据</td></tr><tr><td><code>Team</code></td><td>主队/客队数据：名称、Logo、队员、比分、全局 Ban 记录</td></tr><tr><td><code>Player</code></td><td>当前上场选手：成员、角色、天赋、辅助特质、赛后数据</td></tr><tr><td><code>PlayerData</code></td><td>赛后数据字段，空值显示为 <code>-</code></td></tr></tbody></table><p><code>Game</code> 构造时会基于 <code>SurTeam.SurMemberOnFieldCollection</code> 和 <code>HunTeam.HunMemberOnField</code> 创建 <code>Player</code>，并在队员上场变化时重新装填成员引用。</p><h2 id="ban-状态" tabindex="-1"><a class="header-anchor" href="#ban-状态"><span>Ban 状态</span></a></h2><p><code>SharedDataService</code> 维护四个“是否可用”的 Ban 位列表：</p><table><thead><tr><th>列表</th><th>长度来源</th></tr></thead><tbody><tr><td><code>CanCurrentSurBannedList</code></td><td><code>AppConstants.CurrentBanSurCount</code></td></tr><tr><td><code>CanCurrentHunBannedList</code></td><td><code>AppConstants.CurrentBanHunCount</code></td></tr><tr><td><code>CanGlobalSurBannedList</code></td><td><code>AppConstants.GlobalBanSurCount</code></td></tr><tr><td><code>CanGlobalHunBannedList</code></td><td><code>AppConstants.GlobalBanHunCount</code></td></tr></tbody></table><p><code>SetBanCount</code> 通过把前 N 个位置设为 <code>true</code> 来控制页面可用 Ban 位。列表项变化会触发 <code>BanCountChanged</code>。引导式 BP 启动时会根据 <code>GameRule.json</code> 调整这些列表。</p><p>当前局禁用角色存储在 <code>CurrentGame.CurrentSurBannedList</code> 和 <code>CurrentGame.CurrentHunBannedList</code>。</p><p>全局 Ban 有两层数据：</p><table><thead><tr><th>列表</th><th>语义</th></tr></thead><tbody><tr><td><code>Team.GlobalBannedSurList</code> / <code>GlobalBannedHunList</code></td><td>当前已经生效的全局禁选，是前台显示和选择器禁用判断的权威源</td></tr><tr><td><code>Team.GlobalBannedSurRecordList</code> / <code>GlobalBannedHunRecordList</code></td><td>本轮该阵营 Pick 的暂存记录，供下一次队伍再次轮到该阵营时写入生效列表</td></tr></tbody></table><p>角色选择服务自动记录 Pick 时只写入 <code>RecordList</code>，不会立即改变当前生效的全局禁选。队伍再次轮到对应阵营、新建对局或启动引导时，<code>UpdateGlobalBanFromRecord()</code> 才会把暂存记录中的非空角色覆盖到当前生效列表。</p><h3 id="角色禁用状态联动" tabindex="-1"><a class="header-anchor" href="#角色禁用状态联动"><span>角色禁用状态联动</span></a></h3><p><code>CharaSelectViewModelBase</code> 提供了 <code>DisabledKeys</code> 属性（<code>ISet&lt;string&gt;</code>），实时计算当前对局中同阵营已被 Ban 或已被 Pick 的角色名集合，用于在 <code>CharacterSelector</code> 下拉列表中灰显这些选项。</p><table><thead><tr><th>场景</th><th>被禁用的角色</th></tr></thead><tbody><tr><td>求生者 Ban / Pick 位</td><td>已启用的 <code>CurrentSurBannedList</code> + <code>CurrentGame.SurTeam.GlobalBannedSurList</code> + <code>SurPlayerList</code> 中已 Pick 的角色</td></tr><tr><td>监管者 Ban / Pick 位</td><td>已启用的 <code>CurrentHunBannedList</code> + <code>CurrentGame.HunTeam.GlobalBannedHunList</code> + <code>HunPlayer.Character</code>（若非空）</td></tr></tbody></table><p>数据流：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>CurrentGame.BannedList / Player.Character 变化</span></span>
<span class="line"><span>  → CollectionChanged / PropertyChanged 事件（Game 级别）</span></span>
<span class="line"><span>    ─────────────────────────────────────────────</span></span>
<span class="line"><span>SharedDataService.HomeTeam/AwayTeam.GlobalBannedSur(Hun)List 变化</span></span>
<span class="line"><span>  → CollectionChanged 事件（当前生效列表为权威源）</span></span>
<span class="line"><span>    ─────────────────────────────────────────────</span></span>
<span class="line"><span>    → CharaSelectViewModelBase.UpdateDisabledKeys()</span></span>
<span class="line"><span>      → DisabledKeys 属性变更</span></span>
<span class="line"><span>        → CharacterSelector.DisabledKeys DP</span></span>
<span class="line"><span>          → ComboBox.ItemContainerStyle (MultiBinding)</span></span>
<span class="line"><span>            → ComboBoxItem.IsEnabled = false</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>UpdateDisabledKeys</code> 只读取当前阵营队伍的 <strong>生效列表（GlobalBanned*List）</strong>，不会读取或订阅 <code>RecordList</code>。因此本轮 Pick 写入暂存记录后不会立即干扰 Ban/Pick；等队伍下一次轮到相同阵营、暂存记录覆盖到生效列表后，角色才会被禁用。读取时额外校验 <code>team.Camp</code> 与当前阵营一致，并在 <code>TeamSwapped</code> 完成态再次刷新，避免 <code>Game.Swap()</code> 先换 Camp、后换引用的中间态留下错误结果。</p><h3 id="换边复现步骤实际验证什么" tabindex="-1"><a class="header-anchor" href="#换边复现步骤实际验证什么"><span>换边复现步骤实际验证什么</span></a></h3><p>原始的五步复现不是要求换边时删除队伍自身保存的全局禁选，而是在验证：<strong>选择器禁用状态必须跟随当前阵营上的队伍重新计算，不能残留上一支队伍的结果。</strong></p><p>假设主队 A 在求生者阵营 Pick 了“医生”和“律师”，角色选择服务把它们写入 A 的 <code>GlobalBannedSurRecordList</code>：</p><ol><li><strong>写入暂存记录</strong>：A 的求生者 <code>RecordList</code> 包含医生、律师，但它们尚未因此进入当前生效全局禁选。当前局是否可选仍由当前 Ban、当前 Pick 和生效列表决定。</li><li><strong>第一次换边，A：Sur → Hun</strong>：客队 B 成为当前 <code>SurTeam</code>。求生者选择器必须只读取 B 的 <code>GlobalBannedSurList</code>，不能因为 A 的暂存记录而禁用医生、律师。</li><li><strong>打开 Ban 求生者页面</strong>：用于确认页面初始化时也从当前 <code>SurTeam</code> 的生效列表派生状态，而不是保留旧缓存。</li><li><strong>第二次换边，A：Hun → Sur</strong>：A 再次成为当前 <code>SurTeam</code>。A 的暂存记录已经覆盖到 A 的 <code>GlobalBannedSurList</code>，医生、律师此时应当不能再 Ban，也不能再 Pick。</li><li><strong>第三次换边，A：Sur → Hun</strong>：B 再次成为当前 <code>SurTeam</code>。A 自己的 <code>GlobalBannedSurList</code> 中仍可保留医生、律师，但它们不属于当前求生者队伍，因此必须立即从求生者 <code>DisabledKeys</code> 中消失并恢复可选。</li></ol><p>这五步同时验证三件事：</p><ul><li><code>RecordList</code> 只是下一次同阵营对局的输入，不会直接影响当前选择器。</li><li><code>GlobalBanned*List</code> 跟随队伍保存，不因队伍暂时换到另一阵营而删除。</li><li><code>DisabledKeys</code> 跟随 <code>CurrentGame.SurTeam</code> / <code>HunTeam</code>，换边后不能残留上一支队伍的禁用角色。</li></ul><h3 id="前后状态示例" tabindex="-1"><a class="header-anchor" href="#前后状态示例"><span>前后状态示例</span></a></h3><p>仍以求生者选择器为例，且所有相关 Ban 位均已启用：</p><table><thead><tr><th>数据</th><th>状态一：A 当前为求生者，记录尚未生效</th><th>状态二：A 再次成为求生者，记录已经生效</th></tr></thead><tbody><tr><td>A 的 <code>GlobalBannedSurRecordList</code></td><td>医生、律师</td><td>医生、律师</td></tr><tr><td>A 的 <code>GlobalBannedSurList</code></td><td>空</td><td>医生、律师</td></tr><tr><td>当前局 Ban</td><td>祭司</td><td>祭司</td></tr><tr><td>当前局 Pick</td><td>园丁</td><td>园丁</td></tr><tr><td>不能 Ban / Pick</td><td>祭司、园丁</td><td>医生、律师、祭司、园丁</td></tr><tr><td>仍能 Ban / Pick</td><td>医生、律师及其他未使用角色</td><td>其他未使用角色</td></tr></tbody></table><p>如果状态二之后再换边，让 B 成为当前求生者队伍，则 A 的医生、律师仍保存在 A 的生效列表里，但不会出现在当前求生者 <code>DisabledKeys</code> 中。若某个 Ban 位关闭，即使该位置仍保存角色，也不应加入 <code>DisabledKeys</code>。</p><p>由此得到统一计算规则：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>当前阵营 DisabledKeys</span></span>
<span class="line"><span>  = 当前阵营已启用的当局 Ban</span></span>
<span class="line"><span>  + 当前阵营队伍已启用的生效全局 Ban</span></span>
<span class="line"><span>  + 当前阵营本局已 Pick 的角色</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>RecordList</code>、另一支队伍的生效全局 Ban、已关闭 Ban 位中的角色，都不参与当前阵营的 <code>DisabledKeys</code>。</p><p>导入对局（<code>ImportGameAsync</code>）恢复的是当前生效状态。<code>ImportTeamInfo</code> 在稳定的生效列表对象内更新内容，保证已有 ViewModel 的集合订阅继续有效；导入后调用 <code>ClearGlobalBanRecords()</code> 清空导入前的暂存记录，避免创建新 <code>Game</code> 时旧记录覆盖导入的生效列表。</p><p>设计原则：角色禁用状态是<strong>派生数据</strong>，不存储在 <code>Character</code> 模型上，避免在 NewGame / ImportGame 等操作时需要额外清理。</p><blockquote><p><strong>例外</strong>：PickPage 中用于查看和手动调整暂存记录的 CharacterSelector（<code>HomeSur/HunGlobalBanRecordViewModel</code>、<code>AwaySur/HunGlobalBanRecordViewModel</code>）不绑定 <code>DisabledKeys</code>，否则暂存记录自身会阻止修正。</p></blockquote><h2 id="倒计时" tabindex="-1"><a class="header-anchor" href="#倒计时"><span>倒计时</span></a></h2><p>倒计时使用 WPF <code>DispatcherTimer</code>，因此 Tick 发生在 UI 线程。状态字段是 <code>_remainingSeconds</code>：</p><table><thead><tr><th>方法/属性</th><th>行为</th></tr></thead><tbody><tr><td><code>TimerStart(int? seconds)</code></td><td><code>null</code> 时不启动；否则设置剩余秒数、总秒数并启动计时器</td></tr><tr><td><code>TimerStop()</code></td><td>设置剩余秒数为 <code>-1</code>、总秒数为 <code>0</code>，停止计时器</td></tr><tr><td><code>RemainingSeconds</code></td><td>小于 0 显示 <code>VS</code>，否则显示数字</td></tr><tr><td><code>CountDownRemainingSeconds</code></td><td>供数值计算使用的剩余秒数；未运行时为 <code>0</code>，不返回 <code>VS</code></td></tr><tr><td><code>CountDownTotalSeconds</code></td><td>当前轮倒计时启动时设置的总秒数；未运行时为 <code>0</code></td></tr><tr><td><code>CountDownValueChanged</code></td><td>每次变化后触发</td></tr></tbody></table><p>后台页面和前台窗口不要自己创建独立倒计时，否则会和全局状态不同步。</p><h2 id="bo3-bo5-与比分" tabindex="-1"><a class="header-anchor" href="#bo3-bo5-与比分"><span>BO3/BO5 与比分</span></a></h2><p><code>IsBo3Mode</code> 在 <code>SharedDataService</code> 中集中保存。变更时会：</p><ol><li>发送 <code>PropertyChangedMessage&lt;bool&gt;</code>。</li><li>触发 <code>IsBo3ModeChanged</code>。</li></ol><p><code>ScorePageViewModel</code> 不再维护后台比分页专用的 BO3/BO5 Game/half 选择列表；比分控制跟随全局 <code>CurrentGame.GameProgress</code>。Designer v3 layout 可启用通用 BO3/BO5 Canvas states：root/default state 表示 BO5，<code>BoModeStates[&quot;Bo3&quot;]</code> 表示 BO3。前台 v3 窗口在 <code>IsBo3ModeChanged</code> 后重载 layout，renderer 按 <code>ISharedDataService.IsBo3Mode</code> 选择对应 state；缺少 BO3 state 时回退 root/BO5 并记录 warning。<code>ScoreGlobalWindow</code> 只是该通用机制的一个使用者，背景、总分位置和控件配置不再通过 ScoreGlobal 专用 background variant 切换。<code>FrontedWindowService</code> 不再直接隐藏全局比分控件或移动 Total 控件。</p><p><code>GlobalScoreTotalMargin</code> 仍在共享服务中暴露并保留设置项，但 v3 <code>ScoreGlobalWindow</code> 不依赖它；BO5/BO3 总分位置分别来自 root state 和 <code>BoModeStates[&quot;Bo3&quot;]</code>。</p>`,54)),a("p",null,[e[1]||(e[1]=d("Score System v2 的设计方向见 ",-1)),c(o,{to:"/dev/business/score-system-v2.html"},{default:n(()=>[...e[0]||(e[0]=[d("score-system-v2.md",-1)])]),_:1}),e[2]||(e[2]=t("。后，权威比分状态由现有 <code>Core.Models.Game.MatchScore</code> 持有，类型为 <code>MatchScoreState</code>；<code>IMatchScoreService</code> 只操作 <code>ISharedDataService.CurrentGame.MatchScore</code>，页面 ViewModel、前台窗口 ViewModel、<code>FrontedWindowService</code> 和 UI 控件都不能成为比分数据库。后台 <code>ScorePageViewModel</code> 的比分按钮已改为写入 <code>IMatchScoreService.CurrentHalf</code>，普通 UI 不再提供手动 Game/half 选择、手动 <code>Team.Score</code> 累加或“同步至前台”按钮；清除按钮位于旧“小比分清零”位置，会把当前半场结果设为 <code>null</code>。后台 <code>ScorePage</code> 的导播比分预览表只读显示 <code>CurrentGame.MatchScore.Games</code> 派生行，跟随全局 <code>CurrentGame.GameProgress</code> 和 BO3/BO5 状态，不提供手动场次选择；<code>ScoreSurWindow</code>、<code>ScoreHunWindow</code> 和 <code>ScoreGlobalWindow</code> 默认 v3 布局均读取 <code>CurrentGame.MatchScore</code>。绑定浏览器除主客队总小比分外，还暴露 <code>CurrentSurTeamTotalMinorScore</code> / <code>CurrentHunTeamTotalMinorScore</code>，按当前阵营映射提供全场总小比分并随换边刷新。运行时不再把 <code>MatchScoreState</code> 派生值同步回 <code>Team.Score</code>；<code>Team.Score</code> 仅作为旧 JSON/旧 DTO 的反序列化兼容数据存在，不能作为权威写入点。",43))]),e[11]||(e[11]=t('<h2 id="smartbp-的权威状态与视觉证据" tabindex="-1"><a class="header-anchor" href="#smartbp-的权威状态与视觉证据"><span>SmartBP 的权威状态与视觉证据</span></a></h2><p>SmartBP 不持有第二份长期 Ban/Pick 业务状态。当前角色与空槽位是否已经提交，由 <code>CurrentGame</code> 的角色数据和 <code>BpSlotCommitState</code> 共同表达；<code>BpSlotCommitState</code> 严格绑定 <code>Game.Guid + GameProgress</code>，每个 Ban/Pick 槽位区分 <code>Pending</code>、<code>CommittedEmpty</code>、<code>CommittedCharacter</code>。普通后台点击和 SmartBP 都只能通过 <code>ICharacterSelectionService</code> 修改这些状态。旧对局 JSON 没有该字段时，新建全部为 <code>Pending</code> 的状态；这不会把旧数据里的普通 <code>null</code> 猜成已经提交的空操作。</p><p>OCR 输出首先形成当前帧证据：阶段、字段、固定视觉槽位、Unknown/Empty/Selected、候选角色、玩家 ID、置信度、安全标记、原因和边界框。协调器每次新建业务结果，不跨帧保存长期完整 Ban/Pick 快照。滚动帧缓冲只保存绑定 GameGuid/GameProgress 的原始捕获帧，不是业务状态源；读取时拒绝其他对局上下文。自动链路先以可值比较的 <code>Action + 规范化 Indexes</code> 工作流位置和宿主 Pending 槽位洞做廉价触发判断；位置对齐且没有新槽位证据时不运行 Reconciliation，也不调度历史 OCR。真正落后时，回看服务默认只取目标之前两个工作流步骤，每步最多识别一张代表帧，且历史 Phase/Action 必须与被补步骤一致；结果只把安全角色补入本次临时证据。宿主只有 <code>Pending</code> 或 <code>CommittedEmpty</code> 才可补入，当前帧已有明确角色、历史 Empty、Action 不一致或宿主已经是 <code>CommittedCharacter</code> 时均不补入。</p><p>自动识别和手动强制同步统一进入 Reconciliation，但语义严格分开。自动追赶根据当前 phase、工作流 Action/Indexes、当前帧已选槽位、一次性回看补充证据与宿主提交状态确定目标，然后从 Guidance 当前步骤开始逐步应用、复读宿主状态并调用 <code>NextStepAsync()</code>；任何未完成的 Ban/Pick 都会阻断后续前进，自动路径不调用 <code>MoveToStepAsync()</code>。宿主中当前 Pick 槽已经提交不能单独证明同 Action 的下一组 Pick 已开始；只有下一组固定槽位出现安全角色，或两组之间的其他 Action 已经完成，才会提高目标。中间 Ban 仍为 <code>Pending</code> 时先停在前一 Pick 组；下一 Pick 槽真正出现后，才允许把其前面的明确空 Ban 作为前置步骤提交并逐步追上。若较早的 <code>CommittedEmpty</code> 槽位后来出现安全角色，只通过正常角色服务升级同一槽位并播放动画，不因此移动 Guidance。原地等待优先于回退；只有 Guidance 至少领先目标两个工作流步骤、当前 Action 与目标 Action 不同且目标槽位有安全 <code>Selected</code> 强证据时，才逐次 <code>PrevStepAsync()</code> 回到最早未满足前置步骤，再逐步前进。普通 Ban/Pick 严格使用视觉槽位 Index，明确空 Ban 只在步骤已经越过，或同一步骤更后槽位已明确选中从而证明前槽为空洞时提交；不会用后续角色向前压缩，也不会覆盖已提交角色。<code>DistributeChara</code> 是唯一例外：安全且唯一的缺失角色可以补入第一个宿主空位，再按玩家 ID 交换。回看补充角色与当前帧角色走相同的候选校验和 <code>ICharacterSelectionService</code>，不是直接写集合；Guidance 已经超过目标时不再调度历史回看。自动选择角色传 <code>playAnimation=true</code>。手动强制同步对当前帧固定 OCR 四类角色区域，并使用不依赖当前 phase/Guidance Action 的全局字段解析契约；phase 只负责随后定位工作流步骤，不能过滤非当前 Action 的角色区域。四类明确角色先以 <code>playAnimation=false</code> 直接写入主程序，再依据写入后的宿主槽位寻找当前 phase 下最早未完成步骤，可直接调用 <code>MoveToStepAsync()</code>；手动路径不读取历史帧。角色写回、明确空操作和 Guidance 移动分别返回成功或失败，Guidance 无法对齐不能撤销已安全写回的角色。</p><p>角色业务身份统一使用当前语言角色表中的规范 <code>Character.Name</code>。<code>Character.ImageFileName</code> 只用于 <code>ImageHelper</code> 加载头像和立绘，不得进入文本解析结果、SmartBP 操作、角色选择日志或前台行为筛选 payload；旧行为 payload 的 <code>OldCharacterId</code> / <code>NewCharacterId</code> 仅作为兼容字段保留，其值同样是规范名称，新配置应使用 <code>OldCharacterName</code> / <code>NewCharacterName</code>。</p><p><code>SharedDataService.NewGame()</code> 创建新 <code>Game</code> 时会 clone 当前 <code>CurrentGame.MatchScore</code>，避免新旧对局共享同一个可变比分实例。导入旧 JSON 时如果没有 <code>MatchScore</code> 字段，<code>Game</code> 会创建默认 <code>MatchScoreState</code>，以兼容旧保存记录；如果旧记录只有 <code>Team.Score</code> 值，当前实现不会尝试反推出完整 per-Game/per-Half 历史，只保留反序列化得到的旧字段并记录 warning。导入新格式时，任何有效 <code>MatchScore</code> 都不会被旧字段覆盖。</p><h2 id="事件模式" tabindex="-1"><a class="header-anchor" href="#事件模式"><span>事件模式</span></a></h2><p><code>ISharedDataService</code> 暴露的事件包括：</p><table><thead><tr><th>事件</th><th>常见用途</th></tr></thead><tbody><tr><td><code>CurrentGameChanged</code></td><td>页面/前台窗口刷新 <code>CurrentGame</code></td></tr><tr><td><code>GameProgressChanged</code></td><td>当前 <code>CurrentGame.GameProgress</code> 改变或导入/替换为不同进度的对局后刷新进度相关 UI</td></tr><tr><td><code>BanCountChanged</code></td><td>Ban 页或前台控件刷新可用位</td></tr><tr><td><code>CountDownValueChanged</code></td><td>倒计时显示刷新</td></tr><tr><td><code>TeamSwapped</code></td><td>队伍换边后刷新阵营相关 UI</td></tr><tr><td><code>IsBo3ModeChanged</code></td><td>切换 BO3/BO5 布局与比分</td></tr><tr><td><code>PickedMapChanged</code></td><td>地图显示刷新</td></tr><tr><td><code>MapV2BannedChanged</code></td><td>地图 V2 禁用状态刷新</td></tr><tr><td><code>IsMapV2BreathingChanged</code> / <code>IsMapV2CampVisibleChanged</code></td><td>地图 V2 前台表现刷新</td></tr><tr><td><code>MapV2PickingBorderStateChanged</code></td><td>按地图发布 MapBpV2 选图边框状态，payload 包含 <code>MapKey</code>、<code>IsMapV2Breathing</code>、<code>IsMapBanned</code>、<code>IsPickingBorderVisible</code>；<code>IsPickingBorderVisible</code> 保持旧语义 <code>IsMapV2Breathing &amp;&amp; !IsMapBanned</code>，由 v3 行为负责具体动画。</td></tr></tbody></table><p>因为页面和 ViewModel 多为 singleton，订阅事件时要避免重复订阅，也要注意长期订阅导致对象被持有。构造函数中订阅 singleton 服务事件通常可接受；动态创建对象订阅时需要考虑解绑。</p><h2 id="维护坑点" tabindex="-1"><a class="header-anchor" href="#维护坑点"><span>维护坑点</span></a></h2>',11)),a("ol",null,[e[7]||(e[7]=t("<li>不要在页面 ViewModel 中复制 <code>CurrentGame</code> 的深层状态作为第二数据源。</li><li>替换 <code>CurrentGame</code> 后，旧对象引用不会再收到共享服务事件。</li><li>导入队伍应通过 <code>ImportTeamInfo</code>，不要替换 <code>HomeTeam</code> / <code>AwayTeam</code>。</li><li>全局 Ban 有“记录列表”和“显示列表”两层，修改时要确认目标是哪一层。</li>",4)),a("li",null,[e[4]||(e[4]=a("code",null,"ObservableCollection",-1)),e[5]||(e[5]=d(" 应在 UI 线程更新；后台回调更新集合前先看 ",-1)),c(o,{to:"/dev/architecture/threading-dispatcher-and-async.html"},{default:n(()=>[...e[3]||(e[3]=[d("threading-dispatcher-and-async.md",-1)])]),_:1}),e[6]||(e[6]=d("。",-1))]),e[8]||(e[8]=a("li",null,[d("SmartBP 写回赛后数据时直接修改 "),a("code",null,"CurrentGame"),d(" 中的 "),a("code",null,"PlayerData"),d("；BP 识别写回统一经过 "),a("code",null,"ICharacterSelectionService"),d("，不要维护 OCR 专用业务数据副本。")],-1)),e[9]||(e[9]=a("li",null,[d("导入对局（"),a("code",null,"ImportGameAsync"),d("）后必须清空旧的 "),a("code",null,"RecordList"),d(" 暂存记录，不能把导入的生效列表反写到记录列表。")],-1))])])}const S=r(p,[["render",h]]),g=JSON.parse('{"path":"/dev/architecture/shared-data-and-state.html","title":"共享数据与状态流","lang":"zh-CN","frontmatter":{"description":"共享数据与状态流 为什么 SharedDataService 是中心 SharedDataService 是导播后台、前台窗口、引导式 BP、比分、SmartBP 等功能的共享状态中心。它通过 ISharedDataService 暴露当前对局、主客队、角色字典、Ban 位可用状态、倒计时、BO3/BO5 状态和地图 V2 状态。 前台窗口 ViewM...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"共享数据与状态流\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/architecture/shared-data-and-state.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"共享数据与状态流"}],["meta",{"property":"og:description","content":"共享数据与状态流 为什么 SharedDataService 是中心 SharedDataService 是导播后台、前台窗口、引导式 BP、比分、SmartBP 等功能的共享状态中心。它通过 ISharedDataService 暴露当前对局、主客队、角色字典、Ban 位可用状态、倒计时、BO3/BO5 状态和地图 V2 状态。 前台窗口 ViewM..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":14.28,"words":4285},"filePathRelative":"dev/architecture/shared-data-and-state.md","autoDesc":true}');export{S as comp,g as data};
