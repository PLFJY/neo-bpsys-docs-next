import{_ as d}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as o,b as t,o as a}from"./app-CGOhbY2V.js";const c={};function r(s,e){return a(),o("div",null,[...e[0]||(e[0]=[t(`<h1 id="score-system-v2-设计文档" tabindex="-1"><a class="header-anchor" href="#score-system-v2-设计文档"><span>Score System v2 设计文档</span></a></h1><p>本文定义 Score System v2 的目标模型、计算规则、前台绑定方向和迁移计划。</p><p>当前实现状态：旧比分写入路径清理已完成。现有 <code>Core.Models.Game</code> 已持有 <code>MatchScoreState</code>，后台 <code>ScorePageViewModel</code> 的结果按钮通过 <code>IMatchScoreService.SetCurrentHalfResult(...)</code> / <code>ClearCurrentHalfResult()</code> 写入 <code>CurrentGame.MatchScore</code>，普通 UI 不再提供手动 Game/half 选择、手动 <code>Team.Score</code> 累加或&quot;同步至前台&quot;按钮；比分控制始终跟随全局 <code>CurrentGame.GameProgress</code>。后台 <code>ScorePage</code> 现在提供导播用只读比分预览表，表格行由 <code>CurrentGame.MatchScore.Games</code> 派生，按 BO3/BO5 显示对应 ScoreGame 和半场，并用状态列标记当前半场、已录入和未录入。<code>ScoreSurWindow</code> / <code>ScoreHunWindow</code> / <code>ScoreGlobalWindow</code> / <code>CutSceneWindow</code> / <code>GameDataWindow</code> / <code>BpOverviewWindow</code> / <code>BpWindow</code> 的 v3 layout 已绑定 <code>CurrentGame.MatchScore</code> 派生字段，不再从 <code>Team.Score</code>、<code>ScoreWindowViewModel</code> 总分字段或 <code>FrontedWindowService</code> 动态控件读取比分。<code>GameGlobalInfoRecord</code>、<code>ScoreWindowViewModel.TotalMainGameScore</code> / <code>TotalAwayGameScore</code> 和 <code>FrontedWindowService.SetGlobalScore*</code> / <code>ResetGlobalScore</code> 已移除。<code>.bpui</code> 已迁移到 Window-centric v3 package。</p><p>Score System v2 的核心目标是把权威比分状态放回现有 <code>Core.Models.Game</code>，让比分可以随对局导入、导出、回溯，并能在 <code>SharedDataService.NewGame()</code> 创建新对局时像 <code>MapV2Dictionary</code> 一样从上一局 <code>CurrentGame</code> 延续必要状态。</p><p>本文中的“小比分（MinorScore）”指每半场、每个比分系统 Game 或全场累计的分值。首次说明后，本文会使用“小比分”或 <code>MinorScore</code>。未来代码模型命名应优先使用 <code>MinorScore</code>，避免使用含糊的英文泛称。</p><h2 id="_1-当前问题" tabindex="-1"><a class="header-anchor" href="#_1-当前问题"><span>1. 当前问题</span></a></h2><p>当前比分相关状态分散在模型、页面 ViewModel、窗口 ViewModel 和前台窗口服务中，导致 v3 前台布局难以纯绑定渲染。</p><table><thead><tr><th>问题</th><th>当前位置</th><th>影响</th></tr></thead><tbody><tr><td><code>Team.Score</code> 同时承载大比分和当前小比分</td><td><code>Team.Score.Win</code>、<code>Tie</code>、<code>GameScores</code></td><td>队伍模型既像全场比分，又像当前半场/当前局内临时计分；语义混杂。</td></tr><tr><td>全局比分记录曾由页面 ViewModel 持有</td><td><code>ScorePageViewModel.GameGlobalInfoRecord</code></td><td>已移除；后台页面不再维护第二份半场完成状态。</td></tr><tr><td>总小比分曾通过 messenger 推送</td><td><code>ScorePageViewModel.UpdateTotalGameScore()</code> -&gt; <code>ScoreWindowViewModel.TotalMainGameScore</code> / <code>TotalAwayGameScore</code></td><td>已移除；<code>ScoreGlobalWindow</code> 默认布局直接绑定 <code>MatchScoreState.HomeTotalMinorScore</code> / <code>AwayTotalMinorScore</code>。</td></tr><tr><td>全局比分 UI 曾由服务动态创建和直接修改</td><td><code>FrontedWindowService.GlobalScoreControlsReg()</code>、<code>SetGlobalScore()</code>、<code>SetGlobalScoreToBar()</code></td><td>当前全局比分窗口由 v3 <code>GlobalScoreRow.Cells</code> 配置比分格，再从 <code>CurrentGame.MatchScore</code> 填充文本和阵营图标；<code>SetGlobalScore*</code> / <code>ResetGlobalScore</code> 已从代码库中完全移除。</td></tr><tr><td>局内比分窗口已接入 v3 renderer 并完成绑定迁移</td><td><code>ScoreSurWindow/BaseCanvas.json</code>、<code>ScoreHunWindow/BaseCanvas.json</code></td><td>当前默认布局绑定 <code>CurrentGame.MatchScore.*</code> 派生字段；用户旧布局如果仍保留 <code>CurrentGame.*Team.Score.*</code>，需要后续布局迁移或手动恢复默认。</td></tr></tbody></table><h2 id="_2-核心设计决策" tabindex="-1"><a class="header-anchor" href="#_2-核心设计决策"><span>2. 核心设计决策</span></a></h2><p>权威比分状态必须由现有 <code>Core.Models.Game</code> 持有。</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>ISharedDataService</span></span>
<span class="line"><span>  └─ CurrentGame</span></span>
<span class="line"><span>       ├─ SurTeam / HunTeam</span></span>
<span class="line"><span>       ├─ GameProgress</span></span>
<span class="line"><span>       ├─ MapV2Dictionary</span></span>
<span class="line"><span>       └─ MatchScoreState   &lt;-- Score System v2 权威比分状态</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这里需要区分两个“Game”：</p><table><thead><tr><th>名称</th><th>含义</th></tr></thead><tbody><tr><td>现有 <code>Core.Models.Game</code></td><td>应用当前对局记录对象，保存队伍、BP 状态、地图、Ban、选手数据，并最终保存 <code>MatchScoreState</code>。</td></tr><tr><td>Score System v2 的 Game</td><td>比分系统领域术语，指 Game 1、Game 2、Game 3 Overtime 这样的计分单元，每个 Game 包含 First Half 和 Second Half。Identity V 赛事语境中常用 “Game {x} First Half / Second Half”。</td></tr></tbody></table><p>文档中的 Game 是比分系统领域术语；实现时为避免与现有 <code>Core.Models.Game</code> 冲突，建议使用 <code>ScoreGame</code> 作为类型名。</p><p>不应把权威比分状态放在：</p><table><thead><tr><th>不应存放处</th><th>原因</th></tr></thead><tbody><tr><td><code>Team</code></td><td>队伍信息应可跨对局复用；比分记录属于某场比赛/某个对局进程，不是队伍静态属性。</td></tr><tr><td><code>ScorePageViewModel</code></td><td>页面 ViewModel 是 UI 控制层，不能作为比分数据库。</td></tr><tr><td><code>ScoreWindowViewModel</code></td><td>前台窗口 ViewModel 应暴露绑定状态，不应独占计算结果。</td></tr><tr><td><code>FrontedWindowService</code></td><td>服务可以操作当前比分，但不应通过控件属性保存比分。</td></tr><tr><td>前台 UI 控件</td><td>控件只能显示状态，不能成为状态来源。</td></tr></tbody></table><p><code>Team.Score</code> 暂时不能从模型层立即删除，因为旧 JSON 和旧 DTO 仍可能包含 <code>Team.Score.Win</code>、<code>Team.Score.Tie</code>、<code>Team.Score.GameScores</code>。它不再是运行时兼容镜像，<code>MatchScoreState</code> 不会把派生比分写回 <code>Team.Score</code>。新模型不以它作为权威写入点，后台 <code>ScorePage</code> 也不提供手动 <code>Team.Score</code> 累加入口；<code>CutSceneWindow</code> 和 <code>GameDataWindow</code> v3 默认布局已改为读取 <code>CurrentGame.MatchScore.CurrentSurTeamMajorText</code> / <code>CurrentGame.MatchScore.CurrentHunTeamMajorText</code>。</p><h2 id="_3-新模型概念" tabindex="-1"><a class="header-anchor" href="#_3-新模型概念"><span>3. 新模型概念</span></a></h2><h3 id="_3-1-matchscorestate" tabindex="-1"><a class="header-anchor" href="#_3-1-matchscorestate"><span>3.1 MatchScoreState</span></a></h3><p><code>MatchScoreState</code> 由 <code>Game</code> 持有，表示整场比赛的比分状态。</p><table><thead><tr><th>责任</th><th>说明</th></tr></thead><tbody><tr><td>权威存储</td><td>保存所有已记录半场结果、队伍/阵营映射和可序列化比分数据。</td></tr><tr><td>可序列化</td><td>作为 <code>Game</code> 的一部分导入导出，支持赛后回溯。</td></tr><tr><td>可延续</td><td>新建 <code>Game</code> 时可从上一局 <code>CurrentGame.MatchScore</code> clone 或 carry。</td></tr><tr><td>派生显示字段</td><td>暴露前台和后台可绑定字段，避免 UI 层重复计算。</td></tr><tr><td>重算入口</td><td>在半场结果、队伍换边、BO 模式或进度变化后统一重算派生值。</td></tr></tbody></table><p>已实现结构：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>MatchScoreState</span></span>
<span class="line"><span>  ├─ Games: collection&lt;ScoreGame&gt;</span></span>
<span class="line"><span>  ├─ HomeMajorWin</span></span>
<span class="line"><span>  ├─ HomeMajorTie</span></span>
<span class="line"><span>  ├─ AwayMajorWin</span></span>
<span class="line"><span>  ├─ AwayMajorTie</span></span>
<span class="line"><span>  ├─ HomeMajorText</span></span>
<span class="line"><span>  ├─ AwayMajorText</span></span>
<span class="line"><span>  ├─ HomeTotalMinorScore</span></span>
<span class="line"><span>  ├─ AwayTotalMinorScore</span></span>
<span class="line"><span>  ├─ CurrentSurTeamTotalMinorScore</span></span>
<span class="line"><span>  ├─ CurrentHunTeamTotalMinorScore</span></span>
<span class="line"><span>  ├─ CurrentSurTeamMinorScoreText</span></span>
<span class="line"><span>  ├─ CurrentHunTeamMinorScoreText</span></span>
<span class="line"><span>  ├─ CurrentSurTeamMajorText</span></span>
<span class="line"><span>  └─ CurrentHunTeamMajorText</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>派生字段使用重算后写入的 observable 属性，并以 <code>[JsonIgnore]</code> 排除在持久化之外。<code>Games</code> 内的半场结果、记录时主客队映射和 <code>ScoreGameKey</code> 是持久化数据。</p><h3 id="_3-2-scoregame" tabindex="-1"><a class="header-anchor" href="#_3-2-scoregame"><span>3.2 ScoreGame</span></a></h3><p><code>ScoreGame</code> 表示比分系统中的一个 Game，例如 Game 1 Normal、Game 3 Overtime。每个 <code>ScoreGame</code> 由一上一下两个半场组成。</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>GameNumber</code></td><td>Game 编号，范围 <code>1..5</code>。</td></tr><tr><td><code>GameKind</code></td><td><code>Normal</code> 或 <code>Overtime</code>，实现类型为 <code>ScoreGameKind</code>。</td></tr><tr><td><code>FirstHalf</code></td><td>第一半。</td></tr><tr><td><code>SecondHalf</code></td><td>第二半。</td></tr><tr><td><code>HomeMinorScore</code> / <code>AwayMinorScore</code></td><td>两半都有结果时计算出的 Game-level MinorScore。</td></tr><tr><td><code>MajorResult</code></td><td>两半都有结果时计算出的 Game major result，实现类型为 <code>ScoreGameMajorResult</code>。</td></tr></tbody></table><p>这个 <code>ScoreGame</code> 只有在 <code>FirstHalf.Result != null</code> 且 <code>SecondHalf.Result != null</code> 时才完整。未完整时，不参与大比分计算。</p><h3 id="_3-3-scorehalf" tabindex="-1"><a class="header-anchor" href="#_3-3-scorehalf"><span>3.3 ScoreHalf</span></a></h3><p><code>ScoreHalf</code> 表示某个 <code>ScoreGame</code> 的一半。</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>GameProgress</code></td><td>对应当前半场进度。</td></tr><tr><td><code>Result</code></td><td>可空 <code>GameResult</code>；<code>null</code> 表示未记录或已清空。</td></tr><tr><td><code>SurTeamTypeWhenRecorded</code></td><td>记录时求生者侧对应主队还是客队，计算主客得分时使用。</td></tr><tr><td><code>HunTeamTypeWhenRecorded</code></td><td>记录时监管者侧对应主队还是客队，计算主客得分时使用。</td></tr><tr><td><code>SurMinorScore</code> / <code>HunMinorScore</code></td><td>从 <code>GameResult</code> 派生出的求生者/监管者小比分。</td></tr><tr><td><code>HomeMinorScore</code> / <code>AwayMinorScore</code></td><td>根据记录时求生/监管侧与主客队映射派生出的主客小比分。</td></tr></tbody></table><p>半场必须保存“记录时”的队伍/阵营映射，不能只在显示时读取当前 <code>SurTeam</code> / <code>HunTeam</code>。原因是比分记录需要可回溯；后续换边或导入队伍信息不应改变过去半场的历史得分归属。</p><h3 id="_3-4-scoregamekey" tabindex="-1"><a class="header-anchor" href="#_3-4-scoregamekey"><span>3.4 ScoreGameKey</span></a></h3><p><code>ScoreGameKey</code> 用于稳定定位一个 <code>ScoreGame</code>。</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>GameNumber</code></td><td><code>1..5</code>。</td></tr><tr><td><code>GameKind</code></td><td><code>Normal</code> / <code>Overtime</code>，实现类型为 <code>ScoreGameKind</code>。</td></tr></tbody></table><p>示例：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>ScoreGame 3 Normal   -&gt; { GameNumber: 3, GameKind: Normal }</span></span>
<span class="line"><span>ScoreGame 3 Overtime -&gt; { GameNumber: 3, GameKind: Overtime }</span></span>
<span class="line"><span>ScoreGame 5 Overtime -&gt; { GameNumber: 5, GameKind: Overtime }</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_3-4-1-globalscorerow-cell-布局" tabindex="-1"><a class="header-anchor" href="#_3-4-1-globalscorerow-cell-布局"><span>3.4.1 GlobalScoreRow cell 布局</span></a></h3><p><code>ScoreGlobalWindow</code> 的 v3 <code>GlobalScoreRow</code> 不再根据 <code>MajorGameGap</code> / <code>HalfGameGap</code> 自动展开比分格。布局 JSON 中的父级 <code>GlobalScoreRowControlConfig</code> 保存队伍方向和默认样式，<code>Cells</code> 中的每个 <code>GlobalScoreCellConfig</code> 通过 <code>GameNumber</code>、<code>GameKind</code>、<code>HalfKind</code> 指向一个 <code>ScoreHalf</code>，并用相对父行的 <code>X/Y/Width/Height</code> 定位。运行时只用这些 key 从 <code>CurrentGame.MatchScore.Games</code> 解析显示值；缺失半场或 <code>Result == null</code> 时显示 <code>-</code>。BO3 与 BO5 的差异由通用 Canvas state 保存，因此 BO3 state 可以有独立的 cell 列表和相对坐标。</p><h3 id="_3-5-imatchscoreservice-matchscoreservice" tabindex="-1"><a class="header-anchor" href="#_3-5-imatchscoreservice-matchscoreservice"><span>3.5 IMatchScoreService / MatchScoreService</span></a></h3><p><code>IMatchScoreService</code> 是操作层，所有命令都作用于 <code>ISharedDataService.CurrentGame.MatchScore</code>。服务不拥有权威状态。</p><table><thead><tr><th>命令</th><th>行为</th></tr></thead><tbody><tr><td><code>CurrentHalf</code> / <code>GetHalf(...)</code></td><td>根据 <code>CurrentGame.GameProgress</code> 定位当前 <code>ScoreHalf</code>。</td></tr><tr><td><code>SetCurrentHalfResult(GameResult result)</code></td><td>写入当前半场结果，并记录当时主客队阵营映射。</td></tr><tr><td><code>ClearCurrentHalfResult()</code></td><td>把当前半场结果设为 <code>null</code>。</td></tr><tr><td><code>Recalculate()</code></td><td>从当前 BO 模式可见的 <code>ScoreGame</code> 重新计算大比分、总小比分和前台派生字段。内部调用 <code>MatchScoreState.Recalculate(bool isBo3Mode)</code>，统计范围由 BO3/BO5 可见性决定，不依赖当前进度。</td></tr><tr><td><code>RefreshCurrentProgress()</code></td><td>结合当前 <code>GameProgress</code>、当前求生/监管队伍和 BO3/BO5 状态刷新局内显示字段。</td></tr></tbody></table><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>ScorePageViewModel</span></span>
<span class="line"><span>  └─ IMatchScoreService.SetCurrentHalfResult(...)</span></span>
<span class="line"><span>       └─ ISharedDataService.CurrentGame.MatchScore</span></span>
<span class="line"><span>            └─ Recalculate()</span></span>
<span class="line"><span>                 ├─ 通知后台页面刷新</span></span>
<span class="line"><span>                 ├─ 通知 ScoreSurWindow / ScoreHunWindow 绑定刷新</span></span>
<span class="line"><span>                 └─ 通知 ScoreGlobalWindow 绑定刷新</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_4-gameprogress-映射" tabindex="-1"><a class="header-anchor" href="#_4-gameprogress-映射"><span>4. GameProgress 映射</span></a></h2><p><code>GameProgress.Free</code> 不对应任何确定半场，Score System v2 暂不解析它；这是已知设计缺口。</p><table><thead><tr><th><code>GameProgress</code></th><th>Score System v2 mapping</th></tr></thead><tbody><tr><td><code>Game1FirstHalf</code> / <code>Game1SecondHalf</code></td><td>ScoreGame 1 Normal</td></tr><tr><td><code>Game2FirstHalf</code> / <code>Game2SecondHalf</code></td><td>ScoreGame 2 Normal</td></tr><tr><td><code>Game3FirstHalf</code> / <code>Game3SecondHalf</code></td><td>ScoreGame 3 Normal</td></tr><tr><td><code>Game3OvertimeFirstHalf</code> / <code>Game3OvertimeSecondHalf</code></td><td>ScoreGame 3 Overtime</td></tr><tr><td><code>Game4FirstHalf</code> / <code>Game4SecondHalf</code></td><td>ScoreGame 4 Normal</td></tr><tr><td><code>Game5FirstHalf</code> / <code>Game5SecondHalf</code></td><td>ScoreGame 5 Normal</td></tr><tr><td><code>Game5OvertimeFirstHalf</code> / <code>Game5OvertimeSecondHalf</code></td><td>ScoreGame 5 Overtime</td></tr><tr><td><code>Free</code></td><td>Unresolved / not designed yet</td></tr></tbody></table><p>映射必须显式维护。当前 <code>GameProgress</code> enum 中 <code>Game4FirstHalf</code> 与 <code>Game3OvertimeFirstHalf</code> 共用数值 <code>6</code>，<code>Game4SecondHalf</code> 与 <code>Game3OvertimeSecondHalf</code> 共用数值 <code>7</code>。实现中 <code>MatchScoreState.GetGame(progress)</code> 在缺少上下文时保守按 BO5 第四局解析，<code>MatchScoreService</code> 会结合 <code>ISharedDataService.IsBo3Mode</code> 调用带上下文的解析来区分“Game 3 Overtime”和“Game 4 Normal”。</p><h2 id="_5-计分规则" tabindex="-1"><a class="header-anchor" href="#_5-计分规则"><span>5. 计分规则</span></a></h2><p>以下计分规则基于当前项目实现，不声明为官方赛事规则。</p><table><thead><tr><th><code>GameResult</code></th><th style="text-align:right;">求生者小比分</th><th style="text-align:right;">监管者小比分</th></tr></thead><tbody><tr><td><code>Escape4</code></td><td style="text-align:right;">5</td><td style="text-align:right;">0</td></tr><tr><td><code>Escape3</code></td><td style="text-align:right;">3</td><td style="text-align:right;">1</td></tr><tr><td><code>Tie</code></td><td style="text-align:right;">2</td><td style="text-align:right;">2</td></tr><tr><td><code>Out3</code></td><td style="text-align:right;">1</td><td style="text-align:right;">3</td></tr><tr><td><code>Out4</code></td><td style="text-align:right;">0</td><td style="text-align:right;">5</td></tr></tbody></table><h3 id="_5-1-null-结果行为" tabindex="-1"><a class="header-anchor" href="#_5-1-null-结果行为"><span>5.1 Null 结果行为</span></a></h3><p><code>ScoreHalf.Result == null</code> 表示未记录或已清空。</p><table><thead><tr><th>场景</th><th>行为</th></tr></thead><tbody><tr><td>小比分合计</td><td>不参与小比分合计。</td></tr><tr><td>大比分计算</td><td>不参与大比分计算。</td></tr><tr><td>全局比分格</td><td>显示 <code>-</code>。</td></tr><tr><td>全局比分格阵营图标</td><td>隐藏。</td></tr><tr><td>局内小比分</td><td>当前半场所在 Game 的所有已记录半场（含当前半场）小比分累计；当前半场无结果时仅累计之前已记录的半场；当前半场为第一半且未记录时显示 <code>0</code>。</td></tr></tbody></table><p>空结果不等价于平局，也不等价于 0:0 已记录结果。</p><h3 id="_5-2-半场派生" tabindex="-1"><a class="header-anchor" href="#_5-2-半场派生"><span>5.2 半场派生</span></a></h3><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>ScoreHalf.Result</span></span>
<span class="line"><span>  -&gt; SurMinorScore / HunMinorScore</span></span>
<span class="line"><span>  -&gt; 根据 SurTeamTypeWhenRecorded / HunTeamTypeWhenRecorded</span></span>
<span class="line"><span>  -&gt; HomeMinorScore / AwayMinorScore</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如果记录时主队是求生者：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>HomeMinorScore = SurMinorScore</span></span>
<span class="line"><span>AwayMinorScore = HunMinorScore</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>如果记录时主队是监管者：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>HomeMinorScore = HunMinorScore</span></span>
<span class="line"><span>AwayMinorScore = SurMinorScore</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_5-3-scoregame-派生" tabindex="-1"><a class="header-anchor" href="#_5-3-scoregame-派生"><span>5.3 ScoreGame 派生</span></a></h3><p><code>ScoreGame</code> 只有两半都非空时才计算 Home/Away MinorScore 和 Game major result：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>HomeMinorScore = FirstHalf.HomeMinorScore + SecondHalf.HomeMinorScore</span></span>
<span class="line"><span>AwayMinorScore = FirstHalf.AwayMinorScore + SecondHalf.AwayMinorScore</span></span>
<span class="line"><span></span></span>
<span class="line"><span>if HomeMinorScore &gt; AwayMinorScore:</span></span>
<span class="line"><span>    majorResult = HomeWin</span></span>
<span class="line"><span>else if AwayMinorScore &gt; HomeMinorScore:</span></span>
<span class="line"><span>    majorResult = AwayWin</span></span>
<span class="line"><span>else:</span></span>
<span class="line"><span>    majorResult = Tie</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如果任一半为 <code>null</code>，该 <code>ScoreGame</code> 不完整：</p><table><thead><tr><th>派生项</th><th>行为</th></tr></thead><tbody><tr><td><code>HomeMinorScore</code> / <code>AwayMinorScore</code></td><td>不作为完整 ScoreGame 的小比分输出。</td></tr><tr><td>大比分胜/平</td><td>不计算。</td></tr><tr><td>全场总小比分</td><td>只累加已记录半场的小比分；空半场不累加。</td></tr></tbody></table><h4 id="大比分统计范围" tabindex="-1"><a class="header-anchor" href="#大比分统计范围"><span>大比分统计范围</span></a></h4><p><code>MatchScoreState.Recalculate(bool isBo3Mode)</code> 统计当前 BO 模式下可见的 <code>ScoreGame</code>，不依赖当前 <code>GameProgress</code>，也不使用 <code>Games</code> 的索引位置或 <code>GameNumber</code> 当作遍历边界。</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>foreach game in Games where ScoreGameVisibility.IsVisibleInBoMode(game.Key, isBo3Mode):</span></span>
<span class="line"><span>    count MajorResult from game</span></span>
<span class="line"><span>    count recorded half minor scores from game</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>BO3 可见范围是 Game 1、Game 2、Game 3、Game 3 Overtime。BO5 可见范围是 Game 1、Game 2、Game 3、Game 4、Game 5、Game 5 Overtime。即使当前进度是 <code>GameProgress.Free</code>，重算也会按传入 BO 模式重新派生总分；没有可见已记录结果时，总分会归零。</p><h3 id="_5-4-全场派生" tabindex="-1"><a class="header-anchor" href="#_5-4-全场派生"><span>5.4 全场派生</span></a></h3><p><code>MatchScoreState</code> 从所有 <code>ScoreHalf</code> / <code>ScoreGame</code> 派生以下字段：</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>HomeMajorWin</code> / <code>HomeMajorTie</code></td><td>主队大比分胜/平。</td></tr><tr><td><code>AwayMajorWin</code> / <code>AwayMajorTie</code></td><td>客队大比分胜/平。</td></tr><tr><td><code>HomeMajorText</code> / <code>AwayMajorText</code></td><td>前台大比分文本，建议保持当前 <code>W{Win} D{Tie}</code> 风格。</td></tr><tr><td><code>HomeTotalMinorScore</code> / <code>AwayTotalMinorScore</code></td><td>所有已记录半场的主客小比分合计。</td></tr><tr><td><code>CurrentSurTeamTotalMinorScore</code> / <code>CurrentHunTeamTotalMinorScore</code></td><td>将全场主客总小比分按当前求生者/监管者阵营映射后的合计；换边时随当前队伍映射刷新。</td></tr><tr><td><code>CurrentSurTeamMinorScoreText</code></td><td>当前求生者队伍在当前半场窗口中应显示的累计小比分文本（同 Game 内从第一半到当前半场已记录小比分之和）。</td></tr><tr><td><code>CurrentHunTeamMinorScoreText</code></td><td>当前监管者队伍在当前半场窗口中应显示的累计小比分文本（同 Game 内从第一半到当前半场已记录小比分之和）。</td></tr><tr><td><code>CurrentSurTeamMajorText</code></td><td>当前求生者队伍对应的大比分文本。</td></tr><tr><td><code>CurrentHunTeamMajorText</code></td><td>当前监管者队伍对应的大比分文本。</td></tr></tbody></table><h2 id="_6-scoresurwindow-scorehunwindow-行为" tabindex="-1"><a class="header-anchor" href="#_6-scoresurwindow-scorehunwindow-行为"><span>6. ScoreSurWindow / ScoreHunWindow 行为</span></a></h2><p><code>ScoreSurWindow</code> 和 <code>ScoreHunWindow</code> 已迁移为 v3 renderer pilot。内置默认 layout 已从旧的 <code>Team.Score</code> 字段切换到 <code>CurrentGame.MatchScore</code> 派生字段：</p><table><thead><tr><th>窗口</th><th>旧绑定</th><th>当前默认绑定</th></tr></thead><tbody><tr><td><code>ScoreSurWindow</code></td><td><code>CurrentGame.SurTeam.Score.MajorPointsOnFront</code>、<code>CurrentGame.SurTeam.Score.GameScores</code></td><td><code>CurrentGame.MatchScore.CurrentSurTeamMajorText</code>、<code>CurrentGame.MatchScore.CurrentSurTeamMinorScoreText</code></td></tr><tr><td><code>ScoreHunWindow</code></td><td><code>CurrentGame.HunTeam.Score.MajorPointsOnFront</code>、<code>CurrentGame.HunTeam.Score.GameScores</code></td><td><code>CurrentGame.MatchScore.CurrentHunTeamMajorText</code>、<code>CurrentGame.MatchScore.CurrentHunTeamMinorScoreText</code></td></tr></tbody></table><p>局内比分窗口显示规则：</p><table><thead><tr><th>当前半场</th><th>显示小比分</th></tr></thead><tbody><tr><td>当前半场为第一半</td><td>显示第一半已记录的 MinorScore（按当前阵营映射）；未记录显示 <code>0</code>。</td></tr><tr><td>当前半场为第二半</td><td>显示同 Game 内第一半 + 第二半已记录 MinorScore 之和（按当前阵营映射）；第二半未记录时只累计第一半。</td></tr><tr><td><code>Free</code> 进度 / 无对应 Game</td><td>显示 <code>0</code>。</td></tr></tbody></table><p>累计小比分按当前阵营映射。每个 <code>ScoreHalf</code> 的 <code>HomeMinorScore</code> / <code>AwayMinorScore</code> 由记录时阵营派生，多半场累加以 Home/Away 为稳定身份，再按当前阵营映射到求生者/监管者窗口；记录后发生换边时，历史得分归属仍正确（与全局比分格的阵营映射方式一致）。</p><p>例：第一半录入 <code>Escape4</code>（5:0），切到第二半未录入时显示 <code>5:0</code>；第二半录入 <code>Tie</code>（2:2）后显示 <code>7:2</code>。</p><h2 id="_7-后台-scorepage-行为" tabindex="-1"><a class="header-anchor" href="#_7-后台-scorepage-行为"><span>7. 后台 ScorePage 行为</span></a></h2><p>后台 ScorePage 的编辑对象由 <code>CurrentGame.GameProgress</code> 决定。页面按钮只表达“给当前半场写入一个结果”，不再直接累加 <code>Team.Score.GameScores</code>。</p><table><thead><tr><th>按钮</th><th>新行为</th></tr></thead><tbody><tr><td><code>Escape4</code></td><td><code>SetCurrentHalfResult(GameResult.Escape4)</code></td></tr><tr><td><code>Escape3</code></td><td><code>SetCurrentHalfResult(GameResult.Escape3)</code></td></tr><tr><td><code>Tie</code></td><td><code>SetCurrentHalfResult(GameResult.Tie)</code></td></tr><tr><td><code>Out3</code></td><td><code>SetCurrentHalfResult(GameResult.Out3)</code></td></tr><tr><td><code>Out4</code></td><td><code>SetCurrentHalfResult(GameResult.Out4)</code></td></tr><tr><td><code>Clear</code></td><td><code>ClearCurrentHalfResult()</code>，即把当前半场结果设为 <code>null</code>。</td></tr></tbody></table><p>新模型不需要 <code>IsGameFinished</code> 作为核心概念。是否完成由 <code>GameResult != null</code> 表达。迁移期 UI 可以保留兼容字段或旧交互，但它们应映射到 nullable result，而不是成为新的模型字段。</p><p>当前后台实现不再暴露普通 UI 的 <code>IsGameFinished</code>、手动 Game/half 选择、手动 <code>Team.Score</code> 累加或“同步至前台”流程。清除当前半场比分按钮位于旧“小比分清零”按钮位置，会把全局 <code>CurrentGame.GameProgress</code> 对应半场的 <code>ScoreHalf.Result</code> 设为 <code>null</code>；后台预览表和 <code>ScoreGlobalWindow</code> 都由 <code>CurrentGame.MatchScore</code> 绑定自动刷新。</p><p>后台 <code>ScorePage</code> 的预览表是导播检查用只读 UI。其 <code>ScorePreviewRow</code> 集合从 <code>CurrentGame.MatchScore.Games</code> 重建，BO3 显示 Game 1、Game 2、Game 3 和 Game 3 Overtime，BO5 显示 Game 1 到 Game 5 以及 Game 5 Overtime。表格使用 <code>ScoreGameKey</code> / 现有显式可见性规则区分 Game 3 Overtime 与 Game 4，不把 <code>GameProgress</code> 原始数值作为唯一判断依据。行内结果、阵营和主客小比分均来自对应 <code>ScoreHalf</code> 的已记录结果及记录时主客队映射；空结果显示 <code>-</code>。它不提供行点击切换、手动 Game/half 选择或编辑能力，也不替代 Score System v2 的权威状态。</p><h2 id="_8-scoreglobalwindow-行为" tabindex="-1"><a class="header-anchor" href="#_8-scoreglobalwindow-行为"><span>8. ScoreGlobalWindow 行为</span></a></h2><p><code>ScoreGlobalWindow</code> 已迁移到 v3 renderer，默认布局位于 <code>Resources/FrontedLayouts/ScoreGlobalWindow.json</code>。它绑定现有 <code>Core.Models.Game</code> 持有的 <code>MatchScoreState</code>，不依赖 <code>ScoreWindowViewModel</code> 独有字段或 <code>FrontedWindowService</code> 直接变更控件。</p><p>目标状态流：</p><div class="language-text line-numbers-mode" data-highlighter="shiki" data-ext="text" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-text"><span class="line"><span>ScorePage button</span></span>
<span class="line"><span>  -&gt; IMatchScoreService</span></span>
<span class="line"><span>  -&gt; CurrentGame.MatchScore</span></span>
<span class="line"><span>  -&gt; ScoreGlobalWindow binding</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>全局比分格规则：</p><table><thead><tr><th>状态</th><th>文本</th><th>阵营图标</th></tr></thead><tbody><tr><td><code>GameResult == null</code></td><td><code>-</code></td><td>隐藏</td></tr><tr><td>有结果且该队当半为求生者</td><td>对应小比分</td><td>显示求生者图标</td></tr><tr><td>有结果且该队当半为监管者</td><td>对应小比分</td><td>显示监管者图标</td></tr></tbody></table><p>全局比分格表示 <code>ScoreGame</code> 内部的 <code>ScoreHalf</code> 结果，由内置 v3 控件 <code>GlobalScoreRow</code> 的 <code>Cells</code> 显式配置。总分显示从 <code>MatchScoreState</code> 派生，不再从 <code>ScoreWindowViewModel</code> 独有字段或 <code>FrontedWindowService</code> UI mutation 派生。每个 cell 用 <code>ScoreGameKey</code> 和 <code>ScoreHalfKind</code> 定位比分，避免依赖 <code>GameProgress</code> 原始数值。阵营图标颜色由 <code>CampIconColor</code> 控制，支持黑/白两种填充色；运行时基于原始阵营图标资源的 alpha 直接填充颜色，不需要维护额外黑色图标资源。<code>ScoreGlobalWindow/BaseCanvas</code> 使用 Designer v3 通用 Canvas BO states：BO5 是 root/default state，BO3 是 <code>BoModeStates[&quot;Bo3&quot;]</code>，背景、总分位置、父行框和 cell 列表都由对应 state 决定；窗口订阅 <code>IsBo3ModeChanged</code> 后会重新应用 v3 布局，让 BO3/BO5 state 即时刷新。</p><h2 id="_9-导入、导出与新建对局" tabindex="-1"><a class="header-anchor" href="#_9-导入、导出与新建对局"><span>9. 导入、导出与新建对局</span></a></h2><p><code>MatchScoreState</code> 是现有 <code>Core.Models.Game</code> 的一部分，因此：</p><table><thead><tr><th>场景</th><th>行为</th></tr></thead><tbody><tr><td>导出对局</td><td>序列化 <code>Game.MatchScore</code>。</td></tr><tr><td>导入对局</td><td>从 JSON 恢复 <code>Game.MatchScore</code>，并在队伍信息导入后保持历史半场的阵营映射；有效 <code>MatchScore</code> 不会被旧 <code>Team.Score</code> 字段覆盖。</td></tr><tr><td>新建对局</td><td>从旧 <code>CurrentGame.MatchScore</code> clone/carry 到新 <code>Game</code>。</td></tr><tr><td>回溯对局</td><td>不依赖页面 ViewModel 是否还存在，不依赖前台窗口是否打开。</td></tr></tbody></table><p>新建对局的 carry 行为应和当前地图状态类似：<code>SharedDataService.NewGame()</code> 先读取旧 <code>CurrentGame</code> 的可延续状态，再构造新的 <code>Core.Models.Game</code>。实现时需要明确哪些比分字段可延续；建议延续整场 <code>MatchScoreState</code>，并由当前 <code>GameProgress</code> 决定后续编辑位置。</p><h2 id="_10-兼容策略" tabindex="-1"><a class="header-anchor" href="#_10-兼容策略"><span>10. 兼容策略</span></a></h2><table><thead><tr><th>旧能力</th><th>迁移期策略</th></tr></thead><tbody><tr><td><code>Team.Score.GameScores</code></td><td>仅作为旧 JSON/旧 DTO 反序列化兼容字段保留；运行时不再从 <code>MatchScoreState</code> 派生写回。后台 <code>ScorePageViewModel</code> 和默认 v3 布局不把它作为权威写入点。</td></tr><tr><td><code>Team.Score.MajorPointsOnFront</code></td><td>仅随旧 <code>Team.Score.Win</code> / <code>Tie</code> 自身格式化；局内比分 v3 默认布局读取 <code>MatchScoreState</code> 派生字段。</td></tr><tr><td><code>ScorePageViewModel.GameGlobalInfoRecord</code></td><td>已移除。</td></tr><tr><td><code>ScoreWindowViewModel.TotalMainGameScore</code> / <code>TotalAwayGameScore</code></td><td>已移除；总小比分直接绑定 <code>CurrentGame.MatchScore.HomeTotalMinorScore</code> / <code>AwayTotalMinorScore</code>。</td></tr><tr><td><code>FrontedWindowService.SetGlobalScore*</code> / <code>ResetGlobalScore</code></td><td>已完全移除；全局比分窗口状态由 <code>CurrentGame.MatchScore</code> 驱动。</td></tr></tbody></table><h2 id="_11-已实现功能总览" tabindex="-1"><a class="header-anchor" href="#_11-已实现功能总览"><span>11. 已实现功能总览</span></a></h2><p>Score System v2 的所有环节（Score 当前实现）已全部完成。核心进展：</p><table><thead><tr><th>层次</th><th>实现内容</th></tr></thead><tbody><tr><td>基础模型</td><td><code>MatchScoreState</code>、<code>ScoreGame</code>、<code>ScoreHalf</code>、<code>ScoreGameKey</code> 已实现，权威比分状态由 <code>Core.Models.Game</code> 持有。</td></tr><tr><td>服务层</td><td><code>IMatchScoreService</code> / <code>MatchScoreService</code> 提供结果写入、清除、BO 模式重算和队伍映射。</td></tr><tr><td>后台 UI</td><td><code>ScorePageViewModel</code> 结果按钮直接操作 service；移除手动 Game/half 选择、手动累加和同步按钮；只读预览表由 <code>MatchScoreState.Games</code> 派生。</td></tr><tr><td>前台绑定</td><td><code>ScoreSurWindow</code>、<code>ScoreHunWindow</code>、<code>ScoreGlobalWindow</code>、<code>CutSceneWindow</code>、<code>GameDataWindow</code> 的 v3 layout 绑定 <code>MatchScoreState</code> 派生字段。</td></tr><tr><td>全局比分</td><td>内置 <code>GlobalScoreRow</code> 控件，由 <code>Cells</code> 显式配置比分格，运行时从 <code>MatchScoreState</code> 解析；BO3/BO5 由通用 Canvas states 控制。</td></tr><tr><td>清理</td><td><code>GameGlobalInfoRecord</code>、<code>ScoreWindowViewModel</code> 总分字段、<code>FrontedWindowService.SetGlobalScore*</code> 调用链已完全移除。</td></tr></tbody></table><h2 id="_12-重要警告" tabindex="-1"><a class="header-anchor" href="#_12-重要警告"><span>12. 重要警告</span></a></h2><ol><li>不要把权威比分状态存到 UI 控件里。</li><li>不要继续让 <code>ScorePageViewModel</code> 作为比分数据库。</li><li>不要把新的 v3 前台控件绑定到 <code>ScoreWindowViewModel</code> 独有字段。</li><li>不要把 <code>MatchScoreState</code> 派生值同步回 <code>Team.Score</code>；旧字段只用于旧数据兼容。</li><li><code>GameProgress.Free</code> 不能写入当前半场结果，但总分重算仍必须按 BO 模式执行。</li><li><code>Game3Overtime*</code> 与 <code>Game4*</code> 当前 enum 数值重叠，映射实现必须显式处理上下文。</li></ol><h2 id="_13-待确认问题" tabindex="-1"><a class="header-anchor" href="#_13-待确认问题"><span>13. 待确认问题</span></a></h2><table><thead><tr><th>问题</th><th>当前建议</th></tr></thead><tbody><tr><td><code>Free</code> 模式是否允许手动写比分</td><td>暂不支持，记录为设计缺口。暂时在Free下禁用相关按钮，且对外显示全部为 0</td></tr><tr><td>BO3 中第三场加赛与 BO5 第四场的持久化 key</td><td>使用 <code>ScoreGameKey</code>，避免只靠 <code>GameProgress</code> 数值。</td></tr><tr><td>旧 <code>Team.Score</code> 字段何时删除</td><td>等旧 DTO 和 legacy 导入路径完全收口后再删除。旧记录中的 <code>Team.Score</code> 无法安全还原完整 per-Game/per-Half 历史；导入器不会伪造半场结果。</td></tr><tr><td>全局比分 v3 控件类型</td><td>已新增内置 <code>GlobalScoreRow</code>，通过统一 V3 Control API（<code>FrontedV3ControlBase</code> + <code>[FrontedV3Control]</code> + <code>AddFrontedV3Control&lt;TControl&gt;()</code>）注册并由 JSON <code>ControlType = &quot;GlobalScoreRow&quot;</code> 使用。</td></tr></tbody></table>`,105)])])}const l=d(c,[["render",r]]),m=JSON.parse('{"path":"/dev/business/score-system-v2.html","title":"Score System v2 设计文档","lang":"zh-CN","frontmatter":{"description":"Score System v2 设计文档 本文定义 Score System v2 的目标模型、计算规则、前台绑定方向和迁移计划。 当前实现状态：旧比分写入路径清理已完成。现有 Core.Models.Game 已持有 MatchScoreState，后台 ScorePageViewModel 的结果按钮通过 IMatchScoreService.Se...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Score System v2 设计文档\\",\\"image\\":[\\"\\"],\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Zero PLFJY\\",\\"url\\":\\"https://plfjy.top/\\"}]}"],["meta",{"property":"og:url","content":"https://docs.bpsys.plfjy.top/dev/business/score-system-v2.html"}],["meta",{"property":"og:site_name","content":"neo-bpsys-wpf"}],["meta",{"property":"og:title","content":"Score System v2 设计文档"}],["meta",{"property":"og:description","content":"Score System v2 设计文档 本文定义 Score System v2 的目标模型、计算规则、前台绑定方向和迁移计划。 当前实现状态：旧比分写入路径清理已完成。现有 Core.Models.Game 已持有 MatchScoreState，后台 ScorePageViewModel 的结果按钮通过 IMatchScoreService.Se..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}]]},"git":{},"readingTime":{"minutes":15.5,"words":4651},"filePathRelative":"dev/business/score-system-v2.md","autoDesc":true}');export{l as comp,m as data};
